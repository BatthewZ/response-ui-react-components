// Components — data-display (dashboard primitives).
// Sparkline, ProgressRing, Meter, DescriptionList, ActivityFeed, … are added
// here per implementation phase. Kept as its own group alongside ui/form/layout
// so "dashboard primitives" is a discoverable category; existing components
// (StatCard, Timeline, Table, DataTable) are NOT moved.
export {};
