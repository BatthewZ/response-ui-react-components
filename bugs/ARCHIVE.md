# Archive — closed findings

Rows that are done or decided. **Ids are never reused**; a row here keeps the number it was
filed under, because other rows and commit messages cite it. Nothing is deleted from this file —
a refutation or a declined decision is a result, and the reason this repo writes them down is
that one living only in a commit body is invisible to the next reader.

Anchors here are *not* maintained. A `fixed` row points at code that changed by definition, so
its line number and fingerprint record where the defect was, not where anything is now. Do not
re-anchor them; do not gate on them.

Open work is in [`LEDGER.md`](./LEDGER.md).

### Fixed

Each row names the commit that closed it. Where two commits were needed the row names both — one instance fixed was not a row closed.

| # | Status | Component | File:line | Sev | Summary |
| - | ------ | --------- | --------- | --- | ------- |
| 2 | fixed · 4fd9f22 | Alert | [Alert.tsx:28](src/components/ui/Alert.tsx#L28 "fp:51180423") | med | `aria-live="polite"` downgrades `role="alert"` for every variant |
| 4 | fixed · fa635d2 | Card | [Card.tsx:36](src/components/ui/Card.tsx#L36 "fp:72c385f6") | med | Uses `bg-surface-0`; contract designates `--C-SURFACE-1` for cards **Fixed, and it exposed a collision — see #478.** The contract does designate `--C-SURFACE-1` for cards (`docs/theme-contract.md:39`), verified before moving. ProgressBar's empty track is *also* `--C-SURFACE-1`, so inside a Card that pairing is now exactly 1.00:1. |
| 5 | fixed · a4d2c12 | StatCard | [StatCard.tsx:80](src/components/ui/StatCard.tsx#L80 "fp:94298714") | med | `animateValue` freezes on first target; changed `to` never re-animates |
| 7 | fixed · a4d2c12 | DescriptionList | [DescriptionList.tsx:16](src/components/data-display/DescriptionList.tsx#L16 "fp:c9484a1e") | med | Horizontal (default) layout misaligns a term's 2nd+ detail **Fixed, layout unverified.** `[&>dt]:col-start-1 [&>dd]:col-start-2` pins each pair to its column (static literals, scanner-visible). The test asserts the classes; jsdom performs no layout, so the misalignment itself is not re-measured. |
| 9 | fixed · ab8a0bf | ScrollReveal | [ScrollReveal.tsx:91](src/components/animation/ScrollReveal.tsx#L91 "fp:40ca091a") | **high** | `as` types full element props but `...rest` is never spread onto `<Tag>` → props dropped |
| 10 | fixed · ab8a0bf | Stagger | [Stagger.tsx:22](src/components/animation/Stagger.tsx#L22 "fp:541815b8") | **high** | Rest props accepted by type but dropped at runtime (no `...rest`) |
| 11 | fixed · 656a6e0 | RequireAuth | [RequireAuth.tsx:50](src/components/guards/RequireAuth.tsx#L50 "fp:be4df10f") | med | Inline ref callback `ref={el=>el?.click()}` re-fires navigation every render |
| 12 | fixed · 656a6e0 | RequireAuth | [RequireAuth.tsx:25](src/components/guards/RequireAuth.tsx#L25 "fp:b7f7d17c") | low | Docblock "hard navigation via `<a>`" is misleading (delegates to adapter Link) |
| 13 | fixed · 418ff90 | AnimatePresence | [AnimatePresence.tsx:62](src/components/animation/AnimatePresence.tsx#L62 "fp:a31c9655") | med | Caller `onAnimationEnd` replaces internal handler → element never unmounts |
| 14 | fixed · 5b258f4 | AnimatePresence | [AnimatePresence.tsx:41](src/components/animation/AnimatePresence.tsx#L41 "fp:8c5deeb2") | med | No `target===currentTarget` guard → descendant `animationend` unmounts mid-exit |
| 15 | fixed · c47b1c8 | AnimatePresence | [AnimatePresence.tsx:49](src/components/animation/AnimatePresence.tsx#L49 "fp:c47161d2") | low | `exitClass` without a CSS animation → mounted forever (no timeout fallback) |
| 18 | fixed · c47b1c8 | ViewTransition | [ViewTransition.tsx:48](src/components/animation/ViewTransition.tsx#L48 "fp:facbd63d") | med | `useViewTransition` never awaits `navigate`'s promise → async routers get no transition |
| 19 | fixed · c47b1c8 | ViewTransition | [ViewTransition.tsx:47](src/components/animation/ViewTransition.tsx#L47 "fp:6622504a") | med | Neither export imports `usePrefersReducedMotion`. Blast radius narrower than stated: (css pkg) `animations/view-transitions.css:22-27` already zeroes `animation-duration` under `prefers-reduced-motion` for `::view-transition-old(root)`/`-new(root)`, so `useViewTransition` page transitions are neutralised; a *named* `<ViewTransition name="x">` group is not |
| 20 | fixed · c47b1c8 | ViewTransition | [ViewTransition.tsx:1](src/components/animation/ViewTransition.tsx#L1 "fp:ea4ee469") | low | `"use client"` forces the pure component into the client bundle |
| 22 | fixed · d08ad08 | Meter | [Meter.tsx:71](src/components/data-display/Meter.tsx#L71 "fp:dc056a0f") | low | `aria-valuenow` is unclamped; can exceed `aria-valuemax` |
| 23 | fixed · e4f27c3 | Field | [Field.tsx:48](src/components/form/Field.tsx#L48 "fp:c6680dc9") | med | Control emits `aria-describedby` to a FieldError id that isn't rendered → dangling ref **Root cause was ownership.** `useFieldError` published `field.errorId` to every invalid control, but that id exists only if a `FieldError` actually rendered. Now the element that owns the id publishes it: `FieldError` registers what it rendered, `Field` stores it, `useFieldError` emits that. The test helper resolves the IDREF and **throws** when it names nothing, so a dangling reference fails instead of passing a string comparison. |
| 25 | fixed · 0903176 (response-ui-css) | Checkbox | [Checkbox.tsx:17](src/components/form/Checkbox.tsx#L17 "fp:ea063000") | med | `focus:ring-offset-2` uses Tailwind default `#fff` → white halo on dark themes |
| 26 | fixed · e4f27c3 | Checkbox | [Checkbox.tsx:16](src/components/form/Checkbox.tsx#L16 "fp:1c2cebaa") | low | Themed border/radius are dead styling on a native checkbox (no `appearance-none`) **Confirmed dead by measurement, then removed.** One element, fixed position, only the declaration changing, element screenshots compared byte-for-byte in Firefox 146 and Chrome 144: the themed border/radius, and an exaggerated 4px/8px version, render identical bytes to no border at all — while an `appearance:none` control differed, proving the declarations were reachable and the probe honest. |
| 27 | fixed · b742101 | Input · Select · Textarea | [Input.tsx:1](src/components/form/Input.tsx#L1 "fp:750ac00b") | med | Calls `useFieldError` (exported from the `"use client"` module Field.tsx) with no directive of its own → RSC direct-render fails. **Not Input-only:** Select.tsx:1 and Textarea.tsx:1 have the identical shape; every other `useFieldError` consumer does carry the directive. `verify-directives` passes on all three — it has a blind spot for context-only hooks |
| 28 | fixed · a4d2c12 | ActivityFeed | [ActivityFeed.tsx:18](src/components/data-display/ActivityFeed.tsx#L18 "fp:d52163d3") | low | `<ol>` `list-style:none` without `role="list"` → Safari+VoiceOver drops list semantics **Fixed, announcement unverified** — same fix and same limit as #145: the attribute is asserted, the Safari+VoiceOver announcement is not reachable from this test environment. |
| 30 | fixed · a4d2c12 | Sparkline | [Sparkline.tsx:97](src/components/data-display/Sparkline.tsx#L97 "fp:e0609d93") | low | Single-value line/area can render an invisible point (silent no-op) |
| 31 | fixed · 81e532a | Parallax | [Parallax.tsx:63](src/components/animation/Parallax.tsx#L63 "fp:d9913d9a") | low | Residual `translateY` not cleared on reduced-motion toggle / effect teardown |
| 32 | fixed · 81e532a | Parallax | [Parallax.tsx:60](src/components/animation/Parallax.tsx#L60 "fp:e6f86527") | low | No resize/orientationchange recompute → viewport-center offset goes stale **Measured in Firefox 146, and the row understated it.** Shrinking the viewport 720px→400px left `translateY(246px)` where 150px was correct — 96px of drift, held until the next scroll. A `resize` listener now shares the existing rAF throttle; re-measured at 0 drift. |
| 33 | fixed · 81e532a | Parallax | [Parallax.tsx:75](src/components/animation/Parallax.tsx#L75 "fp:86522262") | low | `will-change: transform` for the element's whole life parks a permanent compositor layer **Confirmed in Firefox:** an element 1326px below the fold reported `will-change: transform`. Now gated on an IntersectionObserver with the package's existing `rootMargin` pattern, falling back to a lifetime hint where IO is absent — a layer beats no layer, and the fallback keeps SSR/jsdom behaviour unchanged. |
| 34 | fixed · 4fd9f22 | ErrorBoundary | [ErrorBoundary.tsx:27](src/components/ui/ErrorBoundary.tsx#L27 "fp:8cfa0f5c") | med | Fallback uses raw Tailwind defaults (`text-2xl`, `mb-2`, `mb-6`, `px-4 py-2`) instead of design tokens |
| 35 | fixed · 0903176 (response-ui-css) | **library-wide** | 5 files, see detail | med | `ring-offset-2` with `ring-offset-color` set **nowhere** → white focus halo on dark themes. Affects Button, IconButton, Checkbox, AvatarUpload, ErrorBoundary (supersedes #25) |
| 36 | fixed · 4fd9f22 | ErrorBoundary | [ErrorBoundary.tsx:29](src/components/ui/ErrorBoundary.tsx#L29 "fp:f7930ff9") | low | Hand-rolled `<button>` re-implements Button's styling instead of composing `Button` |
| 37 | fixed · 4fd9f22 | ErrorBoundary | [ErrorBoundary.tsx:25](src/components/ui/ErrorBoundary.tsx#L25 "fp:6de80c11") | low | No `fallback` prop; the fixed `min-h-screen` fallback makes it unusable for scoped/inline boundaries |
| 38 | fixed · 4fd9f22 | Spinner | [Spinner.tsx:7](src/components/ui/Spinner.tsx#L7 "fp:40925a98") | med | `animate-spin` has no `prefers-reduced-motion` guard — the library's only unguarded continuous animation |
| 39 | fixed · 4fd9f22 | Spinner | [Spinner.tsx:30](src/components/ui/Spinner.tsx#L30 "fp:25f0919f") | med | Visually hidden "Loading" is hard-coded English and unreachable — `children` is omitted from the prop type |
| 40 | fixed · 4fd9f22 | Spinner | [Spinner.tsx:26](src/components/ui/Spinner.tsx#L26 "fp:70100588") | low | Every instance is its own already-full `role="status"` live region; N spinners = N live regions |
| 41 | fixed · 6626d7f | IconButton | [IconButton.tsx:16](src/components/ui/IconButton.tsx#L16 "fp:b2a7507e") | med | No default `type="button"` → renders as `submit`; Toast/Pagination/Carousel call sites submit their enclosing form |
| 42 | fixed · fa635d2 | IconButton | [IconButton.tsx:6](src/components/ui/IconButton.tsx#L6 "fp:cd22428b") | low | Required `aria-label: string` accepts `""`, and `aria-labelledby` cannot substitute for it **Two-arm union (`aria-label` | `aria-labelledby`), intersected rather than `Omit`-ed** — `Omit` plus a rest spread trips `verify-omit-discipline`, since both keys must still reach the `<button>`. The empty-string half cannot be typed away (TypeScript cannot say "non-empty" of a `string`), so it is a render-time warning, which also catches the variable-empty case no type trick reaches. BREAKING for callers passing neither. |
| 43 | fixed · fa635d2 | IconButton | [IconButton.tsx:10](src/components/ui/IconButton.tsx#L10 "fp:e4081a8a") | low | `active:scale-95` has no reduced-motion guard (WCAG 2.3.3) |
| 45 | fixed · fa635d2 | Badge | [Badge.tsx:7](src/components/ui/Badge.tsx#L7 "fp:50399e09") | low | `text-body-3` line-height with no leading reset inflates every chip (~2.25rem on the default scale) (the ~2.25rem figure is unrendered arithmetic; the missing leading reset is the verified part) **The row's ~2.25rem arithmetic was confirmed as pixels:** Firefox rendered the chip at exactly 36px = 28px `--BodyText-3-line-height` + 2×4px padding; 21px after the leading reset. |
| 46 | fixed · 3b15db9 | ToastContext | [ToastContext.tsx:136](src/components/ui/ToastContext.tsx#L136 "fp:871200fa") | **high** | `createPortal(…, document.body)` with no `typeof document` guard → `ToastProvider` throws on any server render |
| 47 | fixed · 4fd9f22 | Portal | [Portal.tsx:10](src/components/ui/Portal.tsx#L10 "fp:7ab17c33") | med | SSR guard stops the server throw but guarantees a hydration mismatch for any unconditional Portal |
| 48 | fixed · fa635d2 | Kbd | [Kbd.tsx:6](src/components/ui/Kbd.tsx#L6 "fp:793f000b") | low | Keycap never reads `--DEFAULT-MONO-FONT`; falls through to Tailwind Preflight's default mono stack |
| 49 | fixed · fa635d2 | Kbd | [Kbd.tsx:6](src/components/ui/Kbd.tsx#L6 "fp:793f000b") | low | Package's only `font-medium` (off-contract weight); no leading reset, so cap height is purely `--BodyText-3-line-height` |
| 50 | fixed · aafb9f8 | TagInput | [TagInput.tsx:190](src/components/form/TagInput.tsx#L190 "fp:b85ffcd1") | low | TagInput.tsx:190 now renders `<Badge className="gap-r6">` instead of a verbatim copy of Badge's class string, so chip styling has one source again. `d14c7be` moved `tag-input.md`'s five chip rows onto Badge and cross-linked them |
| 51 | fixed · cbc6876 (response-ui-css) | **response-ui-css** | `tokens/colors.css:23` + all 3 themes | med | `--C-TEXT-MUTED` fails WCAG AA **and** AA-large on every surface of every shipped theme (max 2.59:1). Retuned in lightness only, hue and chroma untouched; now 4.50–5.23:1 on `--C-SURFACE-0`/`-1`/`-2`. `--C-SURFACE-3` stays at 3.92–4.10:1 and is deliberately excluded — every muted-on-`SURFACE-3` pairing in this package is a disabled control (`.combobox-input:disabled`, `.multiselect[data-disabled]`, the shared `disabled:bg-surface-3` recipe), which WCAG 2.2 §1.4.3 exempts. **The instance rows that cite this token's old ratios are not closed by this and must each be re-measured** — several pair the muted half with a second, unfixed defect (#354's `--C-ACCENT` arrow, #414's `--C-BORDER-DEFAULT` dashed border) |
| 54 | fixed · a4d2c12 | Text | [Text.tsx:60](src/components/ui/Text.tsx#L60 "fp:cea113bc") | low | `weight` builds a dynamic class (`` `font-${weight}` ``) invisible to Tailwind's scanner; `font-bold` survives only via two unrelated literals **Fixed, verified by reading only.** `weight` now composes from a static `weightClassMap` the Tailwind scanner can see. No failing test was possible — the emitted class string is identical either way; what changed is whether the class is *emitted into the build*, which is a build-time property no unit test in this package can observe. |
| 55 | fixed · ee5d181 | Avatar | [Avatar.tsx:65](src/components/ui/Avatar.tsx#L65 "fp:3b30c6d5") | med | `imgError` latch never reset when `src` changes — a recovered URL never renders again |
| 56 | fixed · ee5d181 | Avatar | [Avatar.tsx:59](src/components/ui/Avatar.tsx#L59 "fp:ef8548cf") | med | `children` typechecks (no `Omit`) but is silently dropped at runtime; Skeleton/Spinner get this right |
| 57 | fixed · ee5d181 | Avatar | [Avatar.tsx:100](src/components/ui/Avatar.tsx#L100 "fp:9e1eacdf") | med | Presence dot conveys status by colour alone — unlabelled `<span>`, no role/text/ARIA |
| 58 | fixed · ee5d181 | Avatar | [Avatar.tsx:73](src/components/ui/Avatar.tsx#L73 "fp:558967cf") | med | `role="img"` applied unconditionally, even when `alt` and `name` are both absent/blank |
| 59 | fixed · ee5d181 | AvatarGroup | [Avatar.tsx:137](src/components/ui/Avatar.tsx#L137 "fp:54c64358") | med | `size` sizes only the overlap and the `+N` chip; it never reaches the children |
| 60 | fixed · ee5d181 | Avatar | [Avatar.tsx:24](src/components/ui/Avatar.tsx#L24 "fp:07b19d84") | low | `xs` initials use raw `text-[0.625rem]` — off-contract and non-responsive |
| 62 | fixed · ee5d181 | Avatar | [Avatar.tsx:49](src/components/ui/Avatar.tsx#L49 "fp:4564ffb8") | low | `getInitials` indexes by UTF-16 code unit, so an astral first char yields a lone surrogate |
| 63 | fixed · 4fd9f22 | Skeleton | [Skeleton.tsx:27](src/components/ui/Skeleton.tsx#L27 "fp:d0d9cdb3") | med | Every instance is its own `role="status"` region named "Loading" — a 4-skeleton card mounts 4 live regions |
| 64 | fixed · 4fd9f22 | Skeleton | [Skeleton.tsx:33](src/components/ui/Skeleton.tsx#L33 "fp:135d65b1") | med | Hidden "Loading" text is hard-coded English and unreachable (`children` omitted) — same defect as #39 |
| 65 | fixed · 4fd9f22 | Skeleton | [Skeleton.css:8](src/components/ui/Skeleton.css#L8 "fp:eb565ac5") | low | Only `.skeleton--text` has a height; the other three variants measure 0px without an explicit `height` |
| 68 | fixed · ee5d181 | CopyButton | [CopyButton.tsx:43](src/components/ui/CopyButton.tsx#L43 "fp:6c1fd94d") | med | A failed or unavailable clipboard write is completely unobservable — no error state, no callback, nothing logged |
| 69 | fixed · ee5d181 | CopyButton | [CopyButton.tsx:60](src/components/ui/CopyButton.tsx#L60 "fp:8a571499") | med | The polite live region sits **inside** the `<button>`, a role with Children Presentational: True |
| 70 | fixed · ee5d181 | CopyButton | [CopyButton.tsx:34](src/components/ui/CopyButton.tsx#L34 "fp:748cd9d0") | low | Unmount during the in-flight write sets state post-unmount and leaks one timer for `timeout` ms |
| 71 | fixed · 7d48730 | CopyButton | [CopyButton.tsx:55](src/components/ui/CopyButton.tsx#L55 "fp:5c17b815") | low | `{...props}` moved above `data-copied`/`onClick`, so a caller can no longer replace the copy handler or make `data-copied` lie about state it cannot know. `type`/`aria-label` stay *deliberately* above the spread — 7d48730 resolved per attribute and left author-facing ones overridable; `copiedLabel` is the supported relabelling path |
| 72 | fixed · ee5d181 | CopyButton | [CopyButton.tsx:14](src/components/ui/CopyButton.tsx#L14 "fp:cf897261") | low | `copiedLabel=""` compiles and blanks the accessible name for the whole confirmation window |
| 73 | fixed · ee59e65 · aafb9f8 | Radio | [Radio.tsx:18](src/components/form/Radio.tsx#L18 "fp:5fb8e94d") | **high** | **This row was a phantom from `ee59e65` (2026-07-26) until now** — it read `confirmed` for the repo's highest-severity open finding while its own sentence was false at source, the exact error the preamble names as the most expensive this file can carry. `ee59e65` already paired that reset with `focus:ring-2 focus:ring-border-focus focus:ring-offset-2`, which is why the row was false the day it was written. `aafb9f8` hoisted the pair into `util/focus.ts` and re-keyed it `focus-visible:`; that keying was reverted, because the `focus:` / `focus-visible:` split is a deliberate partition by element category — buttons `focus-visible:`, native form controls `focus:`. `Radio.tsx:18-19` today reads `focusOutlineResetControl` (`focus:outline-none`) followed by `focusRingControl` (`ring-2 ring-transparent focus:ring-border-focus focus:ring-offset-0 focus:border-border-focus`), so the reset this row describes is in the file and its replacement arrives with it, from one recipe. `verify:focus-affordance` covers the pairing since `6ed0ad3` taught the gate to read Tailwind utilities in `.tsx` |
| 74 | fixed · 6626d7f | **Button** | [Button.tsx:36](src/components/ui/Button.tsx#L36 "fp:c5c61ab2") | med | No default `type="button"` — same defect as #41 with the widest blast radius; a Cancel button becomes the form's default submitter |
| 75 | fixed · e4f27c3 | Radio | [Radio.tsx:20](src/components/form/Radio.tsx#L20 "fp:9abd3405") | med | Never consumes `useFieldError`, so a Radio in an invalid `Field` gets no `aria-describedby` **Re-scoped before archiving, 2026-07-26, on the owner's decision — the sentence above is not the one this row was filed with.** As filed it read "…gets no `aria-invalid`/`aria-describedby`", and the `aria-invalid` clause is **refuted**: ARIA 1.2 does not support `aria-invalid` on the `radio` role, verified offline against the installed `aria-query@5.3.2` role table (`radio` false; `checkbox`/`radiogroup`/`switch` true), and `radio` subclasses `input`, not `checkbox`, so it inherits nothing. `radio.md` had argued exactly this in two places *before* the row was written. Setting it would have been the same defect class as #314 — the row as filed asked for a defect, which is why it was rewritten rather than archived as-is. The half that was real is fixed: Radio emits `aria-describedby` through a module-internal `useFieldDescription` derived from `useFieldError` so there is one source, and a test asserts it **never** emits `aria-invalid`, with the reason in the test name. |
| 76 | fixed · e4f27c3 | Checkbox | [Checkbox.tsx:12](src/components/form/Checkbox.tsx#L12 "fp:5df62807") | med | Same as #75 — Radio and Checkbox are the form module's only two unwired controls |
| 77 | fixed · b742101 | Select | [Select.tsx:33](src/components/form/Select.tsx#L33 "fp:456f66d8") | med | Chevron data-URI uses `fill="currentColor"`, which cannot resolve in an SVG-as-image — arrow paints black on every theme (≈1.10:1 on grimdark). **Understated — re-measured 2026-07-26:** the hardcoded black chevron is **1.06:1 on `tech`** and 1.10:1 on grimdark, not grimdark alone. `appearance-none` strips the UA arrow, so this graphic is the only 'I am a dropdown' cue, and the black is this package's own hardcode rather than a theme choice |
| 79 | fixed · d08ad08 | Switch | [Switch.tsx:64](src/components/form/Switch.tsx#L64 "fp:fa06d0f8") | med | Hidden input never receives `disabled`, so a disabled Switch still submits its value |
| 80 | fixed · 4800320 | Slider | [Slider.css:56](src/components/form/Slider.css#L56 "fp:b2747c00") | med | `::-moz-range-thumb` lacks `box-sizing: border-box`, so the handle is 24px in Firefox vs 20px in Chromium (same in RangeSlider). **Left open deliberately:** source facts verified (no `box-sizing` on either thumb pseudo-element, both `1.25rem` + `2px`, and Preflight's `*, ::before, ::after` reaches neither), but the claim is a UA-default divergence that needs a real Firefox *and* Chromium render — jsdom applies no stylesheets **Settled 2026-07-26 against Firefox 146 and Chromium 144**, the render this row waited for. True as written: measured 24.00px in Firefox and 20.00px in Chromium across four variants (both components, as-authored and with a tall input to rule out clipping), and `getComputedStyle(el, '::-moz-range-thumb').boxSizing` returns `content-box` in Firefox — the mechanism, not just the symptom. `box-sizing: border-box` on both thumb pseudo-elements brings all four variants to 20.00px in both engines at identical x-extents. |
| 81 | fixed · 567061e | Input · Select · Textarea | [Input.tsx:33](src/components/form/Input.tsx#L33 "fp:a39d8424") | low | `focus:border-border-focus` survives the invalid swap — focused invalid control shows a focus-blue border inside an error-red ring |
| 82 | fixed · d08ad08 | Switch | [Switch.tsx:64](src/components/form/Switch.tsx#L64 "fp:fa06d0f8") | low | Unchecked submits `name=""` instead of omitting the field, so `FormData.has(name)` is always true |
| 83 | fixed · d08ad08 | Switch | [Switch.tsx:64](src/components/form/Switch.tsx#L64 "fp:fa06d0f8") | low | `form` is not forwarded to the hidden input; a Switch outside its `<form>` submits nothing |
| 84 | fixed · 567061e | Switch · Slider | [Switch.css:40](src/components/form/Switch.css#L40 "fp:24f6b9bd") | low | `:focus-visible` and `[aria-invalid="true"]` are the same specificity and focus is written second — keyboard focus erases the error outline |
| 85 | fixed · 7d48730 | Switch | [Switch.tsx:60](src/components/form/Switch.tsx#L60 "fp:522acb8c") | low | `{...props}` spreads after `role`/`aria-checked`/`data-state`, so a caller can desync the announced state from the thumb |
| 86 | fixed · b742101 | Slider | [Slider.tsx:59](src/components/form/Slider.tsx#L59 "fp:b3dccb1e") | low | `--slider-fill` uses the raw prop while the browser step-rounds the rendered value — fill edge and thumb visibly disagree |
| 88 | fixed · 4800320 | RangeSlider | [RangeSlider.css:111](src/components/form/RangeSlider.css#L111 "fp:64938929") | low | Dead `::-moz-range-track` rule and a false comment ("Firefox paints a default track"). **Left open deliberately:** the rule is at RangeSlider.css:117-119 and the input is already `background: transparent; appearance: none` at :44-54, but whether Firefox's UA still paints a track box under `appearance:none` — which decides whether the rule is dead or load-bearing — needs a Firefox render **Settled 2026-07-26 against Firefox 146.** True in both clauses: under the `appearance: none` already set at `:44-54`, Firefox paints no track box of any colour, so the rule was dead and its comment false. Three renders — rule present, rule deleted, and a positive control giving the pseudo-element a red background, which painted 400x4px exactly centred. The control is what makes the null results mean anything; re-run on a black page to close the white-on-white hole. The rule is replaced by a comment recording the measurement, because the next reader's instinct on finding no `::-moz-range-track` rule is to add one back for the reason the old comment gave. |
| 89 | fixed · b742101 | Select | [Select.tsx:27](src/components/form/Select.tsx#L27 "fp:8bfea927") | low | `placeholder:text-fg-muted` is dead — `::placeholder` does not match a `<select>` |
| 91 | fixed · 656a6e0 | ThemeSwitcher | [ThemeSwitcher.tsx:26](src/components/ui/ThemeSwitcher.tsx#L26 "fp:43ca0846") | med | `role="radiogroup"`/`role="radio"` with no roving focus — no `tabIndex`, no `onKeyDown`, though `useRovingFocus` exists and `Rating` uses it **Deliberately did not use `useRovingFocus` whole.** A radiogroup's tab stop must *be* the checked radio, and the hook's own `onKeyDown` moves focus without selecting — using it whole reproduces exactly the desync #470 records Rating having removed. It takes `tabIndex`/`ref`/`setFocusedIndex` from the hook, runs its own key handler, and derives the roving index from the theme so the value stays one state machine. The hook needed no change. |
| 92 | fixed · 656a6e0 | ThemeSwitcher | [ThemeSwitcher.tsx:20](src/components/ui/ThemeSwitcher.tsx#L20 "fp:dc192c67") | med | Calls `useTheme()` with no options, so themes registered via `useTheme({themes})` are invisible and mis-reported |
| 93 | fixed · 656a6e0 | ThemeSwitcher | [ThemeSwitcher.tsx:9](src/components/ui/ThemeSwitcher.tsx#L9 "fp:f6351b0a") | med | `LABELS` is module-private English and `children` is omitted — option text is unreachable in any locale |
| 94 | fixed · d706bf2 | ThemeSwitcher | [ThemeSwitcher.css:14](src/components/ui/ThemeSwitcher.css#L14 "fp:d29ac9b1") | low | Option type is literal `0.8125rem`/`500`, not `--BodyText-2`/`--Semibold-Weight` — labels ignore theme typography |
| 95 | fixed · c3f244b (ThemeSwitcher) · aafb9f8 (Collapsible) | ThemeSwitcher · Collapsible | [ThemeSwitcher.css:26](src/components/ui/ThemeSwitcher.css#L26 "fp:6a6e8a2a") | low | Both halves closed, two commits — one instance fixed is not a row closed. `ThemeSwitcher.css:26-29` adds `:focus-visible { outline: 2px solid var(--C-BORDER-FOCUS); outline-offset: -2px }`, inset like the sibling segmented control `.tabs-tab` because the group pads its options by 0.125rem. `Collapsible.tsx:96` carries `focusRing` as a utility, because `.collapsible-trigger` is a styling hook with no rule behind it |
| 96 | fixed · 4fd9f22 | Dialog · Drawer | [Dialog.tsx:39](src/components/ui/Dialog.tsx#L39 "fp:04c21176") | med | Neither listens for `close`, so `<form method="dialog">` / `ref.close()` closes the element while `open` stays `true` and it cannot reopen |
| 97 | fixed · 4fd9f22 | Dialog | [Dialog.tsx:48](src/components/ui/Dialog.tsx#L48 "fp:3994017a") | low | Scrim is a literal `backdrop:bg-black/50`, ignoring `--OVERLAY-SCRIM-COLOR` — Drawer tokenises it correctly |
| 98 | fixed · d706bf2 | Drawer · FileUpload | [Drawer.css:9](src/components/ui/Drawer.css#L9 "fp:e97f935d") | low | `color: var(--C-TEXT-DEFAULT)` reads a token **nothing defines** → invalid at computed-value time → `color` falls back to `inherit`. Same line at `FileUpload.css:344` |
| 99 | fixed · 4fd9f22 | Dialog | [Dialog.tsx:47](src/components/ui/Dialog.tsx#L47 "fp:ee9f8db5") | low | `animate-fade-in` has no `motion-reduce:` sibling; the CSS package guards the `.fade-in` *class*, not the utility |
| 100 | fixed · 4fd9f22 | Drawer | [Drawer.tsx:16](src/components/ui/Drawer.tsx#L16 "fp:e856091e") | low | `onClose` shadows the native `<dialog>` `onClose` and is destructured out, making the DOM handler reachable only via `ref` **Fixed, type-level only.** `Omit<…, "open" | "onClose">` plus a docblock, on **both** Drawer and Dialog — Dialog carried the identical shadowing the row logged only against Drawer. `tsc` is the check; no runtime test is possible for a type that no longer permits the call. |
| 101 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:80](src/components/ui/ToastContext.tsx#L80 "fp:058df32f") | med | `dismissAll()`'s 300 ms sweep calls `setToasts([])` unconditionally, deleting toasts queued after it |
| 102 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:85](src/components/ui/ToastContext.tsx#L85 "fp:1b2053b5") | med | `crypto.randomUUID()` is unguarded and secure-context-only — every `toast()` throws on plain http |
| 103 | fixed · 4fd9f22 | Toast | [Toast.tsx:49](src/components/ui/Toast.tsx#L49 "fp:9440ece8") | med | Live region is inserted with its message already inside it, and the always-mounted container has no `aria-live` — same class as #39/#64 |
| 105 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:57](src/components/ui/ToastContext.tsx#L57 "fp:ae22bc77") | low | Removal timer stored under `` `${id}-remove` `` but deleted under `id` — the Map grows one dead entry per dismissed toast |
| 106 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:80](src/components/ui/ToastContext.tsx#L80 "fp:058df32f") | low | The dismissAll sweep and the overflow eviction are untracked `setTimeout`s, missed by the unmount cleanup |
| 107 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:35](src/components/ui/ToastContext.tsx#L35 "fp:3e6bf7bf") | low | 300 ms removal is hard-coded against themeable `--MOTION-DURATION-EXIT`; shipped `grimdark` sets 350 ms, so its toast exits are already truncated |
| 108 | fixed · 4fd9f22 | Toast | [Toast.tsx:46](src/components/ui/Toast.tsx#L46 "fp:eac89b24") | low | `animate-slide-in-right`/`-out-right` ignore `prefers-reduced-motion` (the package block covers only `.fade-*`) |
| 109 | fixed · 4fd9f22 | Toast | [Toast.tsx:56](src/components/ui/Toast.tsx#L56 "fp:cd36612c") | low | Dismissing drops focus to `<body>`; no focus restoration |
| 110 | fixed · 4fd9f22 | ToastContext | [ToastContext.tsx:102](src/components/ui/ToastContext.tsx#L102 "fp:e1dd417c") | low | `setTimeout` side effect inside the `setToasts` updater — impure, double-fires under StrictMode |
| 111 | fixed · 6626d7f | ErrorBoundary | [ErrorBoundary.tsx:29](src/components/ui/ErrorBoundary.tsx#L29 "fp:f7930ff9") | low | The fallback's hand-rolled `<button>` sets no `type`, so "Try again" also submits an enclosing form |
| 112 | fixed · 7d48730 | Tooltip | [Tooltip.tsx:82](src/components/ui/Tooltip.tsx#L82 "fp:f78c7c9f") | **high** | `getReferenceProps()` is called with no args, so a child's own `aria-describedby` is **destroyed** — its hint is never announced |
| 113 | fixed · 572cf53 | ContextMenu | [ContextMenu.tsx:85](src/components/ui/ContextMenu.tsx#L85 "fp:cd2d37b9") | **high** | Trigger `<div>` sets no `tabIndex`; Menu key / Shift+F10 fire `contextmenu` at `<body>` and never reach it — zero keyboard access |
| 114 | fixed · 572cf53 | ContextMenu | [ContextMenu.tsx:96](src/components/ui/ContextMenu.tsx#L96 "fp:d636cf5b") | **high** | `initialFocus={-1}` leaves `activeElement` on `<body>` after a right-click open, so arrows and typeahead do nothing |
| 115 | fixed · 7d48730 | **Popover · HoverCard · DropdownMenu** | [Popover.tsx:154](src/components/ui/Popover.tsx#L154 "fp:6cf4a5a1") | med | `asChild` clones trigger props **over** the child, dropping the child's handlers *and its `ref`*. Tooltip is the only one that merges the ref |
| 116 | fixed · fa1a38e | DropdownMenu | [DropdownMenu.css:41](src/components/ui/DropdownMenu.css#L41 "fp:53d6a6d8") | med | `outline:none` leaves the hover wash as the only focus cue — measured 1.02–1.07:1 in all four themes (SC 1.4.11 wants 3:1) |
| 117 | fixed · 6ed0ad3 | Popover | [Popover.tsx:197](src/components/ui/Popover.tsx#L197 "fp:bbf8d384") | med | `Popover.tsx:197` now reads `<FloatingFocusManager context={context} modal={false}>`, matching ColorPicker and DatePicker; the page outside the portal is no longer `aria-hidden`/inert and Tab is not trapped. Covered by a test added in the same commit. **Scoped to Popover only** — the row's siblings #114/#124/#131 (menu-internals.tsx) are untouched and stay open |
| 118 | fixed · 57c4753 | menu-internals | [menu-internals.tsx:234](src/components/ui/menu-internals.tsx#L234 "fp:a4b86c64") | med | A `disabled` Item still runs the caller's `onClick` — only `onSelect` is guarded, and no native `disabled` is set |
| 119 | fixed · 6626d7f | Popover · DropdownMenu | [Popover.tsx:166](src/components/ui/Popover.tsx#L166 "fp:85055480") | med | Trigger `<button>` has no `type`, so opening a menu inside a `<form>` also submits it (#74 again) |
| 120 | fixed · 4fd9f22 | Tooltip | [Tooltip.css:12](src/components/ui/Tooltip.css#L12 "fp:c32803e9") | med | `pointer-events:none` and no `safePolygon()` — the bubble cannot be hovered, failing WCAG 1.4.13 "Hoverable" **Fixed; one half unverified.** The `pointer-events: none` rule is gone from `Tooltip.css` (fail-first verified) and `handleClose: safePolygon()` is added. The safePolygon half has **no fail-first test** — jsdom synthesises no pointer path, so hover-onto-bubble passes with and without it. Asserted at rule level only. |
| 121 | fixed · 4fd9f22 | Tooltip | [Tooltip.tsx:101](src/components/ui/Tooltip.tsx#L101 "fp:0349fa08") | med | Portals to `<body>`, so a tooltip inside `Dialog`/`Drawer` paints under the modal's top layer; no portal-target prop |
| 122 | fixed · 4fd9f22 | HoverCard | [HoverCard.tsx:97](src/components/ui/HoverCard.tsx#L97 "fp:4911b6f9") | med | `role="dialog"` card gets no accessible name and the trigger no `aria-describedby` — announced as an unnamed dialog, contents never read |
| 124 | fixed · 4fd9f22 | ContextMenu | [ContextMenu.tsx:89](src/components/ui/ContextMenu.tsx#L89 "fp:f4aa3ba9") | med | `contextmenu` is not `stopPropagation`'d — nested triggers open **both** menus, each `aria-hidden`ing the other |
| 125 | fixed · 4fd9f22 | menu-internals | [menu-internals.tsx:123](src/components/ui/menu-internals.tsx#L123 "fp:848eef52") | med | `useListNavigation` on the reference `preventDefault`s ArrowUp/Down inside the trigger — a `<textarea>` there has its caret frozen |
| 126 | fixed · 4fd9f22 | menu-internals | [menu-internals.tsx:200](src/components/ui/menu-internals.tsx#L200 "fp:01b6e8b1") | med | `Item.index` is caller-assigned and unvalidated; duplicate indices make the earlier item permanently keyboard-unreachable, silently |
| 129 | fixed · 6ed0ad3 | Popover | [Popover.css:24](src/components/ui/Popover.css#L24 "fp:0adec377") | low | `Popover.css:24-27` adds `.popover-content:focus-visible { outline: 2px solid var(--C-BORDER-FOCUS); outline-offset: 2px }`, so the `outline: none` at :18 now has a replacement on the one element `FloatingFocusManager` actually focuses. Ring stays load-bearing after #117's `modal={false}` — focus still lands on the panel. `verify:focus-affordance` no longer prints `.popover-content` as an exemption. The DropdownMenu/HoverCard halves were already moot per the prior narrowing |
| 130 | fixed · cbc6876 (response-ui-css) | DropdownMenu | [DropdownMenu.css:52](src/components/ui/DropdownMenu.css#L52 "fp:13ebc25c") | low | Disabled items and labels paint `--C-TEXT-MUTED` on `--C-SURFACE-0` = 2.10–2.59:1, under AA (instance of #51). Closed by the `--C-TEXT-MUTED` retune (css `cbc6876`, in 0.9.0): the `.dropdown-menu-label` twin re-measures **4.85–5.23:1** on the panel's `SURFACE-0`, and the disabled items are WCAG 1.4.3-exempt regardless |
| 131 | fixed · 4fd9f22 | DropdownMenu | [menu-internals.tsx:231](src/components/ui/menu-internals.tsx#L231 "fp:227cf417") | low | Tab never closes the menu; a mouse-opened menu has no tabbable item, so Tab jumps past it leaving it open with focus outside |
| 132 | fixed · 4fd9f22 | HoverCard | [HoverCard.tsx:156](src/components/ui/HoverCard.tsx#L156 "fp:3026ef67") | low | The default (non-`asChild`) trigger is a non-focusable `<span>` carrying `aria-expanded` — invalid on a role-less span, and `useFocus` is dead on that path |
| 133 | fixed · 42db50f | Pagination | [Pagination.css:49](src/components/ui/Pagination.css#L49 "fp:23e0f89b") | **high** | Current page inks `--C-TEXT-ON-PRIMARY` on a `--C-ACCENT` fill; in `tech` those tokens are byte-identical → **1.00:1, invisible** |
| 134 | fixed · 340c66b | Stepper | [Stepper.tsx:131](src/components/ui/Stepper.tsx#L131 "fp:2f79a6e8") | **high** | A clickable **completed** step's button has an empty accessible name, and rest props land on the `<li>` so no `aria-label` can reach it |
| 135 | fixed · 7d48730 | Accordion | [Accordion.tsx:209](src/components/ui/Accordion.tsx#L209 "fp:7d85c7d7") | med | `Trigger` spreads `...props` **after** its own handlers, so a consumer `onClick` replaces the toggle and the section never opens |
| 136 | fixed · 42db50f + 875beb9 | Accordion · Collapsible | [Accordion.tsx:253](src/components/ui/Accordion.tsx#L253 "fp:2f5641f7") | med | Collapsed panels were CSS-clipped only. 42db50f put `inert={!isOpen}` on `Accordion.Content` (Accordion.tsx:253); 875beb9 did the same for `Collapsible.Content` (Collapsible.tsx:126), which this row also names — one instance fixed is not a row closed |
| 137 | fixed · d08ad08 | Accordion | [Accordion.tsx:200](src/components/ui/Accordion.tsx#L200 "fp:45a14be6") | low | Trigger is not inside a heading and there is no `headingLevel` prop, so heading navigation skips every section |
| 138 | fixed · a4d2c12 | Breadcrumbs | [Breadcrumbs.tsx:57](src/components/ui/Breadcrumbs.tsx#L57 "fp:6baa10f3") | med | Collapse slices head and tail independently with no overlap check — a crumb renders twice, with a duplicate React key |
| 140 | fixed · d08ad08 | Stepper | [Stepper.tsx:121](src/components/ui/Stepper.tsx#L121 "fp:ee8b9949") | med | `onStepClick` is all-or-nothing: every step becomes a focusable button, including ones the consumer's handler ignores |
| 141 | fixed · 42db50f | Pagination | [Pagination.css:45](src/components/ui/Pagination.css#L45 "fp:2d58a641") | low | Current page uses `pointer-events:none` rather than `disabled`, so it stays tab-focusable and Enter re-fires `onPageChange` |
| 142 | fixed · d08ad08 | Accordion | [Accordion.tsx:81](src/components/ui/Accordion.tsx#L81 "fp:a543c5b8") | low | `mode` is enforced only in `toggle`, so `mode="single"` with a two-item `defaultValue` renders both open |
| 143 | fixed · d08ad08 | Accordion | [Accordion.tsx:131](src/components/ui/Accordion.tsx#L131 "fp:3b20f93b") | low | `value` is interpolated raw into ids; a value containing a space silently breaks `aria-controls`/`aria-labelledby` |
| 144 | fixed · d08ad08 | Collapsible | [Collapsible.tsx:120](src/components/ui/Collapsible.tsx#L120 "fp:0ec24a1f") | low | `Content` sets `role="region"` with no `aria-labelledby` and the trigger has no id — an unnamed landmark. `Accordion.Content` wires both |
| 145 | fixed · a4d2c12 | Breadcrumbs | [Breadcrumbs.tsx:98](src/components/ui/Breadcrumbs.tsx#L98 "fp:0858a627") | low | `<ol>` + `list-style:none` with no `role="list"` (as #28) — and rest props land on the `<nav>`, so the caller cannot restore it **Fixed, announcement unverified.** `role="list"` is set on the `<ol>` (before the spread in ActivityFeed, so still replaceable; unreachable via rest props in Breadcrumbs). jsdom computes the implicit role regardless, so the Safari+VoiceOver behaviour this row is really about is asserted at attribute level only. |
| 148 | fixed · a4d2c12 | CodeBlock | [CodeBlock.tsx:40](src/components/ui/CodeBlock.tsx#L40 "fp:62839014") | med | The horizontally scrolling `<pre>` gets no `tabIndex`, and a `tabIndex` from the call site lands on the `overflow:hidden` root instead — no call-site fix (WCAG 2.1.1) |
| 149 | fixed · a4d2c12 | CodeBlock | [CodeBlock.tsx:19](src/components/ui/CodeBlock.tsx#L19 "fp:22dfb228") | low | `filename ?? "Code block"` uses `??`, so `filename=""` renders `aria-label=""` on a `role="region"` — a landmark with no accessible name |
| 150 | fixed · a4d2c12 | CodeBlock | [CodeBlock.tsx:23](src/components/ui/CodeBlock.tsx#L23 "fp:6dde06bf") | low | Trailing-newline strip and line split are LF-only, so CRLF input renders every line with a stray `\r` in its text node |
| 151 | fixed · a4d2c12 | CodeBlock | [CodeBlock.tsx:23](src/components/ui/CodeBlock.tsx#L23 "fp:6dde06bf") | low | The trailing-newline strip runs only in `showLineNumbers` mode, so the default path still renders the phantom empty final line the code comment exists to prevent |
| 153 | fixed · a4d2c12 | CodeBlock | [CodeBlock.tsx:28](src/components/ui/CodeBlock.tsx#L28 "fp:19c1d3bb") | low | `role="region"` is unconditional, so every sample however short becomes a landmark, and unnamed ones all announce as "Code block" |
| 154 | fixed · a4d2c12 | CodeBlock | [CodeBlock.css:89](src/components/ui/CodeBlock.css#L89 "fp:75011271") | low | The line-number gutter is a fixed `2.5ch` box with a right-aligned counter, so from line 100 the number overflows leftwards into the code's padding |
| 155 | fixed · cbc6876 (response-ui-css) | CodeBlock | [CodeBlock.css:92](src/components/ui/CodeBlock.css#L92 "fp:dd7445ad") | low | Line numbers ink `--C-TEXT-MUTED` on `--C-SURFACE-0` = 2.10–2.59:1, under the 3:1 large-text floor in all four themes (instance of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: line numbers re-measure **4.50–5.23:1**, clearing 4.5 let alone the 3:1 this row cited |
| 156 | fixed · 656a6e0 | EmptyState | [EmptyState.css:32](src/components/ui/EmptyState.css#L32 "fp:7bc98d0c") | med | The icon slot scales via `font-size` only and sets no `width`/`height`, so every default `lucide-react` icon renders identically at `sm`, `md` and `lg` **Uncovered by test, measured instead.** This package runs vitest with `css: false`. Firefox 146: the icon was 24×24 at all three sizes before, and 20/28/36px after. |
| 157 | fixed · cbc6876 (response-ui-css) | EmptyState | [EmptyState.css:83](src/components/ui/EmptyState.css#L83 "fp:6d3f4ebc") | med | Re-scoped off the number and onto the token role: the description is the panel's only explanatory copy but inks `--C-TEXT-MUTED`, which the contract documents as "most-muted (placeholders, hints)". `--C-TEXT-SECONDARY` ("de-emphasized text — captions, helpers") is the contract-correct role token. The ratio itself is #51's. Closed by the retune: the panel declares no background, so muted inherits `SURFACE-0` — **4.85–5.23:1**. The token-*role* argument (muted vs secondary per the contract) is not a contrast claim and stands or falls on its own |
| 158 | fixed · 656a6e0 | EmptyState | [EmptyState.css:47](src/components/ui/EmptyState.css#L47 "fp:c245c5f7") | low | `[data-size="md"]` and `[data-size="lg"]` both set the icon `font-size: var(--H4)`, so `lg` never enlarges the icon while padding, gap and title all step up **Uncovered by test, measured instead** — same Firefox run as #156. |
| 159 | fixed · 656a6e0 | EmptyState | [EmptyState.tsx:68](src/components/ui/EmptyState.tsx#L68 "fp:7158411b") | low | `EmptyStateTitle` always renders a `<p>` with no `as` prop and no heading role, so an empty state replacing a page's main content contributes nothing to the outline |
| 160 | fixed · ee5d181 | Hero | [Hero.tsx:49](src/components/ui/Hero.tsx#L49 "fp:68760345") | med | The scrim is appended after `children` with no `pointer-events: none`, so only `Hero.Content` (z-10) escapes it — anything else placed directly in `<Hero>` renders dimmed and swallows all pointer input **Fixed, with a residual the row did not name.** `pointer-events: none` on `.hero__overlay` restores pointer input. The *dimming* half is inherent to a full-bleed scrim: only `Hero.Content` (`z-index: 10`) escapes it, and a static child always paints below a positioned overlay. Documented rather than worked around. |
| 162 | fixed · ee5d181 | Hero | [Hero.tsx:39](src/components/ui/Hero.tsx#L39 "fp:52b7a215") | med | `overlay` defaults to `true` regardless of whether a `Hero.Background` exists, so a bare Hero paints a 50%-black rectangle onto the page and drops body ink from 17.74:1 to 4.46:1. Behavioural clause only — the 17.74→4.46:1 figure belongs to #163, which is refuted. **Contrast clause retired 2026-07-26** — the row already disclaims the ratio as #163's. The behavioural half (a bare Hero paints the overlay unconditionally) is what stays open |
| 164 | fixed · ee5d181 | Hero | [Hero.css:49](src/components/ui/Hero.css#L49 "fp:466341d1") | low | `.hero__overlay` reads `var(--OVERLAY-SCRIM-COLOR)` with no fallback, unlike `Drawer.css`/`CommandPalette.css` — without the token layer the scrim is transparent, not 50% black. `AppShell.css:199` shares the identical bare read — logged separately as #444, same root, different stylesheet |
| 165 | fixed · ee5d181 | Hero | [Hero.tsx:69](src/components/ui/Hero.tsx#L69 "fp:d41d451e") | low | `alt` is silently dropped when `src` is absent (no `<img>` renders at all), and `parallax` without `src` still mounts the client Parallax wrapper over an empty layer **Fixed, with a residual.** The Parallax wrapper and its class are now gated on the image, so `parallax` without `src` mounts no client wrapper. Still true: `alt` without `src` is dropped — there is no `<img>` to carry it. |
| 166 | fixed · ee5d181 | Hero | [Hero.css:18](src/components/ui/Hero.css#L18 "fp:e1a25432") | low | `hero--full` is `min-height: 100vh`, not `100dvh`, so on mobile browsers with a retracting URL bar the hero exceeds the visible viewport on first paint |
| 167 | fixed · ee5d181 | MediaCard | [MediaCard.tsx:126](src/components/ui/MediaCard.tsx#L126 "fp:86669073") | med | `MediaCard.Action` renders `absolute inset-0 z-10` with pointer events left on, so the transparent layer covers the whole card and swallows clicks on everything beneath it |
| 168 | fixed · ee5d181 | MediaCard | [MediaCard.css:47](src/components/ui/MediaCard.css#L47 "fp:7ad3b85c") | med | `.media-card__content` re-declares six `--C-TEXT-*` variables to white but sets no `color`, so unstyled children keep the ambient page ink over a dark scrim |
| 169 | fixed · ee5d181 | MediaCard | [MediaCard.tsx:93](src/components/ui/MediaCard.tsx#L93 "fp:c5b62b97") | low | `MediaCard.Content` has no `z-index` while `Badge` and `Action` both set `z-10`, so a card that renders `Overlay` after `Content` gets its caption painted over by the scrim |
| 170 | fixed · ee5d181 | MediaCard | [MediaCard.css:11](src/components/ui/MediaCard.css#L11 "fp:175398ef") | low | The hover scale/lift/elevation has no `:focus-within` counterpart, so tabbing into a control inside the card produces no card-level affordance |
| 171 | fixed · ab8a0bf | Swimlane | [Swimlane.tsx:36](src/components/ui/Swimlane.tsx#L36 "fp:b54f3686") | **high** | `{...rest}` is spread onto `ScrollReveal`, which never forwards it — every prop the type advertises past the named eight is dropped at runtime (downstream instance of #9) |
| 173 | fixed · upstream `@batthewz/response-ui-css` v0.10.0, re-measured this pass | Swimlane | [Swimlane.css:33](src/components/ui/Swimlane.css#L33 "fp:9827162e") | med | The "View all" link inks `--C-ACCENT` at `--BodyText-2` with `text-decoration: none`, failing AA on every surface in `events` (2.72–2.52:1) and `grimdark` (2.96–2.55:1), where `:hover` lowers it further **Re-measured and confirmed; the fix is out of this package.** `docs/components/swimlane.md` already carries the measurement and prescribes retinting `--C-ACCENT`, which lands in `@batthewz/response-ui-css` — the palette retune the owner has declined twice. The only in-package alternative is inking the CTA in `--C-TEXT-PRIMARY`, which removes brand accent from a call to action: a visible design decision, not a mechanical fix. **ARCHIVED 2026-07-26 — the sentence is no longer true.** Re-measured against the shipped `@batthewz/response-ui-css` v0.10.0 the row was written before: `--C-ACCENT` on `--C-SURFACE-0/1/2` is **4.89 / 4.73 / 4.53 in `events`** and **5.69 / 5.33 / 4.90 in `grimdark`** (5.17 default, 14.84 `tech`), all clearing AA body text. The row's own figures — 2.72–2.52 and 2.96–2.55 — are now exactly the values of a *different* token, `--C-BORDER-FOCUS`, which is where each theme's pre-retune accent literal was left behind; that is a separate, still-open cross-package item and must not be read as this row. Measured twice by independent converters, each validated at white-on-black = 21.0000 and against WebAIM reference pairs, and confirmed against browser-painted pixels (Firefox 146 and Chrome both clip out-of-gamut channels identically, 0/255 delta). **Two caveats recorded rather than left implicit:** `--C-SURFACE-3` is not covered (4.17 / 4.12 / 4.27 / 12.37) — the CSS package excluded SURFACE-3 deliberately on the inactive-control exemption and no in-package code puts a Swimlane there; and the `:hover` clause lands at **4.4967 on SURFACE-0 in `grimdark`**, i.e. true by a thousandth, and genuinely under on SURFACE-1 (4.21) — a narrower row if anyone wants one, but not what this row says. |
| 174 | fixed · ee5d181 | Swimlane | [Swimlane.tsx:44](src/components/ui/Swimlane.tsx#L44 "fp:3f1bef1c") | med | "View all" is a hard-coded English literal with no prop to relabel it, and the anchor receives only `href` and a class — no `aria-label`, `target`, `rel` or router hook |
| 175 | fixed · ee5d181 | Swimlane | [Swimlane.tsx:30](src/components/ui/Swimlane.tsx#L30 "fp:3ed747ef") | med | Server-rendered markup carries `scroll-reveal-hidden` (`opacity: 0`) with **no opt-out prop**, so a Swimlane whose JS never runs is entirely invisible, heading included (unavoidable instance of #16). Note for triage: fbdf501 fixed the `matchMedia` *throw*, not the hidden state — do not close this on that commit |
| 176 | fixed · ee5d181 | Swimlane | [Swimlane.tsx:40](src/components/ui/Swimlane.tsx#L40 "fp:3d855ccb") | low | Heading level is hard-coded to `<h2>` with no `as`/`level` prop, so a lane nested under an existing h2 skips an outline level |
| 177 | fixed · cbc6876 (response-ui-css) | Swimlane | [Swimlane.css:26](src/components/ui/Swimlane.css#L26 "fp:2a2cd6ac") | low | Subtitle ink `--C-TEXT-MUTED` measures at most 2.59:1 against any surface token in any shipped theme (1.94:1 in `tech`) at `--BodyText-2` (instance of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: **4.85–5.23:1** on `SURFACE-0`. The '1.94:1 in tech' figure is now wrong by a factor of 2.5 |
| 178 | fixed · ab8a0bf | MasonryGrid | [MasonryGrid.tsx:128](src/components/ui/MasonryGrid.tsx#L128 "fp:2c3e1f67") | **high** | `MasonryGrid.Item` spreads `{...props}` onto `ScrollReveal`, which never forwards them — with `animate` at its default every `div` prop but `className`/`ref`/`children` is dropped (downstream instance of #9, second after #171) |
| 179 | fixed · ee5d181 | MasonryGrid | [MasonryGrid.tsx:91](src/components/ui/MasonryGrid.tsx#L91 "fp:0acc1c70") | med | The root wraps each child in `<MasonryContext.Provider key={index}>`, so React reconciles by position and the caller's `key` is defeated — inserting or reordering remounts every item from that point on |
| 180 | fixed · ee5d181 | MasonryGrid | [MasonryGrid.tsx:62](src/components/ui/MasonryGrid.tsx#L62 "fp:9d97dcd0") | med | `columns` is typed `number` but `MasonryGrid.css` defines rules only for 2, 3 and 4 per breakpoint, so any count above 4 silently renders one column |
| 181 | fixed · ee5d181 | MasonryGrid | [MasonryGrid.tsx:51](src/components/ui/MasonryGrid.tsx#L51 "fp:12f1ceb5") | med | `buildResponsiveClasses` skips any count equal to `1`, so a breakpoint key can widen the grid but can never narrow it back to a single column |
| 183 | fixed · ee5d181 | MasonryGrid | [MasonryGrid.tsx:87](src/components/ui/MasonryGrid.tsx#L87 "fp:b3fadae9") | low | `style={{ ...vars, ...style }}` places the caller's `style` after the `gap`-derived variable, so a `style` carrying `--masonry-gap` silently overrides the `gap` prop (measured: `gap="2rem"` renders at `0.25rem`) |
| 184 | fixed · ee5d181 | MasonryGrid | [MasonryGrid.css:8](src/components/ui/MasonryGrid.css#L8 "fp:0f650ae2") | low | `.masonry-grid__item` sets `margin-bottom` with no `:last-child` reset, and the rule is unlayered, so a Tailwind `mb-0` on the item cannot clear the trailing gap (only `mb-0!` can) |
| 185 | fixed · fbdf501 + ab8a0bf | MasonryGrid | [MasonryGrid.test.tsx:100](src/components/ui/MasonryGrid.test.tsx#L100 "fp:39f90608") | low | Both clauses closed, and NOT by one commit: fbdf501 rewrote `usePrefersReducedMotion` as an alias over the already-guarded `useMediaQuery`, ending the `matchMedia is not a function` throw; ab8a0bf added MasonryGrid.test.tsx:100, a default-`animate` case that exercises the #178 path. Residual: the row's literal count ("all nine") is stale — the suite is now ten |
| 186 | fixed · 572cf53 | Carousel | [Carousel.tsx:111](src/components/ui/Carousel.tsx#L111 "fp:339694cc") | **high** | The root's ArrowLeft/ArrowRight handler has no `e.target` guard, so arrow keys pressed in a text field inside a slide are `preventDefault()`ed and scroll the rail instead of moving the caret |
| 187 | fixed · ee5d181 | Carousel | [Carousel.tsx:217](src/components/ui/Carousel.tsx#L217 "fp:626c03cb") | med | `e.preventDefault()` on every left mousedown over the track suppresses native focus and caret placement, so a form control inside a slide cannot be focused by mouse |
| 188 | fixed · ee5d181 | Carousel | [Carousel.tsx:155](src/components/ui/Carousel.tsx#L155 "fp:2731736e") | med | End-of-rail arrows are hidden with `opacity: 0; pointer-events: none` only — they stay `disabled=false`, `tabIndex=0`, un-`aria-hidden` and focusable, so a keyboard user tabs onto an invisible no-op button |
| 189 | fixed · ee5d181 | Carousel | [Carousel.tsx:138](src/components/ui/Carousel.tsx#L138 "fp:2c4ada37") | med | `aria-roledescription="carousel"` and the accessible name sit on a role-less `<div>` (implicit role `generic`), where ARIA prohibits both — a conforming screen reader announces neither |
| 190 | fixed · ee5d181 | Carousel | [Carousel.tsx:99](src/components/ui/Carousel.tsx#L99 "fp:5fda35f5") | med | `scrollPrev`/`scrollNext`/the drag fling all pass `behavior: "smooth"` explicitly, which per CSSOM View overrides the `scroll-behavior: auto` the reduced-motion block sets — so the media query cannot stop the motion a user actually triggers |
| 191 | fixed · ee5d181 | Carousel | [Carousel.tsx:62](src/components/ui/Carousel.tsx#L62 "fp:93cd8228") | low | RTL unsupported: `canScrollPrev` is `scrollLeft > 0` and the arrows are placed with physical `left: 0`/`right: 0`, so under `dir="rtl"` Previous never enables, Next never hides, and both sit on the wrong side |
| 192 | fixed · ee5d181 | Carousel | [Carousel.tsx:155](src/components/ui/Carousel.tsx#L155 "fp:2731736e") | low | Arrow labels `"Previous"`/`"Next"` are hard-coded English on internal `IconButton`s with no prop to override them (instance of the hard-coded-`sr-only`-English pattern; the root and track labels are at least reachable via rest props) |
| 193 | fixed · ee5d181 | Spotlight | [Spotlight.tsx:87](src/components/ui/Spotlight.tsx#L87 "fp:37c312dc") | med | `Spotlight.Image` passes only `src`/`alt`/`role` to the `<img>` and spreads everything else onto the wrapper `<div>`, so `loading`, `width`/`height`, `srcSet`, `sizes` and `decoding` are unreachable |
| 197 | fixed · ee5d181 | Spotlight | [Spotlight.tsx:118](src/components/ui/Spotlight.tsx#L118 "fp:dbf9498f") | low | The reveal direction is derived from the child index alone, so `reversed` flips the columns without flipping the animation and the copy slides in from the wrong side |
| 198 | fixed · ee5d181 | Spotlight | [Spotlight.tsx:135](src/components/ui/Spotlight.tsx#L135 "fp:54e38064") | low | `Spotlight.Content`'s `ref` lands on the `ScrollReveal` wrapper when the root's `animate` is true and on the `.spotlight-content` div when it is false — the ref target is decided by a prop two components up |
| 199 | fixed · ee5d181 | Spotlight | [Spotlight.tsx:40](src/components/ui/Spotlight.tsx#L40 "fp:5bdec328") | low | `SpotlightItemContext.Provider` is keyed by array index, so reordering rows remounts the content subtree and replays the reveal (same shape as #179) |
| 200 | fixed · ee5d181 | Spotlight | [Spotlight.css:62](src/components/ui/Spotlight.css#L62 "fp:2a2f5aed") | low | `.spotlight-item:nth-child(even) .spotlight-content { order: 1 }` is dead CSS whenever `animate` is on, because the `ScrollReveal` wrapper — not `.spotlight-content` — is the grid item (measured by SSR render) |
| 201 | fixed · d08ad08 | ProgressBar | [ProgressBar.css:56](src/components/ui/ProgressBar.css#L56 "fp:4e36e18e") | med | `variant="gradient"` silently discards `color`: the gradient rule uses the `background` shorthand and is declared after the four colour rules at equal specificity, so it always wins |
| 202 | fixed · d08ad08 | ProgressBar | [ProgressBar.tsx:64](src/components/ui/ProgressBar.tsx#L64 "fp:f34449c1") | med | `aria-valuenow` is the raw, unclamped `value` while the fill width is clamped, so the announcement can fall outside the announced range (same defect as #22; `ProgressRing.tsx:35` clamps, so the two siblings disagree) |
| 203 | fixed · 81e532a | ProgressBar | [ProgressBar.tsx:99](src/components/ui/ProgressBar.tsx#L99 "fp:b400f8eb") | med | `ProgressBar.Label` emits no `id` and the root sets no `aria-labelledby`, so a bar sitting next to a `Label` still has no accessible name — the sub-part implies a wiring it never performs. Structurally unfixable from the call site: the root type is `Omit<…,"children">`, so Label and Value can only ever be siblings **Assessed: the fix is an API change, not a defect fix.** `ProgressBar.Label` and the root are siblings (the root is `Omit<…,"children">`), so no context can join them. Closing this needs either a required accessible name the way `Meter` does, or a `label` prop on the root — both breaking. **Closed by a type constraint, not a prop — and it is BREAKING.** The root now requires one of `aria-label`, `aria-labelledby` or `aria-hidden`. A `label` prop could only ever set `aria-label` (the root omits `children`, and a text child of `role="progressbar"` is presentational), so it would have been a second name for a capability that already exists; a constraint can be widened later without breaking anyone. Deliberately **not** Meter's single-key shape: ProgressBar ships a `Label` sub-part and its headline documented example wires the name with `aria-labelledby`, so requiring `aria-label` would have made the component's primary pattern a compile error. |
| 204 | fixed · d08ad08 | ProgressBar | [ProgressBar.tsx:57](src/components/ui/ProgressBar.tsx#L57 "fp:dc8c660c") | med | `value={NaN}` renders a **full** bar: the clamp propagates `NaN`, the CSSOM rejects `width: NaN%`, and the fill falls back to `width: auto` — the whole track |
| 208 | fixed · d08ad08 | ProgressBar | [ProgressBar.css:60](src/components/ui/ProgressBar.css#L60 "fp:7a1395dc") | low | The `striped` variant is static and unthemeable: no `@keyframes` for it exists anywhere in `src/` or `@batthewz/response-ui-css`, `background-size: 200% 100%` animates nothing, and the stripe colour is a hard-coded `oklch(1 0 0 / 0.15)` |
| 209 | fixed · d08ad08 | ProgressBar | [ProgressBar.tsx:66](src/components/ui/ProgressBar.tsx#L66 "fp:b1efe462") | low | `max <= 0` exposes an invalid ARIA range: the fill fraction is guarded to `0` but `aria-valuemax` is the raw `max`, so `aria-valuemin={0}` is greater than or equal to it |
| 211 | fixed · 572cf53 | Rating | [Rating.tsx:158](src/components/ui/Rating.tsx#L158 "fp:e513074b") | **high** | With `allowHalf` every radio's `sr-only` name is `position − 0.5`, so no star is ever named `max` and the radio reporting `aria-checked="true"` is misnamed by half a star |
| 212 | fixed · b742101 | Rating | [Rating.tsx:111](src/components/ui/Rating.tsx#L111 "fp:8b0a4e39") | med | `valueFromClick` reads `e.clientX`, which is `0` for a keyboard-activated click, so under `allowHalf` `Enter`/`Space` can only ever commit `position − 0.5` — no star can be *activated* to its whole value |
| 213 | fixed · b742101 | Rating | [Rating.tsx:142](src/components/ui/Rating.tsx#L142 "fp:c1f47123") | med | Arrow keys drive roving focus and the value as two unsynchronised state machines: focus loops while the value clamps, and under `allowHalf` focus moves a whole star per `0.5` of value |
| 214 | fixed · b742101 | Rating | [Rating.tsx:145](src/components/ui/Rating.tsx#L145 "fp:88873d99") | med | Clicking a star never updates the roving index, so the next arrow key teleports focus to star 2 whichever star was clicked, and Tab always enters the group on star 1 regardless of the value |
| 216 | fixed · b742101 | Rating | [Rating.tsx:76](src/components/ui/Rating.tsx#L76 "fp:161f9f53") | med | The `readOnly` branch overwrites the caller's **required** `aria-label` with a generated `"{value} out of {max} stars"`, so the mandatory prop is silently discarded and the subject of the rating is lost |
| 217 | fixed · b742101 | Rating | [Rating.tsx:100](src/components/ui/Rating.tsx#L100 "fp:a3ad139a") | low | `Home`/`End` move roving focus but fire no `onValueChange`, unlike every other key the group handles |
| 218 | fixed · b742101 | Rating | [Rating.tsx:158](src/components/ui/Rating.tsx#L158 "fp:e513074b") | low | Hard-coded English `"stars"` in both the `sr-only` name and the `readOnly` label, unreachable from props; star 1 also announces `"1 stars"` (instance of the pattern named for #39/#64) |
| 219 | fixed · b742101 | Rating | [Rating.tsx:81](src/components/ui/Rating.tsx#L81 "fp:d06dadc0") | low | Nothing range-checks or rounds an incoming `value`: `value={9} max={5}` announces `"9 out of 5 stars"`, and `value={4.3}` draws 4 stars while announcing 4.3 |
| 220 | fixed · b742101 | Rating | [Rating.tsx:76](src/components/ui/Rating.tsx#L76 "fp:161f9f53") | low | `readOnly` returns before `disabled` is read, so `disabled` silently no-ops in that mode — no `aria-disabled`, no dimming |
| 221 | fixed · b742101 | SearchInput | [SearchInput.tsx:66](src/components/form/SearchInput.tsx#L66 "fp:49322f62") | med | `disabled`/`readOnly` reach the `<input>` only — the clear button stays enabled and still wipes the value |
| 222 | fixed · b742101 | SearchInput | [SearchInput.tsx:55](src/components/form/SearchInput.tsx#L55 "fp:1b3c4a6c") | med | Clause 1 of the original is overstated — `{...props}` at :64 lands *after* the literal, so a caller CAN pass `aria-label`. Clause 2 is the real one and holds: the default `aria-label="Search"` outranks an associated `<label for>`, so a visible Label is silently ignored unless the caller knows to clear it |
| 223 | fixed · b742101 | SearchInput | [SearchInput.tsx:66](src/components/form/SearchInput.tsx#L66 "fp:49322f62") | med | The clear button unmounts on activation, dropping focus to `<body>` (WCAG 2.4.3) |
| 224 | fixed · b742101 | SearchInput | [SearchInput.tsx:37](src/components/form/SearchInput.tsx#L37 "fp:973ce2dd") | med | The Escape handler calls neither `preventDefault()` nor `stopPropagation()`, so one press clears the field *and* closes the surrounding overlay |
| 225 | fixed · cbc6876 (response-ui-css) | SearchInput | [SearchInput.css:56](src/components/form/SearchInput.css#L56 "fp:c157ad71") | med | Clear-button glyph at rest is `--C-TEXT-MUTED` on `--C-SURFACE-0` — 2.10–2.59:1 in all four themes, under the WCAG 1.4.11 3:1 floor (pattern of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: the clear glyph re-measures **4.85–5.23:1**, clearing the 3:1 floor cited |
| 226 | fixed · b742101 | SearchInput | [SearchInput.tsx:38](src/components/form/SearchInput.tsx#L38 "fp:b0fee5ea") | low | Escape clears unconditionally — an already-empty field still fires `onChange("")` and `onClear()` |
| 227 | fixed · b742101 | SearchInput | [SearchInput.css:21](src/components/form/SearchInput.css#L21 "fp:5ee2a2ab") | low | `size="sm"` overrides only `font-size`; `py-r5` and the `text-body-2` line-height are untouched, so `sm` and `md` are the same height |
| 228 | fixed · b742101 | SearchInput | [SearchInput.tsx:45](src/components/form/SearchInput.tsx#L45 "fp:db826dbb") | low | `className` is retargeted to the wrapper `<div>` while `style`/`id`/`data-*` land on the `<input>`, though the prop type advertises `<input>` props for both |
| 229 | fixed · b742101 | SearchInput | [SearchInput.tsx:14](src/components/form/SearchInput.tsx#L14 "fp:4bb6a563") | low | `defaultValue` survives the `Omit`, so it typechecks alongside the required `value` and then trips React's controlled/uncontrolled warning |
| 230 | fixed · b742101 | SearchInput | [SearchInput.tsx:54](src/components/form/SearchInput.tsx#L54 "fp:abb711dc") | low | Explicit `role="searchbox"` duplicates the implicit role of `<input type="search">` |
| 231 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:108](src/components/form/NumberInput.tsx#L108 "fp:6a6001f9") | med | Stepper buttons and Arrow keys discard uncommitted typed text, stepping from the last committed value instead |
| 232 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:83](src/components/form/NumberInput.tsx#L83 "fp:46eb637f") | med | A controlled `value` is not enforced: the box displays a value the parent refused, permanently, while `aria-valuenow` reports the prop |
| 233 | fixed · b742101 | NumberInput | [NumberInput.tsx:128](src/components/form/NumberInput.tsx#L128 "fp:79275dba") | med | `readOnly` blocks typing but not the steppers or Arrow keys, and no `aria-readonly` is set |
| 234 | fixed · b742101 | NumberInput | [NumberInput.tsx:172](src/components/form/NumberInput.tsx#L172 "fp:e9bae206") | low | Reserved right padding (`pr-r2` = 20px / 32px) is narrower than the stepper column (14px chevron + 2× `px-r5` = 30px / 38px), so long values render under the chevrons |
| 235 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:43](src/components/form/NumberInput.tsx#L43 "fp:a056d6db") | low | `parseDraft` uses `Number()`, so `0x1f` commits as `31` and `Infinity` commits as `Infinity` into the value and `aria-valuenow` |
| 236 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:109](src/components/form/NumberInput.tsx#L109 "fp:2a209c9b") | low | Stepping up from an empty field bases on `min` and *then* adds `step`, so the first press lands on `min + step`, never on `min` |
| 237 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:113](src/components/form/NumberInput.tsx#L113 "fp:47cb4de6") | low | `stepBy` emits unconditionally, so at a clamped bound every further press re-fires `onValueChange` with an unchanged value |
| 238 | fixed · 236e6a0 | OTPInput | [OTPInput.tsx:95](src/components/form/OTPInput.tsx#L95 "fp:78c26965") | **high** | `onComplete` latches on a boolean ref and never re-fires when an already-complete code is edited, so it reports a stale value forever |
| 239 | fixed · 236e6a0 | OTPInput | [OTPInput.tsx:115](src/components/form/OTPInput.tsx#L115 "fp:bf62bfd0") | med | A multi-character value arriving in one box keeps only the LAST character, defeating the `one-time-code` autofill the component advertises |
| 240 | fixed · 236e6a0 | OTPInput | [OTPInput.tsx:114](src/components/form/OTPInput.tsx#L114 "fp:84985107") | med | Delete and cut are silently ignored — only Backspace can clear a box |
| 241 | fixed · upstream `@batthewz/response-ui-css` v0.10.0, re-measured this pass | **library-wide** | [Input.tsx:27](src/components/form/Input.tsx#L27 "fp:42c0a863") | med | Form-control boundary `--C-BORDER-STRONG` on the `--C-SURFACE-0` fill measures **1.41–1.79:1** in all four themes, under the WCAG 1.4.11 3:1 floor. Affects Input, Textarea, Select, NumberInput, SearchInput, TagInput, OTPInput (pattern of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Component list verified: Input.tsx:22, Textarea.tsx:22, Select.tsx:22, OTPInput.tsx:214, TagInput.tsx:180. **Enumeration corrected 2026-07-26:** `.combobox-input` and `.multiselect-control` carry the identical `SURFACE-0` + `BORDER-STRONG` recipe and were missing from the list above. Survives the rubric: the controls paint `bg-surface-0` on a `SURFACE-0` page, so the 1px hairline is the sole rendered boundary **ARCHIVED 2026-07-26 — the sentence is no longer true.** `--C-BORDER-STRONG` on `--C-SURFACE-0` re-measures **3.30 / 3.23 / 3.49 / 3.25** (default / events / grimdark / tech), clearing the 1.4.11 3:1 floor in all four themes; on `--C-SURFACE-1` (a control inside a Card) **3.15 / 3.14 / 3.27 / 3.18**, also clear. The 1.41–1.79 range is the pre-retune value and the CSS package's changelog reproduces it exactly as its "before" column. The row's corrected enumeration — Input, Textarea, Select, NumberInput, SearchInput, TagInput, OTPInput, plus `.combobox-input` and `.multiselect-control` — all share the one recipe, so a single measurement clears every named instance and no half is left open. **Recorded honestly:** on `--C-SURFACE-2` the pairing is **2.995 default / 2.990 tech**, a thousandth under 3.0 where the CSS changelog claims a flat 3.00; the gap is that the retune was solved against a gamut-mapped colour while both shipping engines clip. Not worth a row, but that pairing has no headroom. `disabled:bg-surface-3` sits at 2.62–2.74 and is 1.4.11-exempt as an inactive component. |
| 243 | fixed · b742101 | OTPInput | [OTPInput.tsx:214](src/components/form/OTPInput.tsx#L214 "fp:6d1d4010") | low | Each box is hard-coded ``aria-label={`Digit ${i+1}`}`` — unreachable, unlocalizable, and factually wrong in `mode="alphanumeric"` (pattern of #39/#64) |
| 244 | fixed · 236e6a0 | OTPInput | [OTPInput.tsx:82](src/components/form/OTPInput.tsx#L82 "fp:f6c6887b") | low | Empty slots serialise into the public string as spaces, so `value.length === length` can be true with boxes still empty — a false completeness test for callers |
| 245 | fixed · 5f76b47 | TagInput | [TagInput.tsx:204](src/components/form/TagInput.tsx#L204 "fp:22329591") | **high** | The rest-spread is applied after the input's own `onChange`, so spreading `form.field()` — the binding AGENTS.md and README.md both advertise — replaces the internal handler and crashes the component |
| 246 | fixed · 5f76b47 | TagInput | [TagInput.tsx:192](src/components/form/TagInput.tsx#L192 "fp:5c095c4c") | med | `name` passes through to the inner input whose value is the in-progress draft, so a native form submits the draft rather than the tags |
| 247 | fixed · b742101 | TagInput | [TagInput.tsx:109](src/components/form/TagInput.tsx#L109 "fp:6292b01a") | med | `commitDraft` clears the draft on every rejection path that produces no message, so the `maxTags` cap, a duplicate and `validateTag → false` all destroy the user's typing silently |
| 248 | fixed · b742101 | TagInput | [TagInput.tsx:116](src/components/form/TagInput.tsx#L116 "fp:71daac96") | med | `handleChange` commits only the segment before the first delimiter and then blanks the draft, discarding everything after it |
| 249 | fixed · b742101 | TagInput | [TagInput.tsx:143](src/components/form/TagInput.tsx#L143 "fp:95b4b85f") | med | The paste path discards any pending draft and reads only the accept/reject answer from `evaluate`, so `validateTag` string messages are swallowed |
| 250 | fixed · b742101 | TagInput | [TagInput.tsx:118](src/components/form/TagInput.tsx#L118 "fp:0c1b033d") | med | A `delimiter` carrying `g` or `y` misfires: `delimiter.test()` mutates the caller's own `RegExp`, so `/;/g` commits every *other* entry and `/;/y` commits none |
| 251 | fixed · cbc6876 (response-ui-css) | TagInput | [TagInput.tsx:199](src/components/form/TagInput.tsx#L199 "fp:db2b99a7") | med | The chip remove button's X glyph inks `--C-TEXT-MUTED` on `--C-SURFACE-2` — 1.94–2.31:1, under the WCAG 1.4.11 3:1 floor in all four themes (pattern of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: the chip sits in a default `Badge` (`bg-surface-2`), where muted is **4.50:1 in all four themes** |
| 252 | fixed · 6fcd84b | TagInput | [TagInput.tsx:271](src/components/form/TagInput.tsx#L271 "fp:3571792c") | med | Adding or removing a tag is never announced and the chips carry no list semantics, so screen-reader users get no confirmation for Enter, Backspace, paste or the remove button **Blocked on the same decision as #262:** announcing add/remove needs an overridable announcement-string API. Deliberately not solved twice in two components with two different string conventions. **FIXED · 6fcd84b — both clauses.** Every commit path (Enter, typed delimiter, paste, blur), both removal paths (Backspace and the chip's remove button) and the three previously-silent refusals (`maxTags`, duplicate, `validateTag → false`) now write one sentence into one always-mounted `sr-only role="status" aria-live="polite"` region. A rejected add never announces "Added" — asserted directly. A paste that both adds and refuses joins the two sentences into a **single** write, because a per-tag write would leave only the last one in the region. A `validateTag` *string* is deliberately not re-announced: it already renders in the component's own `aria-live` paragraph, and a second channel on something already announced is worse than none. Chips are `role="list"`/`role="listitem"` on a `display: contents` wrapper — a `list` may own only `listitem`s and the text input and hidden `name` inputs are siblings of the chips. **Browser-verified rather than assumed** (Firefox 146, live component): the a11y tree reads `list → listitem ×3` then `textbox`, so the roles survive `display: contents`, with a no-roles negative control; and every chip and input rect is byte-identical with the wrapper versus with it unwrapped, so the wrapper costs no layout. Fail-first observed on each clause and each file restored byte-identical after re-breaking. **Uncovered, and said in the tests:** jsdom announces nothing, so every assertion is the DOM precondition — carrier attributes plus the text that lands in the region — and no screen reader was run. |
| 253 | fixed · b742101 | TagInput | [TagInput.tsx:235](src/components/form/TagInput.tsx#L235 "fp:7ae27768") | low | The validation message `<p>` has no `id` and is never referenced by `aria-describedby`, which only ever points at a surrounding Field's error element |
| 254 | fixed · b742101 | TagInput | [TagInput.tsx:190](src/components/form/TagInput.tsx#L190 "fp:b85ffcd1") | low | Chips are keyed by the tag string and a controlled `value` is not de-duplicated, so `value={["react","react"]}` logs React's duplicate-key error |
| 255 | fixed · b742101 | TagInput | [TagInput.tsx:176](src/components/form/TagInput.tsx#L176 "fp:9e12c046") | low | The bordered wrapper carries the `focus-within` ring but no click handler, so clicking its padding does not focus the text input |
| 256 | fixed · 340c66b | form-store | [form-store.ts:407](src/components/form/form-store.ts#L407 "fp:6a2b3cfa") | **high** | Array mutations rewrite values but never re-key the error/touched maps, so a validation message — and `aria-invalid` — stays attached to the old index after a remove or a reorder |
| 257 | fixed · b742101 | Repeater | [Repeater.tsx:134](src/components/form/Repeater.tsx#L134 "fp:dee79eda") | med | Clicking a row's Remove button unmounts the button, dropping keyboard focus to `document.body` with no announcement |
| 258 | fixed · b742101 | Repeater | [Repeater.tsx:75](src/components/form/Repeater.tsx#L75 "fp:e6b68f92") | med | The `disabled` prop reaches only Repeater's own Add/Remove/Move buttons; row fields stay editable and `RepeaterItem` exposes no `disabled` for custom row controls |
| 259 | fixed · b742101 | Repeater | [Repeater.tsx:114](src/components/form/Repeater.tsx#L114 "fp:1f8b2fb9") | med | Per-row control `aria-label`s are hard-coded English literals with no prop to change them, so every row's buttons share one accessible name (pattern of #39/#64) |
| 261 | fixed · b742101 | Repeater | [Repeater.tsx:56](src/components/form/Repeater.tsx#L56 "fp:0a7eeb53") | low | The source JSDoc `@example` wires `Field`/`FieldError` with no surrounding `FormProvider`, so the `FieldError` it advertises can never render |
| 262 | fixed · 6fcd84b | Repeater | [Repeater.tsx:134](src/components/form/Repeater.tsx#L134 "fp:6a9b9fcd") | low | Rows are plain `<div>`s with no list/group semantics and no live region, so adding or removing a row is never announced **Half fixed by b742101, half blocked on a decision.** Rows are `role="list"`/`role="listitem"` now, tested. **Still open:** there is no live region, because announcing add/remove needs an announcement-string API — the same unresolved decision #252 names. Both wait on one choice, not two investigations. **FIXED · 6fcd84b — the open half only.** The row's list-semantics half was already done and is untouched; this closes the live region it named. `addRow()` and `removeRow()` write "Added item 2. 2 items." / "Removed item 1. 1 item." (count *after* the change) into one `sr-only role="status" aria-live="polite"` node outside the list, and the render prop's own `remove()` routes through the same `removeRow`, so a custom row control announces too. **The double-announcement check was performed before adding, not after:** removal already moves focus to the successor Remove button, which does announce — but it names the control focus landed on, never that a row went, so these carry different information. Fail-first observed (4 red). Same convention and same carrier shape as #252, by design — one convention, two components. **Uncovered:** jsdom announces nothing; and the ordering between a polite region write and the focus move on removal lands in one tick, so which a real reader speaks is not settleable in this environment. |
| 263 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:210](src/components/form/MultiSelect.tsx#L210 "fp:1a17b5d8") | med | The rest-spread lands on the outer wrapper `<div>`, so `id` and `aria-labelledby` never reach the combobox input and `aria-label` is the only naming path |
| 264 | fixed · 127096a | MultiSelect | [MultiSelect.css:168](src/components/form/MultiSelect.css#L168 "fp:f7751855") | med | The keyboard-highlighted option is marked only by a `--C-SURFACE-1` background — **1.05 / 1.03 / 1.02 / 1.07:1** against the listbox fill (instance of #206/#51) |
| 265 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:209](src/components/form/MultiSelect.tsx#L209 "fp:daa05bf0") | med | The listbox can never be closed from the control or its chevron, and nothing dismisses it on focus-out |
| 266 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:171](src/components/form/MultiSelect.tsx#L171 "fp:7409462d") | med | `Enter` with the list open but no option highlighted falls through and submits the surrounding form |
| 267 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:241](src/components/form/MultiSelect.tsx#L241 "fp:abb28b83") | med | Clicking a chip's remove button drops focus to `<body>` — the button unmounts itself and nothing restores focus (pattern of #257) |
| 268 | fixed · cbc6876 (response-ui-css) | MultiSelect | [MultiSelect.css:75](src/components/form/MultiSelect.css#L75 "fp:1bb53311") | low | The chip's remove glyph inks `--C-TEXT-MUTED` on `--C-SURFACE-2` — 2.31 / 2.27 / 1.94 / 2.23:1, under the 3:1 graphical floor (same defect as #251, different component) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: muted on `SURFACE-2` is **4.50:1 in all four themes** |
| 269 | fixed · cbc6876 (response-ui-css) | MultiSelect | [MultiSelect.css:111](src/components/form/MultiSelect.css#L111 "fp:ad1e5f79") | low | Placeholder, the "No options" row and disabled option labels all ink `--C-TEXT-MUTED` on `--C-SURFACE-0` — 2.54 / 2.45 / 2.10 / 2.59:1, below AA (instance of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: the menu fill is `SURFACE-0`, so placeholder, 'No options' and disabled labels all re-measure **≥4.85:1** |
| 270 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:240](src/components/form/MultiSelect.tsx#L240 "fp:82bfefc6") | low | `aria-controls` always names the listbox id, but the listbox only exists while open, so a closed combobox points at nothing (measured `getElementById` → null) |
| 271 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:241](src/components/form/MultiSelect.tsx#L241 "fp:abb28b83") | low | With `searchable={false}` the input is `readOnly` and filters nothing, yet still advertises `aria-autocomplete="list"` |
| 272 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:217](src/components/form/MultiSelect.tsx#L217 "fp:775d6b99") | low | Chips are keyed by value and a controlled `value` is never de-duplicated, so `value={["react","react"]}` renders two chips and logs React's duplicate-key error (same as #254) |
| 273 | fixed · d706bf2 | MultiSelect | [MultiSelect.css:59](src/components/form/MultiSelect.css#L59 "fp:6194bf61") | low | Chip label and selected-option weights are a literal `font-weight: 600` rather than `--Semibold-Weight` (500 below 40rem, 600 above), so they are a step heavier than the rest of the system on mobile |
| 274 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:213](src/components/form/MultiSelect.tsx#L213 "fp:244dedbe") | low | Stale source comment: it claims the whole control is the anchor "so the menu spans its width", but `useFloating` adds only `offset`/`flip`/`shift` and `.multiselect-content` sets `min-width` alone — the panel is content-sized |
| 275 | fixed · 127096a | Combobox | [Combobox.css:88](src/components/form/Combobox.css#L88 "fp:f7ae1dad") | **high** | The active-option cue is `--C-SURFACE-1` on `--C-SURFACE-0` (**1.02–1.07:1**, all four themes) and virtual focus means no option ever takes a focus ring — keyboard navigation has no perceptible indicator at all |
| 276 | fixed · b742101 | Combobox | [Combobox.tsx:295](src/components/form/Combobox.tsx#L295 "fp:3ce05be1") | med | The chevron toggle can never close the popup; every click while open emits a spurious `onOpenChange(false)` then `onOpenChange(true)` |
| 277 | fixed · b742101 | Combobox | [Combobox.tsx:328](src/components/form/Combobox.tsx#L328 "fp:97ce66d7") | med | `loading` swaps children for a Spinner but the item count is taken before the swap, so `aria-activedescendant` points at an option id that is not in the document |
| 278 | fixed · b742101 | Combobox | [Combobox.tsx:408](src/components/form/Combobox.tsx#L408 "fp:5b8a1510") | med | Selecting an option with the mouse leaves DOM focus on `<body>` instead of returning it to the input (pattern of #257) |
| 279 | fixed · b742101 | Combobox | [Combobox.tsx:146](src/components/form/Combobox.tsx#L146 "fp:6dccceb0") | med | No focus-out dismissal: tabbing away leaves the portalled listbox mounted with `aria-expanded="true"` on an unfocused combobox |
| 280 | fixed · b742101 | Combobox | [Combobox.tsx:286](src/components/form/Combobox.tsx#L286 "fp:b17ae98d") | low | `aria-controls` is set unconditionally but `Content` returns `null` while closed, so the IDREF dangles whenever the popup is shut |
| 281 | fixed · b742101 | Combobox | [Combobox.tsx:201](src/components/form/Combobox.tsx#L201 "fp:265c0110") | low | The input label is `node.textContent` with no `label` escape hatch, so a multi-node option writes concatenated text into the field (measured `"Ada LovelaceAnalytical Engine"`) |
| 282 | fixed · 7d48730 | Combobox | [Combobox.tsx:406](src/components/form/Combobox.tsx#L406 "fp:521e385c") | low | `Combobox.Item` spreads `getItemProps` (caller props last) after its generated `id`, so a consumer `id` silently replaces the option id `aria-activedescendant` points at |
| 283 | fixed · b742101 | Combobox | [Combobox.tsx:293](src/components/form/Combobox.tsx#L293 "fp:d659a732") | low | The chevron carries a hard-coded English `"Open"`/`"Close"` `aria-label` callers cannot reach, and no `aria-expanded`/`aria-controls` of its own (pattern of #39/#64) |
| 284 | fixed · b742101 | Combobox | [Combobox.css:15](src/components/form/Combobox.css#L15 "fp:7d8d9423") | low | **Narrowed, not closed.** `c3f244b` measured the recipe at *parity* with `Input.tsx` — `.combobox-input:focus-visible` (:24-26) is the same border-swap plus flush 2px ring — so the "it diverges" reading is refuted. What the row still names is live: the recipe is hand-written CSS, not the shared `src/util/focus.ts` constants `aafb9f8` created, so a single edit there still will not reach Combobox. Closing it needs `.tsx` work (moving the control onto the shared utilities) that this pass did not do. The contrast figures are #51/#241/#242's, not this row's. **Contrast clause retired 2026-07-26** — the ratios were #51/#241/#242's, not this row's. What stays open is the hand-written focus CSS bypassing `src/util/focus.ts` |
| 285 | fixed · 236e6a0 | ColorPicker | [ColorPicker.tsx:32](src/components/form/ColorPicker.tsx#L32 "fp:cc76370f") | **high** | `<ColorPicker {...form.field<string>("name")} />` — the binding the library advertises — typechecks clean and renders a permanently inert control, because the closed prop type honours `value`/`disabled` and drops everything else |
| 286 | fixed · b742101 | ColorPicker | [ColorPicker.tsx:224](src/components/form/ColorPicker.tsx#L224 "fp:01c521d6") | med | The trigger's hard-coded `aria-label` replaces the visible hex, so the current colour is never in the accessible name and is unreadable to AT |
| 288 | fixed · b742101 | ColorPicker | [ColorPicker.tsx:324](src/components/form/ColorPicker.tsx#L324 "fp:60355c28") | med | A preset the hex parser rejects still renders as a clickable swatch and commits nothing (measured: zero `onValueChange` calls for `"rebeccapurple"`) |
| 289 | fixed · d859a02 · 5295190 | ColorPicker | [ColorPicker.tsx:110](src/components/form/ColorPicker.tsx#L110 "fp:1bb446a9") | med | The re-seed effect and `lastHexRef` are deleted. Three representations became one committed `hex` plus two derivations: the hex field derives from it (`hexText = draft ?? hex`, :107), and HSV derives from it too — `hsvMemory` is believed only while `hsvToHex(hsvMemory) === hex` (:103), which keeps hue/saturation alive at the greyscale extremes without ever letting the panel outrun a parent that refused the commit |
| 290 | fixed · b742101 | ColorPicker | [ColorPicker.tsx:186](src/components/form/ColorPicker.tsx#L186 "fp:175501eb") | low | `disabled` set while the panel is open leaves the square's arrow keys and the preset buttons live (measured: both still commit) |
| 291 | fixed · 567061e | ColorPicker | [ColorPicker.css:23](src/components/form/ColorPicker.css#L23 "fp:4171c08d") | low | `.colorpicker-trigger:focus-visible` (0,2,0) outranks `.colorpicker-trigger--error` (0,1,0), so keyboard focus erases the invalid border (pattern of #84) |
| 292 | fixed · b742101 | ColorPicker | [ColorPicker.tsx:251](src/components/form/ColorPicker.tsx#L251 "fp:e3e60d65") | low | The floating panel is a `role="dialog"` with no accessible name (measured `aria-label`/`aria-labelledby` both null) and no prop to supply one |
| 294 | fixed · b742101 | ColorPicker | [ColorPicker.css:53](src/components/form/ColorPicker.css#L53 "fp:42c9adda") | low | Stale comment: `.colorpicker-swatch` claims a checkerboard backdrop "for transparency", but no checkerboard is drawn and 8-digit hex is rejected by the parser |
| 295 | fixed · b742101 | RangeSlider | [RangeSlider.tsx:152](src/components/form/RangeSlider.tsx#L152 "fp:f5e35387") | med | Widened to absorb #296's residual: `mergeProps(props, fieldErrorProps)` lands **both** `aria-invalid` and `aria-describedby` on the wrapper `<div>` (RangeSlider.tsx:141-153); neither `<input type="range">` (:156, :172) carries either, so the focused control never reports itself invalid and never points at the Field's error text |
| 297 | fixed · b742101 | RangeSlider | [RangeSlider.tsx:120](src/components/form/RangeSlider.tsx#L120 "fp:87b3a997") | med | When both thumbs hold the same value one is unreachable by pointer, and the static midpoint z-index heuristic only changes which one is buried |
| 298 | fixed · b742101 | RangeSlider | [RangeSlider.tsx:34](src/components/form/RangeSlider.tsx#L34 "fp:033ddb51") | med | The props type is `ComponentPropsWithRef<"div">` and rest lands on the wrapper, so no per-thumb ARIA is reachable — `aria-valuetext` cannot fix a non-percentage announcement |
| 299 | fixed · b742101 | RangeSlider | [RangeSlider.tsx:15](src/components/form/RangeSlider.tsx#L15 "fp:0d136b77") | low | `RangeSliderValue`'s exported docblock claims the pair is "always kept ordered (low <= high) by the component", but an incoming `value`/`defaultValue` is never ordered: `value={[80,20]}` renders as given and the fill disappears |
| 300 | fixed · b742101 | RangeSlider | [RangeSlider.tsx:59](src/components/form/RangeSlider.tsx#L59 "fp:1d27e50d") | low | `minDistance` is enforced only on changes the component computes: `defaultValue={[50,50]}` with `minDistance={10}` renders both thumbs on 50 |
| 301 | fixed · c3f244b | RangeSlider | [RangeSlider.css:66](src/components/form/RangeSlider.css#L66 "fp:d42244c7") | low | The reset is now unconditional — `.range-slider__input:focus-visible { outline: none }` at :66-68, no `[aria-invalid]` scope — matching `Slider.css:30-31`, and the thumb pseudo-elements paint the ring in every state. The inversion the row describes is gone from the cascade |
| 302 | fixed · 57c4753 | Wizard | [Wizard.tsx:170](src/components/ui/Wizard.tsx#L170 "fp:cf1346ad") | **high** | Step content is rendered without a key, so React reconciles one step's content against the next and DOM/component state bleeds across steps |
| 303 | fixed · d08ad08 | Wizard | [Wizard.tsx:77](src/components/ui/Wizard.tsx#L77 "fp:74ad4257") | med | `next()` fires `onComplete` unconditionally after calling the setter, so a controlled parent cannot refuse completion and Finish can re-fire it indefinitely. Clause 2 re-scoped: Finish can only re-fire under a *controlled* parent — uncontrolled, `activeStep` reaches `count`, the `>= count` guard returns and `disabled={wizard.isComplete}` disables the button |
| 304 | fixed · d08ad08 | Wizard | [Wizard.tsx:157](src/components/ui/Wizard.tsx#L157 "fp:d904dcb8") | med | No rest spread on the root: `aria-*`/`data-*` typecheck (TS exempts hyphenated JSX attributes) and are silently dropped, so the flow cannot be named or targeted |
| 305 | fixed · d08ad08 | Wizard | [Wizard.tsx:170](src/components/ui/Wizard.tsx#L170 "fp:cf1346ad") | med | The step panel has no role, no `aria-live`, no `id` and no focus management, so advancing a step is silent for assistive tech |
| 306 | fixed · d08ad08 | Wizard | [Wizard.tsx:152](src/components/ui/Wizard.tsx#L152 "fp:68530386") | low | `allowBackNavigation` makes every step marker a focusable button via `Stepper`, but Wizard's handler ignores all forward ones, leaving dead tab stops (completed ones are also unnamed — #134). Parenthetical closed: 340c66b named the completed markers (Stepper.tsx:131), so they are no longer unnamed — #134 is fixed |
| 307 | fixed · d08ad08 | Wizard | [Wizard.tsx:152](src/components/ui/Wizard.tsx#L152 "fp:68530386") | low | In the completed state every marker satisfies `index < activeStep`, including the last, so clicking it un-completes the flow and re-enables Finish (measured) |
| 308 | fixed · 567061e | Wizard | [Wizard.tsx:66](src/components/ui/Wizard.tsx#L66 "fp:56b6d1be") | low | `goTo` calls the setter even when the clamped index equals the current one, and `useControllableState` notifies unconditionally, so `goTo(0)` at index 0 emits `onStepChange(0)` (same shape as #237) |
| 309 | fixed · d08ad08 | Wizard | [Wizard.tsx:149](src/components/ui/Wizard.tsx#L149 "fp:97c05be1") | low | The `onStepClick` `useMemo` depends on `wizard`, a fresh object literal every render, so it never memoizes |
| 310 | fixed · ea625b7 | CalendarBase | [CalendarBase.tsx:202](src/components/ui/CalendarBase.tsx#L202 "fp:8e442863") | med | **Sentence re-verified and still true** at `CalendarBase.tsx:174` (`if (!isMonthVisible(focusDate)) setFocusDate(computeInitialFocus())` watches the roving day, not the selection). Declined with reasons, so the next agent does not re-litigate it: `displayedMonth` is *independently public* (`month`/`defaultMonth`/`onMonthChange`, :150-153) and a user may page away from the selection on purpose, so this is not the duplicate-representation class #324/#325/#335/#289 belong to; and every available fix reintroduces a hand-rolled reconciliation of the kind `5295190` deleted. Reopen only with an owner decision on the `month`-vs-`value` precedence |
| 311 | fixed · ea625b7 | CalendarBase | [CalendarBase.tsx:153](src/components/ui/CalendarBase.tsx#L153 "fp:f92c0f0b") | med | `defaultMonth` is silently overridden by any seeded selection — `CalendarBase` seeds `focusAnchor ?? defaultMonth`, the opposite precedence to `Calendar.tsx:47`'s `defaultMonth ?? value` |
| 312 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:281](src/components/ui/CalendarBase.tsx#L281 "fp:ed9d8101") | med | The `showToday` button emits `new Date()` with the wall-clock time while every day cell emits local midnight, so two picks of the same day are not `getTime()`-equal |
| 313 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:331](src/components/ui/CalendarBase.tsx#L331 "fp:a803e554") | med | The month and year quick-nav views render `role="grid"` holding twelve bare buttons — measured 0 `row`, 0 `gridcell`, twelve tab stops, and no arrow-key handling |
| 314 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:441](src/components/ui/CalendarBase.tsx#L441 "fp:a29dc490") | med | `aria-selected` is written on the day `<button>`, a role ARIA does not support it on, while the `role="gridcell"` wrapper carries no selection state (measured `null`) |
| 316 | fixed · 418ff90 | CalendarBase | [CalendarBase.tsx:477](src/components/ui/CalendarBase.tsx#L477 "fp:b34b48df") | med | Rest props are spread *after* the component's own `onPointerLeave`, so a caller's handler silently replaces the one that clears the range hover preview (instance of the "rest-spread after own handler" pattern) |
| 317 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:319](src/components/ui/CalendarBase.tsx#L319 "fp:e8e7fe3a") | med | In multi-month day view the header caption becomes an inert `aria-hidden` spacer, removing both the month/year quick-nav and the component's only `aria-live` region (measured: 0 `[aria-live]` at `numberOfMonths={2}`, 1 at `{1}`) |
| 318 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:269](src/components/ui/CalendarBase.tsx#L269 "fp:35f40447") | low | Clicking a leading/trailing padding day pages the calendar to that month and unmounts the button, leaving `document.activeElement` as `<body>` (measured); `handleSelect` never sets `pendingFocusRef` the way `moveFocusTo` does |
| 319 | fixed · upstream `@batthewz/response-ui-css` v0.10.0, re-measured this pass | Calendar | [Calendar.css:211](src/components/ui/Calendar.css#L211 "fp:0fe61c7d") | med | The selected day inks the contract's own intended pair `--C-TEXT-ON-ACCENT` on `--C-ACCENT` and still fails AA: measured **2.80:1** in `events` and **3.81:1** in `grimdark` (5.17 default, 14.84 `tech`) **Re-verified true, and the fix is out of this package.** `Calendar.css:211` already inks the contract's own intended pair, so there is nothing to correct here: the measured failure is in `@batthewz/response-ui-css`'s palette (`src/themes/*.css`). Fixing it in this package means abandoning the token contract. Cross-package door — needs the owner. **ARCHIVED 2026-07-26 — the sentence is no longer true, and the row closed the way it was designed to.** `--C-TEXT-ON-ACCENT` on `--C-ACCENT` re-measures **5.17 / 5.04 / 5.69 / 14.84**; the row's 2.80 `events` and 3.81 `grimdark` are pre-retune. The row's conclusion — that the component was already inking the contract's own designated pair and the defect was a palette value owned by `@batthewz/response-ui-css` — was correct, and the upstream retune actioned it: `events` by darkening the accent, `grimdark` by inverting `--C-TEXT-ON-ACCENT` from light to dark parchment, which is why it moved 3.81 → 5.69 without the accent moving toward white. This is a row closing on an upstream fix, not a refutation. **Caveat:** `Calendar.css:216` swaps the *hover* fill to `--C-ACCENT-HOVER` while the ink stays, landing at **4.4931 in `grimdark`** — under 4.5 by seven ten-thousandths, which is clip-vs-gamut-map noise rather than a defect, but it leaves grimdark's hover state with zero headroom. |
| 321 | fixed · d08ad08 | RangeCalendar | [RangeCalendar.tsx:82](src/components/ui/RangeCalendar.tsx#L82 "fp:bb0a934f") | med | `min`/`max`/`isDateDisabled` gate range *endpoints* only, so a committed range freely spans blocked days — measured, a Fri→Mon pick renders the Saturday with both `aria-disabled="true"` and `data-in-range` |
| 322 | fixed · d08ad08 | RangeCalendar | [RangeCalendar.tsx:99](src/components/ui/RangeCalendar.tsx#L99 "fp:ed4da51b") | low | `showToday` renders a Today button that only navigates: `onTodayClick` is never passed and is absent from the prop type, so the selecting behaviour `Calendar` gets cannot be restored by the caller (measured: 0 `onValueChange` calls) |
| 323 | fixed · 5295190 (side effect) | DatePicker | [DatePicker.tsx:108](src/components/form/DatePicker.tsx#L108 "fp:131f713b") | med | Closed as a *side effect* of #324/#325's derivation, not by a targeted patch: `DatePicker.tsx:108` is now `const text = draft ?? display(selected, locale, formatOptions)`, so `locale` is read on every render and there is no reseed condition left to omit it from. Measured on the switch: the field reformats to 06/05/2026 and a subsequent focus/blur emits 0 (previously committed Jun 5). **`bugs/components/date-picker.md`'s prescribed fix for this row is moot** and has been rewritten to say so |
| 324 | fixed · d859a02 · 5295190 | DatePicker | [DatePicker.tsx:101](src/components/form/DatePicker.tsx#L101 "fp:e036a7fb") | med | Two commits, both needed. `d859a02` made `useControllableState`'s change gate's equality injectable (`isEqual`, defaulting to `Object.is` so all 24 existing call sites are unchanged); `5295190` supplied the day-granular comparator at the call site — `DatePicker.tsx:101` reads `isEqual: isSameDateValue`. `handleBlur` still commits unconditionally, but the fresh `Date` `clampDate` returns now compares equal to the committed day, so a no-edit blur emits **0** (was 1). Clause 2 was already refuted in the prior reconcile |
| 325 | fixed · 5295190 | DatePicker | [DatePicker.tsx:108](src/components/form/DatePicker.tsx#L108 "fp:131f713b") | med | The reference-comparing reseed and `lastFormattedRef` are deleted outright rather than rewired. The draft is now a transient `string \| null` override over text derived from the committed `Date` (:107-108), so nothing compares `Date` identity and an inline `value={new Date(…)}` on an unrelated parent re-render no longer touches in-progress typing |
| 326 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:242](src/components/form/DatePicker.tsx#L242 "fp:b2538369") | med | Opening the calendar sends focus to the "Previous month" button instead of the selected day, so arrow keys are dead until three `Tab`s reach the grid |
| 327 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:230](src/components/form/DatePicker.tsx#L230 "fp:f7593e62") | med | "Open calendar", "Clear date", "Choose date" (plus `CalendarBase`'s "Previous month"/"Next month") are hard-coded English with no prop to override, so a `locale="fr-FR"` picker announces English (pattern: hard-coded English in unreachable strings) |
| 328 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:220](src/components/form/DatePicker.tsx#L220 "fp:7c4df700") | med | `getReferenceProps()` is spread onto the visible text input, which therefore advertises `aria-haspopup="dialog"`, `aria-expanded` and `aria-controls` for a popup nothing on it can open — no `useClick`/`useFocus` is registered (same defect as #333) |
| 331 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:206](src/components/form/DatePicker.tsx#L206 "fp:144c8455") | low | A `form="id"` attribute lands on the visible unnamed input and never on the hidden ISO input (measured), so a picker rendered outside its form submits nothing |
| 332 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:250](src/components/form/DatePicker.tsx#L250 "fp:1e5105de") | low | `Calendar`'s `weekStartsOn`, `numberOfMonths` and `showToday` are neither accepted nor forwarded, so `locale="fr-FR"` gets French names in a Sunday-first grid with no Today button |
| 333 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:197](src/components/form/DateRangePicker.tsx#L197 "fp:38121e69") | med | `getReferenceProps()` is spread onto the start text input, so that input claims a dialog it cannot open (measured `aria-haspopup`/`aria-expanded`/`aria-controls`) while the trigger button that does open it has no `aria-controls` and the end input has none of it |
| 334 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:142](src/components/form/DateRangePicker.tsx#L142 "fp:e36d464f") | med | A typed date that `isDateDisabled` rejects resolves to `null`, silently clearing the endpoint and discarding the previously committed date — while merely unparseable text is preserved |
| 335 | fixed · d859a02 · 5295190 | DateRangePicker | [DateRangePicker.tsx:118](src/components/form/DateRangePicker.tsx#L118 "fp:3ba63cf7") | med | Same two-commit shape as #324: `DateRangePicker.tsx:118` reads `isEqual: isSameDateRange`, `lastRangeRef` is gone, and both endpoint drafts became transient overrides over text derived from the committed range. Measured: draft `"06/1"` survives an unrelated parent re-render (was `""`), and a no-edit blur emits 0 (was 1) |
| 336 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:195](src/components/form/DateRangePicker.tsx#L195 "fp:0f37a251") | med | Both inputs carry hard-coded English `aria-label`s ("Start date"/"End date") and forward no `id`, so the names are unlocalizable, unoverridable and un-bindable to a `Label` (same shape as #222) |
| 337 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:184](src/components/form/DateRangePicker.tsx#L184 "fp:056e8ce6") | low | `disabled` never reaches the hidden `name` inputs (measured: neither carries the attribute), so a disabled picker still submits its dates, unlike a native disabled control |
| 339 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:163](src/components/form/DateRangePicker.tsx#L163 "fp:bb7358f8") | low | `commit` runs on every blur, so focusing and leaving a field with no edit rewrites the value: a reversed `defaultValue` is reordered and `onValueChange` fires for a change the user never made |
| 340 | fixed · ab8a0bf | Timeline | [Timeline.tsx:99](src/components/ui/Timeline.tsx#L99 "fp:38ff34b4") | **high** | `Timeline.Item` types itself as a `<div>` but spreads its rest props onto `ScrollReveal`, which never spreads them onto the element it renders, so they all vanish on the default animating path |
| 341 | fixed · d706bf2 | Timeline | [Timeline.css:85](src/components/ui/Timeline.css#L85 "fp:11e9e5ce") | med | The responsive `r`-scale is read backwards: `--R-SIZE-6` (0.25rem at every width) separates two events while `--R-SIZE-1` (2.25rem → 6rem) separates a date from its title — `ActivityFeed` uses the same two tokens in the opposite roles |
| 346 | fixed · a4d2c12 | Timeline | [Timeline.tsx:46](src/components/ui/Timeline.tsx#L46 "fp:442c878e") | low | The root keys each context provider by array index, so prepending or reordering events remounts every entry from that point on, losing child state and replaying the entrance |
| 347 | fixed · fbdf501 + ab8a0bf | Timeline | [Timeline.test.tsx:89](src/components/ui/Timeline.test.tsx#L89 "fp:6e50d6ba") | low | Same pair as #185: fbdf501 ended the `matchMedia` throw, ab8a0bf added Timeline.test.tsx:87-99, a default-`animate` case covering the #340 path. Residual: the row's literal count ("all eight") is stale — the suite is now nine |
| 348 | fixed · aafb9f8 | util/date | [util/index.ts:9](src/util/index.ts#L9 "fp:f7917900") | low | Both reach the barrel: `getMonthNames` at `src/util/index.ts:9` and `toISODate` at :17 |
| 349 | fixed · a4d2c12 | Table | [Table.tsx:63](src/components/ui/Table.tsx#L63 "fp:0ccdbadb") | med | Every caller prop lands on the wrapper `<div>`; the inner `<table>` receives nothing, so it can never be given an accessible name via props |
| 350 | fixed · 7d48730 | Table | [Table.tsx:206](src/components/ui/Table.tsx#L206 "fp:db9857d8") | med | `Table.HeaderCell` spreads `{...props}` after `onClick={onSort}`, so a caller `onClick` silently replaces the sort click while Enter/Space still sort — mouse and keyboard diverge |
| 352 | fixed · 48f9736 | Table | [Table.css:119](src/components/ui/Table.css#L119 "fp:8b810590") | med | `stickyHeader` is inert by default: `.table-wrapper` is the header's scrollport (`overflow-x:auto` forces `overflow-y:auto`) but is content-height, so `<thead>` never pins **Verified true, no CSS-only fix exists.** `.table-wrapper` sets `overflow-x:auto`, which computes `overflow-y:auto`, making the wrapper the header's scrollport — and it is content-height. Sticky needs a bounded scrollport. `Table` callers can pass `style={{maxHeight}}` (documented); see #361 for the `DataTable` half. **Closed with `maxHeight?: number | string`.** The wrapper's `overflow-x: auto` computes `overflow-y: auto`, so the wrapper *is* the header's scrollport and it was content-height; sticky needs a bounded one. **Unverified half:** jsdom performs no layout, so the tests assert only that the bound lands on `.table-wrapper` and not on the `<table>` — nothing here observes the header actually pinning. |
| 354 | fixed · cbc6876 (response-ui-css) | Table | [Table.css:57](src/components/ui/Table.css#L57 "fp:eaf06b4a") | low | Sort-arrow contrast is under WCAG 1.4.11's 3:1 — idle `--C-TEXT-MUTED` on `--C-SURFACE-1` is 2.43/2.37/2.06/2.43:1 (default/events/tech/grimdark), and active `--C-ACCENT` is 2.63:1 in `events`, 2.77:1 in `grimdark`. **Both halves resolved, by different routes — neither leaves a true sentence on this row.** Muted half *fixed* by the css `cbc6876` retune: the head is `SURFACE-1`, so idle arrows re-measure **4.70–4.90:1**. Accent half *assessed and declined*: 2.63:1 `events` / 2.77:1 `grimdark` re-confirmed, but colour is not the sole channel — `Table.tsx` swaps three distinct glyphs (`ArrowUpDown`/`ArrowUp`/`ArrowDown`) and emits `aria-sort`. **Side effect worth knowing:** the retune inverts the pair, so the *active* arrow is now less contrasty than the idle one |
| 355 | fixed · a4d2c12 | Table | [Table.tsx:197](src/components/ui/Table.tsx#L197 "fp:e379aaef") | low | `sortDirection` without `onSort` emits `aria-sort="ascending"` but renders no arrow, no `tabIndex` and no sortable styling — announced to AT, invisible to sighted users |
| 356 | fixed · d706bf2 | Table | [Table.css:27](src/components/ui/Table.css#L27 "fp:3fcfb14e") | low | `.table-header-cell` hard-codes `font-weight: 600` instead of `--Semibold-Weight` (500/600 responsive, 700 in `grimdark`), so a theme's weight override never reaches the header |
| 357 | fixed · 236e6a0 | DataTable | [DataTable.tsx:44](src/components/ui/DataTable.tsx#L44 "fp:d2484bfa") | **high** | Controlled `sort` is not round-trippable: `sort?: SortState` rejects the `null` `onSortChange` emits, so `sort={sort ?? undefined}` silently flips the table to uncontrolled and it reorders rows from a stale `internalSort` |
| 358 | fixed · 7f651a6 | DataTable | [DataTable.tsx:446](src/components/ui/DataTable.tsx#L446 "fp:04201cd3") | med | The early returns are gone. `DataTable` has a **single** return (:419-433): one `renderHeader()`, one `<Table.Body>` choosing between `renderLoadingRows`/`renderRows`/`renderEmptyRow`, and `{footer}` (:428) + `renderPaginationBlock()` (:430) outside all three, so no state can drop them. A deliberate side effect: the loading state gains the outer `<div>` the other two already had |
| 359 | fixed · a4d2c12 | DataTable | [DataTable.tsx:244](src/components/ui/DataTable.tsx#L244 "fp:0b87f891") | med | `selectable` renders checkboxes that are permanently inert unless both `selectedKeys` and `onSelectionChange` are supplied, though the type marks both optional |
| 360 | fixed · cc6032f | DataTable | [DataTable.tsx:413](src/components/ui/DataTable.tsx#L413 "fp:9c84de88") | med | **Sentence re-verified and still true** after `7f651a6`: the paging migration onto `useControllableState` changed *who owns* the page, not which index is passed, and `pageData.map((row, i) => …)` still hands the slice index to `rowKey`, `column.render` (:397) and `renderExpanded` (:407). Handed back rather than fixed because changing it is a behaviour change to three public callbacks that any consumer may already have compensated for — a door the owner has not walked |
| 361 | fixed · 48f9736 | DataTable | [DataTable.tsx:95](src/components/ui/DataTable.tsx#L95 "fp:143ac12e") | med | Both cited clauses hold (the wrapper is the scrollport per Table.css:6-10 + 115-119; DataTableProps exposes no className/style/ref/rest). **"Can never take effect" overreaches:** as a scroll container the wrapper's flex/grid automatic min-size is 0, so a height-bounded flex or grid parent — or CSS on the public `.table-wrapper` class — does make the header pin. Default block-flow usage fails as claimed. docs/components/data-table.md:444-448 ships the stronger version and inherits the overreach **Verified true; closing it is an API addition, not a fix.** `DataTable` exposes no `className`/`style`/rest, so a caller cannot bound the scrollport at all. The fix is a new public prop — the same class of addition #463 was declined for, so it needs the owner rather than an agent. **Closed by the same prop as #352, given to both components deliberately.** `DataTable` exposed no `className`, `style`, `ref` or rest props at all, so its callers could not bound anything; giving the prop only to the composite would have left the primitive — the component the docs tell you to reach for when you need a pinned header — as the one table in the family without it. **This row's own overreach is corrected in the docs**: "can never take effect" was too strong, because a height-bounded flex or grid parent already makes the header pin (a scroll container's automatic min-size is 0). Default block flow is what failed, and `docs/components/data-table.md` now says so and retracts the claim. |
| 362 | fixed · a4d2c12 | DataTable | [DataTable.tsx:442](src/components/ui/DataTable.tsx#L442 "fp:79ce9411") | med | Row checkbox accessible names interpolate the raw row key into a hard-coded English string — `aria-label="Select row 8f3a-91c2-4de1"` (hard-coded-English pattern, as #222/#243/#259) |
| 363 | fixed · 7f651a6 | DataTable | [DataTable.tsx:365](src/components/ui/DataTable.tsx#L365 "fp:56559f85") | low | **Not patched — deleted.** The empty branch no longer builds a header at all, so there is no header cell left to omit; `renderEmptyRow()` (:347-353) is one `<Table.Cell colSpan={totalColumns}>` under the single shared `renderHeader()`, and `totalColumns` (:258) is the same count that header renders |
| 364 | fixed · 7f651a6 | DataTable | [DataTable.tsx:299](src/components/ui/DataTable.tsx#L299 "fp:8fa06538") | low | The header was written three times and the copies had drifted; exactly one `<Table.Head>` block remains, `renderHeader()` at :284-322, called once from the single return. Deliberate behaviour delta recorded in the commit: the loading and empty headers now carry a live select-all checkbox and live sort affordances, because they are now literally the same header |
| 365 | fixed · 933f1fb | DataTable | [DataTable.tsx:437](src/components/ui/DataTable.tsx#L437 "fp:bb9b4f74") | low | **Sentence re-verified and still true** after `7f651a6`: no `<tbody>` row was added or removed in any state (the loading/empty branches still pass `striped={false}` via `striped={hasRows && striped}` at :421), so an open `ExpandableDetailRow` (:402-409) is still a real `<tr>` flipping `:nth-child(even)` parity below it. Sibling of #368 — same root, different component, do not merge. Second clause stays visually inert as previously scoped |
| 366 | fixed · a4d2c12 | DataTable | [DataTable.tsx:330](src/components/ui/DataTable.tsx#L330 "fp:b02278ac") | low | The loading state renders one `Skeleton` per cell, each a `role="status"` region named "Loading" — measured 10 simultaneous polite live regions for `loadingRowCount={5}` × 2 columns (instance of #63, multiplied by rows × columns) |
| 367 | fixed · aafb9f8 | DataTable | [index.ts:18](src/components/ui/index.ts#L18 "fp:8e74cbdc") | low | `src/components/ui/index.ts:18` now reads `export { type ColumnDef, DataTable, type SortState } from "./DataTable"`, so the type is reachable without a deep import. **Narrower than the row's wording:** `DataTableProps` is still not exported — that half was never this row's claim and remains an open gap for whoever wants it |
| 368 | fixed · 933f1fb | VirtualizedDataTable | [VirtualizedDataTable.tsx:295](src/components/ui/VirtualizedDataTable.tsx#L295 "fp:26e27941") | med | **Sentence re-verified and still true** after `7f651a6`: the top spacer `<tr className="table-virtual-spacer">` is still emitted as a `<tbody>` child at :294-297, so it still shifts `:nth-child(even)` parity and the zebra still inverts on every row of scroll. Sibling of #365, not a duplicate — different component, different anchor, different severity |
| 369 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:163](src/components/ui/VirtualizedDataTable.tsx#L163 "fp:1f6dbc3f") | med | The select-all key list maps over the entire dataset unconditionally, including when `selectable` is `false` — the one thing a virtualizer exists to avoid (measured 100,068 `rowKey` calls for 100,000 rows) |
| 370 | fixed · 236e6a0 | VirtualizedDataTable | [VirtualizedDataTable.tsx:126](src/components/ui/VirtualizedDataTable.tsx#L126 "fp:07e1bb65") | med | `sort?: SortState` cannot express the cycle's third state, so mirroring the `null` from `onSortChange` silently switches the table from controlled to uncontrolled — same defect as #357, one component over |
| 371 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:170](src/components/ui/VirtualizedDataTable.tsx#L170 "fp:e85c19b5") | med | `selectable` without `selectedKeys` + `onSelectionChange` renders an enabled checkbox column in which nothing works (same as #359) |
| 372 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:282](src/components/ui/VirtualizedDataTable.tsx#L282 "fp:f626fea6") | med | A windowed table exposes no `aria-rowcount`/`aria-rowindex` and no `role="grid"`, so assistive tech sees only the mounted slice as the whole table — and the caller cannot patch it |
| 373 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:312](src/components/ui/VirtualizedDataTable.tsx#L312 "fp:30b736a6") | med | Row checkboxes are named with the raw `rowKey` value and there is no prop to override it — measured `aria-label="Select row inv_0"` (same as #362) |
| 374 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:184](src/components/ui/VirtualizedDataTable.tsx#L184 "fp:55d7c4ed") | low | `onEndReached` fires before any scroll whenever the first window already reaches the threshold, including from the `loading` branch (measured: one call on a mount with `loading` and 10 rows) **One clause fixed, one refuted.** The `loading`-branch fire is gone (the effect returns early while loading). The "fires before any scroll" clause is **not a defect**: `docs/components/virtualized-data-table.md:386` documents it — "It fires whenever the *rendered* window reaches within `endReachedThreshold` rows of the end" — so the row over-claimed. |
| 375 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:153](src/components/ui/VirtualizedDataTable.tsx#L153 "fp:1500b99a") | low | A non-numeric `height` seeds the viewport estimate as `0`, so the SSR/first paint renders only `overscan * 2` rows (measured server-side: 16 for `height="60vh"` vs 26 for `height={400}`) until the client measures **Fixed but unverified.** A non-numeric `height` seeded the viewport estimate at 0; it now seeds the documented 400px default. No test: jsdom's measurement overwrites the seed with 0 immediately, so the behaviour is only observable server-side. Claimed as unverified rather than covered. |
| 378 | fixed · 3f5463e | AvatarUpload | [AvatarUpload.tsx:163](src/components/ui/AvatarUpload.tsx#L163 "fp:339694cc") | **high** | Without `onUpload`, the optimistic object URL is revoked in the same tick it is set, so the chosen file never displays and — via Avatar's latching error flag (#55) — the avatar falls back to initials for the instance's life |
| 379 | fixed · 236e6a0 | AvatarUpload | [AvatarUpload.tsx:93](src/components/ui/AvatarUpload.tsx#L93 "fp:ecfa7273") | **high** | `accept` is validated with an exact-string `Array.includes`, so the conventional `["image/*"]` wildcard reaches the OS dialog but then rejects every file the dialog allowed |
| 380 | fixed · 3f5463e | AvatarUpload | [AvatarUpload.tsx:194](src/components/ui/AvatarUpload.tsx#L194 "fp:a4ae5155") | med | `{...props}` is spread after the component's own `onClick`/`onKeyDown`, so a caller-supplied handler silently replaces the file-picker trigger and the dialog never opens |
| 381 | fixed · ee5d181 | AvatarUpload | [AvatarUpload.tsx:212](src/components/ui/AvatarUpload.tsx#L212 "fp:76fd842c") | med | No busy state is exposed while `onUpload` is pending: the spinner is inside an `aria-hidden` span and the root gains neither `aria-busy` nor `aria-disabled`, while clicks in that window are silently dropped |
| 382 | fixed · ee5d181 | AvatarUpload | [AvatarUpload.tsx:255](src/components/ui/AvatarUpload.tsx#L255 "fp:f03a8326") | med | The validation/upload error is a `role="alert"` rendered inside the root `role="button"`, where ARIA's presentational-children rule makes it an unreliable live region — and `aria-label` wins the name computation |
| 385 | fixed · aafb9f8 | AvatarUpload | [index.ts:7](src/components/ui/index.ts#L7 "fp:b1228d81") | low | `src/components/ui/index.ts:5-9` now exports `AvatarUpload`, `type AvatarUploadProps` (:7) and `type AvatarUploadResult` (:8). The row's DataTable comparator was closed alongside it by #367 |
| 387 | fixed · 933f1fb | AppShell | [AppShell.tsx:202](src/components/ui/AppShell.tsx#L202 "fp:78d726e4") | **high** | On a mobile viewport `AppShell.Toggle` opens the drawer but cannot close it — `useClickOutside` closes on `mousedown` and the toggle's later `click` reopens it |
| 388 | fixed · 933f1fb | AppShell | [AppShell.tsx:311](src/components/ui/AppShell.tsx#L311 "fp:886505d9") | **high** | A collapsed desktop sidebar link has an empty accessible name — the label `<span>` is `display:none` and the icon is `aria-hidden`, so a collapsed rail is a list of unnamed links |
| 389 | fixed · 236e6a0 | AppShell | [AppShell.tsx:324](src/components/ui/AppShell.tsx#L324 "fp:a45e0228") | med | `AppShell.Main` renders a plain `<div>` with no `<main>` and no `role`, so a shell built from the parts exposes `banner` and `navigation` but zero main landmark and no skip-link target |
| 390 | fixed · 418ff90 | AppShell | [AppShell.tsx:181](src/components/ui/AppShell.tsx#L181 "fp:adbe1186") | med | `AppShell.Toggle` spreads rest props after its own `onClick`, so a caller-supplied `onClick` silently replaces the toggle's behaviour (rest-spread-after-handler pattern) |
| 391 | fixed · ee5d181 | AppShell | [AppShell.tsx:121](src/components/ui/AppShell.tsx#L121 "fp:69afd61c") | med | **Survived the `da9f457` AppShell migration and was re-measured open after it — do not close this from the controlled-mode cascade.** The mode lock changed which state `setOpen` writes, not when it is called: `AppShell.tsx:121`'s render-phase `if (isMobile && open) setOpen(false)` still runs inside the `prevPathname !== pathname` block (:119-122) and still calls the caller's `onOpenChange` (:95) during render. Measured after the migration: a route change with a controlled open drawer logs `console.error` "Cannot update a component while rendering a different component", **1 occurrence**. It needs the separate fix the detail block prescribes — notify from an effect, keep the render-time adjustment to internal state |
| 392 | fixed · ee5d181 | AppShell | [AppShell.tsx:240](src/components/ui/AppShell.tsx#L240 "fp:27dcc7f7") | med | The mobile drawer sets `aria-modal="true"` on a `role="navigation"` element (where the attribute is undefined) and marks nothing else `inert`, so AT can browse behind the scrim while the DOM focus trap disagrees |
| 393 | fixed · 127096a | AppShell | [AppShell.css:155](src/components/ui/AppShell.css#L155 "fp:ddc04a88") | med | The active link's `--C-ACCENT` ink over its own 10% wash measures 2.46:1 (`events`) / 2.83:1 (`grimdark`) — *lower* than a resting link (7.40 / 5.95) — so marking a link current makes it the least legible item (status-by-colour, #51 family) |
| 394 | fixed · ee5d181 | AppShell | [AppShell.tsx:174](src/components/ui/AppShell.tsx#L174 "fp:28557756") | low | The Toggle's `aria-controls` names an id absent from the document whenever the mobile drawer is closed (`AppShell.Sidebar` returns `null`) or no Sidebar is rendered |
| 396 | fixed · cbc6876 (response-ui-css) | AppShell | [AppShell.css:115](src/components/ui/AppShell.css#L115 "fp:55764bf1") | low | Sidebar section titles paint `--C-TEXT-MUTED`, measured 2.10–2.59:1 on `--C-SURFACE-0` across the four themes — under AA everywhere (instance of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Closed by the retune: **4.70–5.23:1** against the sidebar surface |
| 398 | fixed · 127096a | CommandPalette | [CommandPalette.css:137](src/components/ui/CommandPalette.css#L137 "fp:a361c98d") | **high** | The highlighted option is marked only by a `--C-SURFACE-2` wash over `--C-SURFACE-0` — 1.08–1.16:1 across the four themes, under WCAG 1.4.11's 3:1 — and there is no focus ring because focus is virtual, so keyboard users arrow blind (surface-ramp/keyboard-nav family, #275/#264) |
| 399 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:109](src/components/ui/CommandPalette.tsx#L109 "fp:6e4a96d7") | med | Arrow/Home/End traverse the flat `filtered` array while the DOM renders grouped, so non-contiguous members of one `group` make the highlight move out of visual order (measured rendered `[New document, Save, Copy]` vs arrow `[New document, Copy, Save]`) |
| 400 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:176](src/components/ui/CommandPalette.tsx#L176 "fp:fffcf720") | med | The effect that snaps `activeIndex` to the first selectable row depends on `filtered` → the `items`/`filter` props, so an inline array literal or inline arrow resets the highlight to row 1 on every parent re-render. Precise consequence: it resets to the first *selectable* index, not literally row 1 |
| 401 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:260](src/components/ui/CommandPalette.tsx#L260 "fp:16793bd5") | med | Re-scoped to "unnameable by the caller": the input carries no `aria-label`/`aria-labelledby`/`<label>` (:260-275) and rest props land on the `<dialog>` (:257), so nothing a caller passes can name it. It is not strictly nameless — `placeholder` (default "Type a command or search…") is accname's last-resort fallback for a textbox |
| 402 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:278](src/components/ui/CommandPalette.tsx#L278 "fp:3186489d") | med | No live region: filtering swaps the option list and the `emptyMessage` node silently (measured 0 `aria-live` nodes), so a screen-reader user gets no feedback on how many commands remain |
| 403 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:300](src/components/ui/CommandPalette.tsx#L300 "fp:195057c1") | med | Options sit in an unroled `<ul>` inside `<li role="group">`, so the listbox does not directly own its options; an ungrouped item also gets a nameless `role="group"` wrapper |
| 404 | fixed · cbc6876 (response-ui-css) | CommandPalette | [CommandPalette.css:179](src/components/ui/CommandPalette.css#L179 "fp:58dc799a") | med | Group headers and the empty message ink with `--C-TEXT-MUTED` on `--C-SURFACE-0` — 2.10–2.59:1 across the four themes, below the 4.5:1 body-text floor — and the empty message is the only content on screen in its state (instance of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Anchor covers the empty-message half only; the group-header twin is CommandPalette.css:105. Closed by the retune: the palette fill is `SURFACE-0`, so both the empty message and the group-header twin re-measure **4.85–5.23:1** |
| 405 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:255](src/components/ui/CommandPalette.tsx#L255 "fp:347159c2") | low | The panel omits the `no-body-scroll` class its sibling `Dialog` applies and `.command-palette-list` sets no `overscroll-behavior`, so the page scrolls behind the scrim |
| 406 | fixed · 4fd9f22 | CommandPalette | [CommandPalette.tsx:282](src/components/ui/CommandPalette.tsx#L282 "fp:733ba5f8") | low | The listbox's `aria-label="Commands"` is hard-coded English with no prop reaching it (hard-coded-English pattern; unlike the dialog's label, which the rest spread lets callers override) |
| 407 | fixed · 418ff90 | FileUpload | [FileUpload.tsx:478](src/components/ui/FileUpload.tsx#L478 "fp:91130b09") | **high** | `{...props}` is spread after the component's own `onClick`/`onKeyDown`/`onDragOver`/`onDragLeave`, so a caller silently replaces the only interactions the component has — measured `<FileUpload onClick={track} />` fires `track` and never opens the picker (rest-spread-after-handler pattern) |
| 408 | fixed · 236e6a0 | FileUpload | [FileUpload.tsx:372](src/components/ui/FileUpload.tsx#L372 "fp:de5fcb95") | med | `accept` is matched with exact-string `Array.includes(file.type)`, so the wildcard/extension forms it forwards to the input's `accept` attribute reject every file that comes back — `["image/*"]` → 0 `onFilesSelected` calls (same as AvatarUpload #379) |
| 409 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:395](src/components/ui/FileUpload.tsx#L395 "fp:22e134f2") | med | Files rejected by `accept`/`maxSize` produce no callback, no message and no state — the rejection is completely silent, despite the `error` prop's docblock claiming it "overrides internal state" that does not exist |
| 410 | fixed · ee5d181 | FileUpload | [FileUpload.css:43](src/components/ui/FileUpload.css#L43 "fp:5975ac88") | med | `uploading` combined with a non-empty `files` renders an inert preview (`pointer-events: none`) with no uploading indication and `aria-busy` null — Replace/Clear all/Remove are all dead and nothing says why |
| 411 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:502](src/components/ui/FileUpload.tsx#L502 "fp:c077fc89") | med | A per-file remove button falls back to `onClear` when `onRemoveFile` is not supplied, so removing one file deletes all of them (measured: X on the second of three fires `onClear` once, dropping all three) |
| 412 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:468](src/components/ui/FileUpload.tsx#L468 "fp:ce5d4fcc") | med | In the preview state three real `<button>`s are nested inside the root `role="button"` `tabIndex={0}` element, whose descendants ARIA makes presentational; Enter on the root reopens the file dialog rather than acting on the preview |
| 413 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:586](src/components/ui/FileUpload.tsx#L586 "fp:429c93f7") | med | The `error` and `success` messages render as a plain `<p>` with no live-region role and no `aria-describedby` from the dropzone, so they are never announced |
| 414 | fixed · cbc6876 (response-ui-css) | FileUpload | [FileUpload.css:16](src/components/ui/FileUpload.css#L16 "fp:b71e03c8") | med | The dropzone's `--C-TEXT-MUTED` prompt (2.06–2.43:1) and its `--C-BORDER-DEFAULT` 2px dashed border (1.18–1.26:1) on the `--C-SURFACE-1` fill the component paints are both far below their WCAG floors (contrast/#51 family; border is the whole "drop here" affordance) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. The `--C-BORDER-DEFAULT` half is a 1.4.11 non-text case the other #51 instances do not carry. **Both halves resolved, by different routes — neither leaves a true sentence on this row.** Muted half *fixed* by the css `cbc6876` retune: the prompt re-measures **4.70–4.90:1** on the dropzone's own `SURFACE-1` fill. Dashed-border half *assessed and declined*: 1.18–1.26:1 re-confirmed, but the border is not the sole identifier — the dropzone also renders an icon and the words 'Drag & drop … browse' |
| 417 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:56](src/components/ui/FileUpload.tsx#L56 "fp:a64437aa") | low | `children` is typed (props intersect `ComponentPropsWithRef<"div">` without omitting it) but always dropped, because the rest spread precedes the component's own JSX children (types-advertise-props-runtime-drops pattern) |
| 418 | fixed · ee5d181 | FileUpload | [FileUpload.tsx:477](src/components/ui/FileUpload.tsx#L477 "fp:66169a56") | low | `success` is silently ignored whenever `files` is non-empty — both the `--success` class and the success paragraph are gated on the empty state — while `error` renders in both |
| 419 | fixed · ee5d181 | FileUpload | [FileUpload.css:136](src/components/ui/FileUpload.css#L136 "fp:023bfcbb") | low | The `.file-upload__input` rule block (and its `--disabled` variant) is dead CSS: the hidden input renders with `className="sr-only"`, never that class |
| 421 | fixed · fbdf501 | usePrefersReducedMotion | [use-reduced-motion.ts:14](src/hooks/use-reduced-motion.ts#L14 "fp:647c1568") | med | `window.matchMedia` called unguarded while sibling `useMediaQuery` guards it, so the hook threw in any environment with `window` but no `matchMedia` (jsdom, headless) — which is the default `animate` path of every ScrollReveal/Stagger consumer, i.e. the exact configuration carrying #9/#10/#171/#178/#340 |
| 423 | fixed · 7d48730 | Tabs | [Tabs.tsx:295](src/components/ui/Tabs.tsx#L295 "fp:b6357153") | med | `Tab` spreads `{...props}` after its own `onClick`/`onKeyDown`, so a caller's handler replaces tab selection and roving-focus arrow keys (rest-spread-after-handler pattern, PLAN.md §2) |
| 424 | fixed · 7d48730 | Tabs | [Tabs.tsx:345](src/components/ui/Tabs.tsx#L345 "fp:04a63f20") | med | `TabPanel` spreads `{...props}` after its own `onAnimationEnd`, so a caller's handler replaces the one that fires `onExitComplete` — literally #13's shape in a second component |
| 425 | fixed · 7d48730 | Carousel | [Carousel.tsx:138](src/components/ui/Carousel.tsx#L138 "fp:2c4ada37") | med | The root spreads `{...props}` after its own `onKeyDown`, so a caller's handler replaces arrow-key navigation entirely (rest-spread-after-handler pattern) |
| 426 | fixed · 236e6a0 | NumberInput | [NumberInput.tsx:150](src/components/form/NumberInput.tsx#L150 "fp:952bb2c4") | med | `onChange` is `Omit`ted from the type but neither destructured nor guarded, so a JSX spread of `form.field()` both replaces the draft setter and writes a raw string into a numeric field — measured `{"qty":"57"}` after typing `7` into a field holding `5`, i.e. concatenation, not replacement (PLAN.md §2 and §3) |
| 427 | fixed · 15590d3 | FileUpload | [FileUpload.tsx:496](src/components/ui/FileUpload.tsx#L496 "fp:107959d1") | med | `onDrop` was the one handler not composed, because destructuring it meant removing `Omit<…, "onDrop">` — a public type change, so it was left `deferred` as a door. **The door was walked in `15590d3`**: the `Omit` is gone, `onDrop` is destructured (`FileUpload.tsx:329`) and composed (`:486`) with the same `composeEventHandlers` opt-out the sibling handlers already used. The defect it was hiding was live — a spread `onDrop` replaced the internal handler, so `onFilesSelected` never fired and the drop pipeline died silently. Revert: restore the `Omit`, drop `onDrop` from the destructure, and hard-set `onDrop={handleDrop}` |
| 428 | fixed · d08ad08 | Calendar | [Calendar.tsx:52](src/components/ui/Calendar.tsx#L52 "fp:c5ac569a") | low | `Calendar` and `RangeCalendar` (RangeCalendar.tsx:107) spread `{...rest}` after their own `onDaySelect`/`onDayHover`/`onTodayClick`, so those custom handlers are replaceable through a spread object even though a literal is blocked by the excess-property check |
| 429 | fixed · 236e6a0 | DatePicker | [DatePicker.tsx:197](src/components/form/DatePicker.tsx#L197 "fp:f0570c7c") | **high** | Spreading the advertised `form.field()` binding crashes on the first keystroke — `TypeError: d.getFullYear is not a function` — because the `Omit`ted `onChange` still lands via the spread (PLAN.md §3) |
| 430 | fixed · 236e6a0 | MultiSelect | [MultiSelect.tsx:194](src/components/form/MultiSelect.tsx#L194 "fp:bfd01c45") | **high** | Spreading the advertised `form.field()` binding crashes it — `selected.map is not a function`, thrown at MultiSelect.tsx:216 — because the `Omit`ted `onChange` still lands via the spread onto the wrapper, where React's delegated handler catches the inner search input's event (PLAN.md §3) |
| 431 | fixed · 236e6a0 | Slider | [Slider.tsx:60](src/components/form/Slider.tsx#L60 "fp:3c83ace2") | med | Spreading the advertised `form.field()` binding silently writes a string into a numeric field (`{"vol":"60"}`) — no crash, so nothing surfaces it (PLAN.md §3) |
| 432 | fixed · 236e6a0 | RangeSlider | [RangeSlider.tsx:137](src/components/form/RangeSlider.tsx#L137 "fp:c5c9f7a1") | med | Spreading the advertised `form.field()` binding writes a string where a `[number, number]` is declared; the component destructures that string, so the two thumbs visibly jump to `3` and `5` — corrupt, but not silent (PLAN.md §3) |
| 433 | fixed · 236e6a0 | OTPInput | [OTPInput.tsx:167](src/components/form/OTPInput.tsx#L167 "fp:77517a70") | **high** | Spreading the advertised `form.field()` binding makes the control unusable rather than merely lossy: each keystroke wipes the previous box, so typing `1234` leaves the store `{"code":"4"}` and the boxes `["4","","",""]` — a 4-digit code can never be entered (PLAN.md §3) |
| 434 | fixed · 236e6a0 | library-wide | [TagInput.tsx:204](src/components/form/TagInput.tsx#L204 "fp:22329591") | med | `form.field()` always includes `"aria-invalid": undefined`, and three components spread it *after* their computed `fieldErrorProps`, so binding a field erases the invalid state it just computed |
| 435 | fixed · 418ff90 | FileUpload | [FileUpload.tsx:594](src/components/ui/FileUpload.tsx#L594 "fp:5a843a4d") | med | The hidden input's programmatic `click()` bubbled back into the clickable dropzone and re-entered its handler — measured 2 `input.click()` calls per single user click, masked by a test asserting `toHaveBeenCalled()` rather than a count (#422 shape) |
| 436 | fixed · 3f5463e | AvatarUpload | [AvatarUpload.tsx:261](src/components/ui/AvatarUpload.tsx#L261 "fp:f332bbc6") | low | Same hidden-input `click()` re-entry as #435, fixed with #380 in the same commit for the same reason — composing the caller's handler would otherwise have double-fired it |
| 437 | fixed · b10cff5 | **library-wide** | library-wide | low | Both clauses are now false: `eslint.config.js` exists (jsx-key + rules-of-hooks) and package.json:46 carries `"lint": "eslint . --max-warnings 0"`. The config also sets `linterOptions.noInlineConfig`, which mechanically enforces CONTRIBUTING's "never suppress" rule the row said could not mean anything |
| 439 | fixed · 236e6a0 | DateRangePicker | [DateRangePicker.tsx:165](src/components/form/DateRangePicker.tsx#L165 "fp:85b4355f") | med | Spreading the advertised `form.field()` binding writes a raw string where `{ start, end }` is declared — no crash, so nothing surfaces it (PLAN.md §3) |
| 440 | fixed · e4f27c3 | FieldError | [FieldError.tsx:23](src/components/form/FieldError.tsx#L23 "fp:82987a8e") | med | An explicit `id` on `FieldError` overrides the context `errorId` while every bound control still points `aria-describedby` at `field.errorId`, so naming your own error element produces a dangling IDREF on the control |
| 441 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:237](src/components/form/MultiSelect.tsx#L237 "fp:bb17319e") | med | Chips are mouse-only removable (WCAG 2.1.1): MultiSelect.tsx:237 puts `tabIndex={-1}` on every chip's remove `<button>`, and the only keyboard route to removal is Backspace on an empty query (:195-202), which removes *the last chip only*. An arbitrary chip cannot be reached at all. Sibling `TagInput.tsx:194-200` renders its remove buttons with no `tabIndex`, so the two components disagree. Distinct from #267 (focus loss), #268 (glyph colour) and #272 (keys) |
| 442 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:182](src/components/ui/VirtualizedDataTable.tsx#L182 "fp:d426137d") | med | `onEndReached` can deadlock infinite loading permanently. `firedRef` is re-armed only by the `else if (!nearBottom)` branch (:189-190), but `nearBottom = endIndex >= len - endReachedThreshold` stays true as the user keeps scrolling toward the end — so once a page is appended without the window leaving the threshold, the loader is never called again. Measured: 20 rows → 1 call; consumer appends 4 → still 1 call. Any page size smaller than the window's overshoot past `len - threshold` stalls the loader for good. Related to #374 (fires too early) but the opposite failure |
| 443 | fixed · 4fd9f22 | Skeleton | [Skeleton.tsx:28](src/components/ui/Skeleton.tsx#L28 "fp:35b24efc") | low | Skeleton names itself twice on one node: :28 sets `aria-label="Loading"` and :33 renders an `.sr-only` "Loading" child inside the same `role="status"`. `aria-label` wins the name computation, so the span is unreachable markup rather than a second channel — and `Spinner` has the opposite shape (sr-only only, no `aria-label`), so the two are also inconsistent. #63 covers the region count and #64 the hard-coded string; neither covers the redundancy |
| 444 | fixed · ee5d181 | AppShell | [AppShell.css:200](src/components/ui/AppShell.css#L200 "fp:f8a0a84b") | low | `.app-shell-scrim` reads `var(--OVERLAY-SCRIM-COLOR)` with no fallback, so without the token layer the mobile drawer's scrim is transparent rather than 50% black. Identical root to #164 (Hero.css:49), different stylesheet — and the same two counter-examples one directory over, `Drawer.css:22` and `CommandPalette.css:22`, both carry `, rgb(0 0 0 / 0.5)` |
| 445 | fixed · b742101 | MultiSelect | [MultiSelect.tsx:104](src/components/form/MultiSelect.tsx#L104 "fp:f6fe4b3c") | low | No controlled-open API, while the sibling the docs present as its pair has one: MultiSelect.tsx:104-106 builds `open` from `useControllableState({ defaultValue: false })` with no `value` and no `onChange`, so `open`/`onOpenChange` are unreachable; `Combobox.tsx:127-131` passes all three. Undocumented asymmetry — with #265 (nothing can close the listbox) this leaves no way to shut the panel programmatically either |
| 447 | fixed · 7f651a6 | VirtualizedDataTable | [VirtualizedDataTable.tsx:198](src/components/ui/VirtualizedDataTable.tsx#L198 "fp:dd5bdcb6") | low | `renderHeaderStatic` is **deleted**; VDT keeps its early returns (`useVirtualRows` is documented to expect a null scroll element in the loading/empty branch) but all three now route through the one `renderHeader()` at :198-232, called from :239, :264 and :292. Exactly one `<Table.Head>` block remains in the file. The old anchor :332 no longer exists — the file is 331 lines |
| 448 | fixed · a4d2c12 | VirtualizedDataTable | [VirtualizedDataTable.tsx:249](src/components/ui/VirtualizedDataTable.tsx#L249 "fp:5c348b04") | low | The loading state renders one `Skeleton` per cell (:238-253), each its own `role="status"` region named "Loading" — the same multiplied-live-region defect #366 logs against DataTable, in the component #366 does not name (instance of #63) |
| 449 | fixed · d08ad08 | DatePicker | [DatePicker.tsx:206](src/components/form/DatePicker.tsx#L206 "fp:144c8455") | low | `disabled` never reaches the hidden ISO input: DatePicker.tsx:206 emits `<input type="hidden" name value>` and nothing else, so a disabled picker still submits its date — unlike a native disabled control. Exact mirror of #337, which is scoped to DateRangePicker only |
| 450 | fixed · d08ad08 | DatePicker · DateRangePicker | [DatePicker.tsx:163](src/components/form/DatePicker.tsx#L163 "fp:2ed266a7") | low | Enter is `preventDefault()`ed unconditionally in both pickers (DatePicker.tsx:163, DateRangePicker.tsx:171), so implicit form submission from a date field is dead — measured 0 submit events. Commit-on-Enter is a defensible design, but nothing re-offers the submit and no prop opts out |
| 451 | fixed · d08ad08 | DateRangePicker | [DateRangePicker.tsx:168](src/components/form/DateRangePicker.tsx#L168 "fp:9bfef653") | med | `handleKeyDown` (:168-176) neither invokes the caller's `onKeyDown` nor checks `defaultPrevented`, so a consumer's key handling on the range picker is silently dropped — while DatePicker.tsx:160-161 does both. 418ff90 composed DatePicker's handler and did not reach the range picker: the rest-spread-after-handler family (#390/#423/#425) with one call site missed **The row's first clause was false:** `onKeyDown` stayed in `props` and reached the wrapper `<div>`, so the caller's handler *did* fire by synthetic bubbling — it could not *preempt*, because the input handler had already called `preventDefault()`. Fixed as the row intended: composed into both inputs with a `defaultPrevented` check. Behaviour change to note — the caller's handler no longer sees keydowns on the trigger button. |
| 452 | fixed · d08ad08 | CalendarBase | [CalendarBase.tsx:345](src/components/ui/CalendarBase.tsx#L345 "fp:69316a00") | med | The month and year quick-nav grids write `aria-selected` on plain `<button>`s (:317, :346) — a role ARIA does not support it on, the same unsupported-attribute defect as #314 but in the views #313 covers, and named by neither. Worse than #314: the flag is `isCurrent`, the *displayed* month/year, so quick-nav announces navigation state as selection |
| 453 | fixed · d08ad08 | Calendar | [Calendar.css:193](src/components/ui/Calendar.css#L193 "fp:4a601db9") | low | A hovered ordinary day and an in-range day are pixel-identical: Calendar.css:193 paints hover `--C-SURFACE-2` and :233-237 paints `[data-in-range]`/`[data-preview]` the same token. Since range extent has no ARIA channel at all (#315), the wash is the only signal — and it collides with the hover state. Compounds #315 beyond its stated contrast problem |
| 454 | fixed · ee5d181 | Spotlight | [Spotlight.tsx:86](src/components/ui/Spotlight.tsx#L86 "fp:b714554d") | low | `Spotlight.Image` guards `src ? … : null` at :86 although `src` is typed required at :76 — a dead branch. Copy-paste residue from `Hero.Background`, where `src` is genuinely optional (Hero.tsx:59) and the guard is load-bearing (#165) |
| 455 | fixed · b742101 | Input · Select · Textarea | [Input.tsx:36](src/components/form/Input.tsx#L36 "fp:28ff5981") | med | The three plain-`<input>` controls spread `{...props}` **after** `{...ariaProps}` (Input.tsx:19+:31, Select.tsx:19+:32, Textarea.tsx:19+:31), so a `form.field()` binding erases the invalid state they computed from `error` — the exact class of #434, which fixed only TagInput, Slider and RangeSlider and never named these three. Measured: `<Input error {...{"aria-invalid": undefined}} />` renders `aria-invalid` **null**; RangeSlider under the same spread renders `"true"` |
| 457 | fixed · da9f457 · 7f651a6 | **library-wide** | 6 files, see detail | **high** | The controlled-mode bypass, filed as a class so it is inheritable: six components recomputed `controlled = prop !== undefined` on **every** render, so a parent writing `value={x ?? undefined}` flipped them uncontrolled mid-life — and the mirror direction was just as live (an uncontrolled component handed a `value` later flipped controlled and discarded what the user had done). Measured before each fix, then fixed: Accordion `value`/`defaultValue`, Tabs `value`/`defaultValue`, Popover `open`/`defaultOpen`, AppShell drawer `open`/`defaultOpen`, AppShell sidebar `collapsed`/`defaultCollapsed` (all `da9f457`), DataTable `page`/`onPageChange` (`7f651a6`). Same root as #357/#370/#232 (`236e6a0`) — siblings, not duplicates |
| 458 | fixed · da9f457 | AppShell | [AppShell.tsx:315](src/components/ui/AppShell.tsx#L315 "fp:10d8be0e") | med | `SidebarLink` took `to` for the destination and then spread `{...props}` after it into an adapter that renders `<a href={to} {...rest}>`, so a spread `href` silently won the destination. Measured: `<AppShell.SidebarLink to="/dashboard" {...{href:"/somewhere-else"}}/>` rendered `href="/somewhere-else"` — a link that navigates somewhere other than where it says, with `tsc` silent because `Omit` is erased at runtime and a JSX spread performs no excess-property check. Now `href?: never` (:300) and destructured out (:305). This was the last `verify:omit-discipline` violation; the guard exits 0 (102 protected, 0 unprotected) |
| 461 | fixed · b742101 | Combobox · MultiSelect | [Combobox.css:38](src/components/form/Combobox.css#L38 "fp:d7be8f80") | med | Focus erases the invalid border in the CSS-authored controls: `.combobox-input:focus-visible` (:24-26) sets `border-color: var(--C-BORDER-FOCUS)`, and `.combobox-input-error:focus-visible` (:38-40) declares **only** `box-shadow`, so at equal (0,2,0) specificity the error border repaints focus-blue the moment the field is focused — the error signal disappears exactly when the user is acting on it. `MultiSelect.css:34-36` is identical against `:focus-within` (:25-27). `ColorPicker.css:32-34` is the counter-example that gets it right (declares `border-color: var(--C-STATUS-ERROR)` too), and `Switch.css:45-49`'s comment already names the class: "Focus must not erase the invalid state". Instance of the #84/#567061e family. **Scoped to CSS after re-verification** — see #461's note in the detail block: the `.tsx` half of this split no longer exists |
| 462 | fixed · d08ad08 | Calendar · RangeCalendar | [Calendar.tsx:43](src/components/ui/Calendar.tsx#L43 "fp:c6ac6f21") | med | Same root as #324, untreated one layer up: `Calendar.tsx:43` and `RangeCalendar.tsx:60` call `useControllableState` with **no `isEqual`**, so the gate falls back to `Object.is`. `onDaySelect` hands back a fresh grid `Date` (`Calendar.tsx:57`) and RangeCalendar rebuilds `{start, end}` every commit, so re-selecting the already-selected day re-emits `onValueChange` for a value that did not change. The pickers were shielded by `5295190`; a direct `<Calendar>`/`<RangeCalendar>` consumer is not. Fix is the one `d859a02` already provides: pass `isEqual: isSameDateValue` / `isSameDateRange` from `date-picker-internals` **Calendar half fixed, RangeCalendar half refuted — both resolved.** `Calendar` now passes `isEqual: isSameDateValue`. `RangeCalendar` does not, deliberately: no reachable `setRange` resolves to the range already held, since `handleDaySelect` always flips `end` between a `Date` and `null`, so every commit is a genuine change. An `isEqual` there would be the inert comparator `use-controllable-state.ts` warns against. |
| 463 | fixed · 48f9736 | DataTable | [DataTable.tsx:87](src/components/ui/DataTable.tsx#L87 "fp:41de47e0") | low | API asymmetry, deliberately not added: `sort` has a `defaultSort` (:51) but `page` (:80) has no `defaultPage`, so deep-linking to a page — "open this table on page 3, then let it manage itself" — requires taking full control of `page` + `onPageChange` and re-implementing the paging the component already does. `7f651a6` migrated paging onto `useControllableState`, which is what would make `defaultPage` a one-line addition; it was left out because it is a public API addition, not a fix **Closed with `defaultPage?: number`, mirroring `defaultSort` exactly** — same docblock form, same seed-once-then-ignored semantics, and no clamping, because `defaultSort` clamps nothing either. An asymmetry closed by introducing a differently-shaped asymmetry is not closed. One line, as the row predicted, because `7f651a6` had already moved paging onto `useControllableState`. |

| 465 | fixed · upstream `@batthewz/response-ui-css` v0.10.0, re-measured this pass | FileUpload | [FileUpload.css:94](src/components/ui/FileUpload.css#L94 "fp:d8c521e6") | med | The dropzone's call to action — the word "browse" in `.file-upload__text-emphasis` — inks `--C-ACCENT` on the `--C-SURFACE-1` fill the component paints: **2.63:1 `events` / 2.77:1 `grimdark`**, under AA for body text. Surfaced 2026-07-26 by the contrast re-scope, not by any earlier pass; the same category-1 defect as #173 and unlogged until now. Not closed by the `--C-TEXT-MUTED` retune — this is accent ink, a different token **Reproduced independently** from the shipped OKLCH token values: `--C-ACCENT` on `--C-SURFACE-1` = **2.63:1 events / 2.77:1 grimdark**. Same family and same door as #173 and #319 — a palette value owned by the CSS package. **ARCHIVED 2026-07-26 — the sentence is no longer true.** `--C-ACCENT` on the dropzone's `--C-SURFACE-1` re-measures **4.95 / 4.74 / 5.32 / 14.56**; the row's 2.63 `events` / 2.77 `grimdark` are pre-retune values. Because `FileUpload.tsx:678` renders the emphasised word only in the empty/prompt branch, which can still carry the state classes, **every backdrop the word can actually land on was measured, not just the one the row names**: idle `--C-SURFACE-1` 4.95/4.73/5.33/14.56, drag-over `--C-SURFACE-2` 4.70/4.53/4.90/13.70, success `--C-STATUS-SUCCESS-BG` 4.94/4.80/4.54/13.39, error `--C-STATUS-ERROR-BG` 4.72/4.60/5.13/14.30. All four states clear 4.5 in all four themes. |
| 466 | fixed · upstream `@batthewz/response-ui-css` v0.10.0, re-measured this pass | Pagination | [Pagination.css:47](src/components/ui/Pagination.css#L47 "fp:ff3688c7") | med | The current page chip inks `--C-TEXT-ON-ACCENT` on `--C-ACCENT` — **2.80:1 `events` / 3.81:1 `grimdark`**, the same failing pair as #319. Surfaced 2026-07-26 by the contrast re-scope. `#133`'s fix was correct as far as it went (it escaped a 1.00:1 pairing) but landed on a pairing that still fails AA in two themes, so the row it closed should not be read as clearing this surface **Out of this package.** `--C-TEXT-ON-ACCENT` on `--C-ACCENT` is the contract-designated pairing, so the component is doing the right thing; the ratio is a palette value owned by `@batthewz/response-ui-css`. Same family as #319 — fixing it here means abandoning the named pairing. **ARCHIVED 2026-07-26 — the sentence is no longer true; same pairing and same upstream fix as #319.** `--C-TEXT-ON-ACCENT` on `--C-ACCENT` re-measures **5.17 / 5.04 / 5.69 / 14.84** against the shipped v0.10.0. The row's own reasoning was right on both counts — that `#133`'s fix had escaped a 1.00:1 pairing only to land on one that still failed AA, and that the remaining defect was a palette value the component could not correct without abandoning the contract-designated pairing. The palette moved; the component never needed to. |
| 467 | fixed · this pass | Swimlane | [Swimlane.tsx:66](src/components/ui/Swimlane.tsx#L66 "fp:4dda1c95") | med | `docs/components/swimlane.md` contradicts the component and the a11y advice built on it is wrong. The prop table (`:35`) says rest props are "**typed but never reach the DOM**" and `:249` repeats it, then concludes the lane "cannot be named" because `aria-label`/`aria-labelledby` are "among the props that never reach the DOM". They do reach it: `{...rest}` is spread onto the `<section>` at `Swimlane.tsx:66` and `:79`, with a passing test. `docs/` ships to npm, so this tells consumers they cannot do something they can **FIXED — the code was right and the page was wrong, so only the page changed.** Verified at source before touching anything: `{...rest}` is spread on the `<section>` at `Swimlane.tsx:66` (the `animate={false}` path) **and** onto `ScrollReveal as="section"` at `:79` (the animated path), and `Swimlane.test.tsx:195` asserts `aria-label="Featured titles"` lands on the root — re-run this pass, 14/14 green. So the prop table's "typed but never reach the DOM" and the accessibility section's "`aria-label`/`aria-labelledby` are among the props that never reach the DOM" were both false, and the page **contradicted itself**: its own Gotchas section at `:206` already described the true behaviour in detail. Corrected the prop-table row, the paragraph beneath it, and the accessibility bullet — which is the one that mattered, because it reasoned *from* the false claim to a false conclusion ("the section cannot be named") on a page that ships to npm, telling consumers the library cannot do something it does with the obvious prop. The bullet now shows the `aria-label` call, keeps the true half (Swimlane wires up no `titleId`, so nothing names the landmark for you), and points at `aria-labelledby` where the title already has an id. |
| 479 | fixed · this pass | **library-wide** | scripts/gen-docs.mjs | med | `scripts/gen-docs.mjs` **silently deletes documentation** — an audit row, not a component defect. Its marker regex spelled the body as an optional fence, so an empty ```` ```tsx ```` block's lazy inner quantifier could not find a closing fence before its own `<!-- /example -->`, kept expanding past every heading and paragraph that followed, and terminated on the *next* example's closing fence. One injection then overwrote the lot. The only signal was an "unused example" error naming a **different** example, and `--check` afterwards compared against the damaged file and agreed with it. It ate a section of `empty-state.md` and a section of `scroll-reveal.md`, a file its author had never opened. **Reproduced first**, on a fixture, before anything changed: the injection removed a `## Accessibility` heading, its prose and the entire following example, reporting only "unused example". **Fixed** by matching the opening marker to the *first* `<!-- /example -->` with an unconstrained lazy body — there is no longer an inner quantifier that can fail and force the match to grow past a close marker — plus a replacer guard that refuses (and reports) any body still containing an opening marker, which is the one remaining shape that could swallow a neighbour. **Coverage proved unchanged**: old and new patterns both match exactly 541 blocks across `docs/components/*.md`, so healthy pages are untouched and only the destructive path is gone. **Guarded permanently** by `assertMarkerCannotSpanBlocks()`, which runs unconditionally on every invocation *including the write path* and throws before any file is opened; it lives in the script rather than in a test because `vitest.config.ts` includes only `src/**`, so a `scripts/` test would never run with the suite. Fail-first verified in both directions: restoring the old pattern exits 1 with "MARKER self-check failed for \"empty fence\" — matched [First], expected [First,Second]", and the four fixtures (empty fence, no fence, normal fence, a fence quoting a fence) plus the unclosed-block case all pass on the new one. |


### Won't fix / decided

Assessed and declined, with the reason in the status. Re-opening one needs the owner, not a re-measurement.

| # | Status | Component | File:line | Sev | Summary |
| - | ------ | --------- | --------- | --- | ------- |
| 3 | wontfix · no measured pairing; the inherited ink is correct | Card | [Card.tsx:36](src/components/ui/Card.tsx#L36 "fp:72c385f6") | med | Themed surface with no paired text colour → contrast risk in dark themes — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 52 | wontfix · a bare token pair in another package; nothing here inks `TEXT-SECONDARY` on `SURFACE-3` | **response-ui-css** | grimdark theme | low | `--C-TEXT-SECONDARY` on `--C-SURFACE-3` is 4.45:1 — just under the 4.5:1 AA threshold — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 78 | wontfix · surface-vs-surface, and state also rides thumb position, track hue and `aria-checked` | Switch | [Switch.css:63](src/components/form/Switch.css#L63 "fp:5abefb41") | med | Thumb-vs-track contrast is 1.08–1.16:1 unchecked in **all four** themes (WCAG 1.4.11 needs 3:1) — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 87 | wontfix · surface-vs-surface rim; decorative separation | Slider · RangeSlider | [Slider.css:51](src/components/form/Slider.css#L51 "fp:84162c0f") | low | **Half closed by `c3f244b`, and open on the other half — do not read this row as fixed.** The *focus-ring gap* (`box-shadow: 0 0 0 2px var(--C-SURFACE-0)` under the ring) is gone from all four thumb pseudo-elements; the ring now lands flush on the thumb's own border. Still true: the thumb **border** is `2px solid var(--C-SURFACE-0)` at Slider.css:51, :70 and RangeSlider.css:77, :102, so the handle still draws a surface-0 rim on any other layer. Deliberately left — the rim is load-bearing separation between the accent fill and the track, so retinting or removing it is a design call, not a mechanical fix — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 90 | wontfix · behaviour deliberate; 57c4753 corrected the docs instead | **useTheme** | [use-theme.ts:80](src/hooks/use-theme.ts#L80 "fp:01d0cac4") | **high** | `setTheme` writes `localStorage["theme"]` (use-theme.ts:86,93) and nothing reads it back — `getSnapshotFor` reads only `<html data-theme>`. Still true. 57c4753 fixed the *claim*, not the behaviour: README.md:86 and the hook docblock now both state "Persistence is not included" and point at the blocking-script recipe. Left as-is on purpose |
| 206 | wontfix · progress *track* fill, not a control boundary | ProgressBar | [ProgressBar.css:10](src/components/ui/ProgressBar.css#L10 "fp:8c7dd73c") | med | The track is `--C-SURFACE-1`, measured at **1.05 / 1.03 / 1.02 / 1.07:1** against `--C-SURFACE-0` in default / `events` / `tech` / `grimdark` — the unfilled remainder is effectively invisible in every shipped theme (instance of the unmeasured-contrast pattern) — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 207 | wontfix · progress *track*; the fill never vanishes and `aria-valuenow` carries the value | ProgressBar | [ProgressBar.css:40](src/components/ui/ProgressBar.css#L40 "fp:74cb3f5d") | med | Re-scoped: the contract's own remedy for a fill drawn anywhere but under its paired ink — "outline it in its `on-*` token" — is not applied to `--C-ACCENT` on the `--C-SURFACE-1` track, so the fill can vanish into its track in any theme where the two land close. The quoted ratio is #51's; the missing outline is this row's — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 210 | wontfix · ring *track* stroke | ProgressRing | [ProgressRing.css:16](src/components/data-display/ProgressRing.css#L16 "fp:3dfdead4") | med | The track strokes `--C-SURFACE-2`, measured at **1.10 / 1.08 / 1.08 / 1.16:1** against `--C-SURFACE-0` across the four shipped themes — the same invisible-track defect as #206, marginally better and still an order of magnitude under the 3:1 floor — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |
| 320 | wontfix · today is also marked by `Semibold` weight and `aria-current="date"` | Calendar | [Calendar.css:205](src/components/ui/Calendar.css#L205 "fp:c54739f2") | low | Today's only visual marker is a 1px `--C-BORDER-STRONG` inset ring, measured 1.41–1.79:1 against `--C-SURFACE-0` across all four themes — effectively invisible (`aria-current="date"` covers AT only) — **re-scoped 2026-07-26:** out of scope under the contrast rubric (real only when it is text a user must read, text on a coloured fill, status text on a status fill, or a state whose *only* channel is colour). Ratios that are a property of a theme's token choices are the theme author's call |

### Refuted

Claimed, measured, disproved. Kept so the claim is not re-filed by the next reader.

| # | Status | Component | File:line | Sev | Summary |
| - | ------ | --------- | --------- | --- | ------- |
| 53 | refuted · fa635d2 · documented contract, and the proposed fix would break the documented use case | Text | [Text.tsx:11](src/components/ui/Text.tsx#L11 "fp:ed6c4a09") | med | `variant` sets size only; heading face/tracking/weight come from `h1`–`h6` element selectors, so `variant`+`as` desyncs look from size **Disproved as a defect.** The row's *fact* is true — `text-h*` sets size and leading only, while face, tracking and weight come from `h1`–`h6` element selectors (confirmed at source and in Firefox: `<h2>` 700 vs `<p class="text-h2">` 400). But `docs/components/text.md` has a section named **"Size and semantics are separate"** whose example is `<Text variant="h3" as="p">`, described as "the honest way to get heading-sized text that is not a heading", plus a gotcha saying the same. Adding `.h1`–`.h6` to the variant map would give that construct the full Cinzel/uppercase/700 heading voice in `grimdark`, destroying exactly what the section sells. `Text.tsx` is unchanged; the doc gained one sentence naming the opt-in that already exists (`class="text-h2 h2"`, measured 48px/700). |
| 123 | refuted · measured, the reference is rebuilt on every open | ContextMenu | [ContextMenu.tsx:86](src/components/ui/ContextMenu.tsx#L86 "fp:25a7ce5b") | med | `setPositionReference` is never cleared, so every later open reuses the last cursor point |
| 127 | refuted · 4fd9f22 · both sides resolve to floatingId; verified at source | Popover · HoverCard · DropdownMenu | [Popover.tsx:196](src/components/ui/Popover.tsx#L196 "fp:0c8f1506") | med | **Re-scoped from low, and from "harmless".** Tooltip is fixed (Tooltip.tsx:110 sets `id` *after* the spread, with the reason in a comment). The other three still set `id` first: Popover.tsx:196 before :199, HoverCard.tsx:185 before :192, menu-internals.tsx:182 before :185. `useRole` is re-exported bare from `@floating-ui/react` (use-floating.ts:51) and supplies its own floating `id`, which wins — so Popover.tsx:149, HoverCard.tsx:139 and DropdownMenu.tsx:73 each advertise an `aria-controls` naming an id no element in the document carries. A dangling IDREF (WCAG 4.1.2), not dead wiring. One-line fix per component **Disproved, and this row's own detail block was wrong.** The block tabulated three panels that set `id` above floating-ui's spread — true — and missed that the *trigger* side is overwritten by the identical spread: `triggerProps` sets `aria-controls` before `...getReferenceProps(props)`, and `useRole`'s reference props carry `'aria-controls': floatingId`. Both sides therefore land on `floatingId` and agree; `getElementById` resolves. Measured on all three (`_r_1_`/`_r_5_`/`_r_9_`) and re-verified at source before closing. `Tooltip` needed its fix for a different reason — it sets `aria-describedby` *after* the spread. |
| 163 | refuted · the package ships the non-guarantee itself | Hero | [Hero.tsx:49](src/components/ui/Hero.tsx#L49 "fp:68760345") | med | docs/components/hero.md:253-258 states it verbatim: "The contract only promises `--C-TEXT-ON-PRIMARY` against `--C-PRIMARY` fill; a scrimmed photograph is not that fill, and nothing in the system checks the pair." A documented, measured non-guarantee with three remedies listed — not an undiscovered defect. The scrim's own behavioural defect is #162 |
| 172 | refuted · ee5d181 · not scrolling is the documented contract; the row's evidence was a stale test name | Swimlane | [Swimlane.css:47](src/components/ui/Swimlane.css#L47 "fp:da05b52c") | med | Nothing in the component scrolls: no `overflow`, `scroll-snap-type`, `scroll-behavior` or `tabindex` anywhere in `Swimlane.css`, despite the name and a test called "renders a scrollable container" **Disproved.** `docs/components/swimlane.md` states it in three places, including "**It does not scroll.** … Bring your own scroller" and an a11y bullet telling the caller to add `tabIndex`/`role`/`aria-label` themselves; both shipped examples supply their own scroller. The row's only evidence was the stale test name "renders a scrollable container", now renamed with a comment pointing at the doc. Adding `overflow` would be a breaking change against the component's stated contract. |
| 195 | refuted · ee5d181 · overstated; true only for an authoring order the docs call unsupported | Spotlight | [Spotlight.css:58](src/components/ui/Spotlight.css#L58 "fp:3e2f7038") | med | Every `order` rule pushes the image last and the copy first, so a row authored content-then-image is already in that order — both the automatic alternation and `reversed` silently do nothing **Overstated.** "Both the automatic alternation and `reversed` silently do nothing" holds *only* for content-then-image authoring, which the docs already name unsupported ("Image-before-content is load-bearing"). For the documented image-first order — all six examples — alternation and `reversed` both work, traced through all four states including the reversed-even cancel. The underlying fragility was removed anyway by #200's order-independent selector pair, so the row's scenario now works too. |
| 215 | refuted · the shape channel exists at source | Rating | [Rating.css:9](src/components/ui/Rating.css#L9 "fp:5128e77e") | med | Re-scoped off the ratios (`--C-STATUS-WARNING` is a status foreground the contract pairs only with `--C-STATUS-WARNING-BG`, and names no number against a surface). The surviving component-level defect: the empty/filled distinction is **opacity alone** (Rating.css:54, `opacity: 0.45`) — one channel, no shape, outline or glyph change. **Refuted 2026-07-26:** the surviving sentence is false — `Rating.css` bases the star `fill:none; stroke:currentColor` and the fill layer `fill:currentColor`, so outline-star vs solid-star *is* a shape channel, not opacity alone |
| 242 | refuted · a 2px ring replaces the outline in the same declaration | **library-wide** | [Input.tsx:30](src/components/form/Input.tsx#L30 "fp:07694564") | med | Clause 2 is false at source: Input.tsx:30-31 is `focus:outline-none` followed by `ring-2 ring-transparent focus:ring-border-focus` — the UA fallback is replaced, not removed, and fa1a38e's `verify-focus-affordance` gate now enforces exactly that pairing (43f45ea wires it into the build; css `0903176` themed the offset). Clause 1 is a bare ratio against `--C-BORDER-FOCUS` used per contract — that is #51's token-value claim, not this recipe's |
| 296 | refuted · the whole props object is taken and spread | RangeSlider | [RangeSlider.tsx:92](src/components/form/RangeSlider.tsx#L92 "fp:6a993762") | med | Falsified at source: RangeSlider.tsx:92 takes all of `useFieldErrorProps(error)` — Field.tsx:54-56 returns both `aria-invalid` and `aria-describedby` — and :152 spreads the pair via `mergeProps`. Nothing is discarded. The surviving defect is #295's: both land on a non-focusable wrapper, so neither reaches a focused thumb |
| 329 | refuted · measured with a controlled harness; the mechanism was closed by the `isEqual` commit and never re-verified | DatePicker | [DatePicker.tsx:144](src/components/form/DatePicker.tsx#L144 "fp:4e80e8de") | low | Commit re-parses the formatted display string, so a value carrying a time of day is silently truncated to midnight on the first blur (measured with `new Date(2026,5,10,14,30)`) **Disproved:** with `new Date(2026,5,10,14,30)`, focus + blur and no edit, the time of day survives. `DatePicker` already passes `isEqual: isSameDateValue` to `useControllableState`, so the re-parsed midnight `Date` is day-equal to the committed value and the setter is a no-op. Re-measured with the draft guard removed — still survives. |
| 459 | refuted · measured against source; characterisation test at ProgressBar.test.tsx | ProgressBar | [ProgressBar.tsx:68](src/components/ui/ProgressBar.tsx#L68 "fp:61147686") | med | **Claimed and disproved:** that `ProgressBar` `Omit`s `children` without destructuring it, so a spread `{children}` displaces the fill. It provably cannot. JSX element children are emitted **after** the spread in the object the JSX runtime builds, so the component's own children always win. Measured with a bag of `{children:"HIJACKED", id:"bar"}`: root `textContent` `""`, `id` `"bar"`, fill still present. The `Omit` needs no runtime guard. Recorded as a row rather than left in `2006872`'s commit body — a refutation living only in a commit message is invisible to the next reader |
| 460 | refuted · measured against source; characterisation test at StatCard.test.tsx | StatCard | [StatCard.tsx:147](src/components/ui/StatCard.tsx#L147 "fp:bdeba930") | med | Same claim as #459 against `StatCard.Trend`, disproved the same way and by the same mechanism. Measured: `<StatCard.Trend value={12} direction="up" {...{children:"HIJACKED"}}/>` renders `textContent` `"+12%"` and still contains the arrow `<svg>`. Filed separately from #459 per the preamble's do-not-merge rule: different component, different anchor, different test |

---

## How this record was built, and where it went wrong

Kept because the failures were expensive and are not obvious in advance.

Code defects surfaced while documenting the components. **Not doc issues** — those get
fixed in the doc. These are places where the *code* is wrong, misleading, or violates a
contract (accessibility, the contrast contract, ETHOS token rules, logic). Logged here
for later audit rather than fixed inline, so the documentation pass stays reversible and
one concern at a time.

**Provenance:** surfaced by the `gen-component-docs` workflow — author + adversarial
verifier agents. Confidence tags apply to `unaudited` rows only: `corroborated` = an
author and an independent verifier flagged it separately; `spot-checked` = confirmed by
hand against the source; `candidate` = single-source, unverified; `caveat` = a passing
guard disagrees, or the claim cannot be settled in this repo.

**What a row *is*** — its kind (accessibility, behaviour, consumer contract, ETHOS contract,
environment, repo health) and its harm (blocking, content-loss, exclusionary, portability,
contract-only, meta) — is defined in [`TAXONOMY.md`](./TAXONOMY.md), with the seam rules for the
cases that classify two ways. Neither axis is a column here, by the same rule that keeps cluster
membership out: the join is done at report time. `Sev` predates both axes and blends them.

**Reconciled 2026-07-25 against HEAD.** Three read-only sweeps re-verdicted every open row
in #1–#440 against source; their verdicts are written in above. What that means for
reading this file:

- `unaudited` is now the exception, not the rule — **two** rows keep it (#80, #88), both
  because the claim is a browser UA-default divergence jsdom cannot settle. They are open
  on purpose; do not close them from the cascade.
- A `fixed` row names the commit that closed it. Where two commits were needed the row
  names both (#136, #185, #347) — one instance fixed is not a row closed. #71 keeps its
  `fixed` mark but its wording was wrong: it described behaviour 7d48730 deliberately
  *preserved*. A row reading `fixed` while its own sentence is still true is the most
  expensive error this file can carry.
- Contrast rows measuring a `--C-TEXT-*` / `--C-BORDER-*` token against a WCAG number stay
  **open** and now say so: the ratio is #51's token value, not the component's usage.
  `docs/theme-contract.md:88` is explicit that the pairing is "a convention, not a measured
  ratio", so the entitlement was misattributed — but the contract's silence does not waive
  the standard, and the fix still has to land somewhere.
- Rows that look like duplicate pairs (#357/#370, #379/#408, #410/#381, #412/#382,
  #436/#435, #396/#404/#414) are **not** duplicates and must not be merged. They are
  same-root siblings in different components with different anchors and severities, and
  #435/#436 carry two different fixing SHAs. Merging destroys a commit reference.

**~~Known gap in the oracle~~ — closed 2026-07-26.** `verify:bugs` used to only bounds-check
that an anchor's line number existed in its file; nothing checked that the line still held the
code the row describes. That reconcile moved **157** anchors rotted under intervening patches —
better than a third of the table — and no gate would have caught one. Every anchor now carries a
content fingerprint in the markdown link title, `(src/x.tsx#L12 "fp:1a2b3c4d")`, which renders as
a tooltip and stays out of the table. It hashes the anchored line **plus one line either side**,
each trimmed and internally whitespace-collapsed, so a reindent does not fire but a token change
does. A missing or malformed fingerprint is a violation, not a skip.

**Editing an anchor:** run `node scripts/bugs-ledger.mjs --reanchor`. Where the code merely
moved, it finds the stored fingerprint elsewhere in the file and slides the line number to match,
leaving the digest alone — that is the 157-row case, and it is fully automatic. Where the content
at the anchor actually *changed*, it restamps and prints the row under **RE-VERIFY BY HAND**:
those rows may now be describing different code, and only a reader can say whether the row's
sentence still holds. Do not run `--reanchor` to clear a red gate without reading that list.
The fingerprints were first stamped against `1ed1755`; a first stamping certifies today's
content and nothing more.

**Reconciled again 2026-07-26 against `c3f244b 00f6b03 6ed0ad3 aafb9f8 2006872 d859a02 da9f457
5295190 7f651a6 d14c7be`.** Every status written in that pass was re-verified against source by
reading the file at the anchor, not by trusting the commit message. What a reader needs to know:

- **#73 was a phantom** and is now `fixed`. It read `confirmed · high` — the file's highest-severity
  open row — while its own sentence had been false at source since `ee59e65`, which is precisely the
  error the note above calls the most expensive this file can carry. It had two other copies:
  `bugs/components/radio.md` and `docs/components/radio.md`. Nothing could see any of them; the class
  is now filed as **#464**.
- **Partly-fixed rows stay open and say which half closed.** #87 (focus gap closed, thumb border
  open), #284 / #293 (divergence refuted, hand-rolled duplication open), #397 (navbar height closed,
  breakpoint structurally unfixable). A half-closed row marked `fixed` is the same error as a false
  `fixed`.
- **#391 was re-measured *after* the migration that looked like it should have closed it** and is
  still open, 1 `console.error` occurrence. Do not close it from #457's cascade.
- **Refutations are rows now (#459, #460), not commit-message prose.** The prior pass's lesson was
  that a refutation living only in a commit body is invisible to the next reader.
- **`deferred` marks an explicitly-declined decision, and each one carries its reason in the status
  payload** — #365 / #368 (cross-package door: the fix deletes a public CSS selector), #360 (public
  callback contract), #310 (assessed, judged RC-4 not RC-3). They are open on purpose. Re-deciding
  them needs the owner, not a re-measurement.
- **The palette retune / WCAG floor is declined, twice, by the owner.** The contrast rows (#51, #52
  and the `--C-TEXT-*` / `--C-BORDER-*` family) stay open and their measurements stand;
  `docs/theme-contract.md:88` records that no ratio commitment was made. Do not re-open the question
  as a finding — it is a product decision, and the fix still has to land somewhere.


---

## Retired detail blocks

Kept because the analysis is instructive, not because it holds. A detail block that outlives
> Row #441 is closed; this block describes the defect as it stood.

### 441 · MultiSelect — a chip cannot be removed from the keyboard (med)

`MultiSelect.tsx:237` sets `tabIndex={-1}` on every chip's remove `<button>`. The only keyboard
route to removal is Backspace on an empty query (`:195-202`), which removes the **last** chip;
an arbitrary chip in the middle of the selection has no keyboard path at all. WCAG 2.1.1.

The sibling proves the fix is available and free: `TagInput.tsx:194-200` renders the same
control with no `tabIndex`, so its chips are tabbable. Removing the attribute makes each chip a
tab stop, which is the standard multiselect pattern; the alternative (arrow-key roving over the
chip row) is what `useRovingFocus` exists for.

Not a duplicate of the three MultiSelect chip rows already logged: #267 is focus loss *after* a
successful mouse removal, #268 is the glyph's contrast, #272 is duplicate React keys.

> Row #442 is closed; this block describes the defect as it stood.

### 442 · VirtualizedDataTable — `onEndReached` deadlocks infinite loading (med)

```
const nearBottom = endIndex >= sortedData.length - endReachedThreshold;   // :182
if (nearBottom && sortedData.length > 0 && !firedRef.current) {           // :185
  firedRef.current = true; onEndReached();
} else if (!nearBottom) { firedRef.current = false; }                     // :189
```

The latch is re-armed only when the window leaves the threshold. A consumer appending a page
smaller than the window's overshoot past `len - threshold` never leaves it, so `nearBottom`
stays `true`, `firedRef.current` stays `true`, and the loader is never called again — the list
stops loading permanently with no error and no signal.

Measured: 20 rows, threshold default → 1 call. Consumer appends 4 rows → still 1 call.

#374 is the opposite end of the same effect (it fires before any scroll, including from the
`loading` branch, because the effect sits above both early returns). Fixing one does not fix the
other: #374 wants a guard on the *first* call, #442 wants the re-arm keyed to the data length
rather than to `nearBottom`.

> Row #451 is closed; this block describes the defect as it stood.

### 451 · DateRangePicker — the caller's `onKeyDown` is dropped (med)

`DateRangePicker.tsx:168-176` is:

```
const handleKeyDown = useCallback((e) => {
  if (e.key === "Enter") { e.preventDefault(); commit(); }
}, [commit]);
```

No `onKeyDown?.(e)`, no `defaultPrevented` check. `DatePicker.tsx:160-161` has both. `418ff90`
composed DatePicker's handler under the rest-spread-after-handler programme (#390/#423/#425) and
did not reach the range picker — a shared-fix-missed-a-call-site, the same shape as `875beb9`'s
two findings.

> Row #452 is closed; this block describes the defect as it stood.

### 452 · CalendarBase — quick-nav marks the *displayed* month as selected (med)

`CalendarBase.tsx:317` and `:346` write `aria-selected={isCurrent}` on plain `<button>`s in the
month and year quick-nav grids. Two defects at once:

1. `aria-selected` is not supported on role `button`, so a conforming AT drops it — the same
   unsupported-attribute defect as #314, but in the views #313 covers. Neither row names it.
2. `isCurrent` is `m === displayedMonth.getMonth()` — the month the grid is *showing*, not the
   month of the selected value. Where the attribute is honoured, quick-nav announces navigation
   state as selection, which is worse than dropping it.

Fix both together: the selection state belongs on the `role="gridcell"` wrapper (as #314
prescribes) and the flag should read the selected value, with the displayed month carried by
`aria-current` instead.

its row is worse than none — it reads as current.

> **Refuted by 4fd9f22.** The table below is accurate about the panel side and silent about the
> trigger side, which is overwritten by the same spread. Both resolve to `floatingId`.

### 127 · Popover · HoverCard · DropdownMenu — a dangling `aria-controls` on three floating triggers (low → med)

Re-scoped from `low` and from "harmless today". The mechanism the row named is real, but the
consequence it dismissed is a live WCAG 4.1.2 failure.

`useRole` is re-exported bare from `@floating-ui/react` (`src/hooks/use-floating.ts:51`) and
supplies its own floating `id`. Three components set their own `id` *above* the spread that
carries floating-ui's, so floating-ui's wins and the panel never carries the id its trigger
advertises:

| trigger advertises | panel sets `id` at | spread that overwrites it |
| ------------------ | ------------------ | ------------------------- |
| `Popover.tsx:149` | `Popover.tsx:196` | `:199` |
| `HoverCard.tsx:139` | `HoverCard.tsx:185` | `:192` |
| `DropdownMenu.tsx:73` | `menu-internals.tsx:182` | `:185` |

`Tooltip` already carries the one-line fix — `Tooltip.tsx:110` sets `id` *after*
`{...getFloatingProps()}`, with a comment naming this exact failure. The same move closes all
three. Do not fix by deleting the `useId()`: the trigger would then point at an id floating-ui
mints per render.
