# Findings ledger

Open defects in this package's shipped code, one row per finding. **This file is the work
list**: everything in it is believed true at source and unfixed. Closed rows live in
[`ARCHIVE.md`](./ARCHIVE.md); findings about the audit itself — gates, tests, the record —
live in [`AUDIT.md`](./AUDIT.md). Arriving cold? Start with [`HANDOVER.md`](./HANDOVER.md).

- **What a row *is*** — its kind and its harm — is defined in [`TAXONOMY.md`](./TAXONOMY.md).
  Work order is **harm first**: blocking, then content-loss, then exclusionary, then
  portability, then contract-only.
- **Work order is harm**, per [`TAXONOMY.md`](./TAXONOMY.md). ([`PLAN.md`](./PLAN.md) is retired — it survives only as a section map for the archived rows that cite it.)
- **Verify before you fix.** A row is a claim, not a fact. This file has carried a row marked
  `confirmed · high` whose sentence was false at source, and seven claims that measurement
  disproved. Read the code before you change it; if the row is wrong, say so in the row.
- **Enumerate, never count.** A row saying "three components" that does not name them cannot be
  audited, and one such row hid the same defect in three further components for a whole pass.
- **Same-root siblings are not duplicates.** Rows sharing a cause in different components have
  different anchors and sometimes different fixing commits. Merging them destroys a reference.
- **One instance fixed is not a row closed.** A row naming two components needs both.
- `deferred` means an owner declined it, with the reason in the status. Those rows are open on
  purpose; re-deciding needs the owner, not a re-measurement.

Line numbers in anchors drift as code moves. `node scripts/bugs-ledger.mjs --reanchor` slides
them back when the code merely moved, and prints the rows where the content actually changed —
those may now describe different code, and only a reader can say.

## Findings

| # | Status | Component | File:line | Sev | Summary |
| - | ------ | --------- | --------- | --- | ------- |
| 8 | confirmed · source | DescriptionList | [DescriptionList.tsx:22](src/components/data-display/DescriptionList.tsx#L22 "fp:db024a7f") | low | `display:grid/flex` on `<dl>` can drop list semantics in Safari+VoiceOver **Investigated and deliberately not fixed.** `role="list"` on a `<dl>` is not the remedy it is on `<ul>`/`<ol>`: it would leave `dt`/`dd` as non-`listitem` children, which risks worse semantics than the bug. Needs a decision about what the `<dl>` should expose, not a one-line attribute. **Re-confirmed 2026-07-26 and correctly left open, now grounded in the installed `aria-query` rather than in reasoning.** No code changed. Three facts, all checked offline: `roles.get("list").requiredOwnedElements` is `[["listitem"]]` while `elementRoles` maps `dt → term` and `dd → definition`, so `role="list"` here yields a list owning **zero** listitems — a worse tree than the bug, exactly as the row said. **There is no role to restore at all:** `elementRoles` has no `dl` entry, and `associationlist` / `associationlistitemkey` / `associationlistitemvalue` — ARIA 1.3's answer — are absent from the shipped vocabulary. `role="group"` plus a name would be valid but changes what the element *is* in every browser, including the ones that get it right today. So this genuinely "needs a decision about what the `<dl>` should expose", and the three facts are now written onto `description-list.md` so the next reader does not re-derive them. **Unfixably uncovered here:** jsdom computes the implicit role whatever the stylesheet does, and the affected engine is Safari+VoiceOver. |
| 16 | confirmed · source | ScrollReveal | [ScrollReveal.tsx:146](src/components/animation/ScrollReveal.tsx#L146 "fp:31ab72c9") | med | No IntersectionObserver / no-JS / SSR → element stuck `opacity:0`, content hidden **Opt-out added by `c47b1c8`; the default is unchanged, so this row's sentence still holds.** `animate={false}` drops the hidden class, the observer, the animation class and the delay — the same prop Hero, Timeline, MasonryGrid, Spotlight and Swimlane already shipped, so the primitive was the only member of the family without it. **Still true:** the default renders `opacity: 0`, so a page whose JS never runs still shows nothing. Auto-revealing when `IntersectionObserver` is absent is a one-line change, deliberately not made — it flips a shared default and breaks a sibling's test, which is an owner's call. **TWO OF THREE CLAUSES CLOSED — the row stays open for the third, which is SSR.** The owner authorised the SSOT route and it was taken: the default moved in `ScrollReveal.tsx` and the one sibling test resting on it was repaired, rather than forking a standalone component. **The sibling test was established as fact before anything changed:** `Swimlane.test.tsx`'s *"still hides behind the reveal by default"* rendered with **no observer stub** and passed *only* because jsdom ships no `IntersectionObserver`, so the effect returned early — i.e. it asserted the defect. It went red on the fix (observed), and was repaired by stubbing an observer that observes and reports nothing, which is the state a real browser is in before the element scrolls into view; it now asserts the same thing for the right reason. Two tests in `ScrollReveal.test.tsx` had the identical dependency. Exactly one sibling test broke across the whole suite. **Clause 1, no `IntersectionObserver` — CLOSED:** a feature-detect reveals statically, with no entrance class and no delay, the same shape as the reduced-motion path. Measured in Firefox 146 with `delete window.IntersectionObserver`: `class=""` at `opacity: 1`. **Clause 2, no JS — PARTLY CLOSED:** a new `ScrollReveal.css` adds `@media (scripting: none) { .scroll-reveal-hidden { opacity: 1 } }`, verified by toggling only `javaScriptEnabled` on the same element against an always-visible control. **Phase 1 re-armed it:** moving this package's CSS into `@layer components` put that rule *below* the foundation's unlayered `.scroll-reveal-hidden { opacity: 0 }`, which would have re-opened this clause in full; it now carries `!important`, which is measured by `probe:cascade-layer`'s `scrollreveal-no-js-opacity` row and fenced with its reason in the file. It does **not** cover scripting *enabled* with a bundle that never executes — a hydration error, a blocked script — because the browser reports `scripting: enabled` either way. **Clause 3, SSR — NOT CLOSED.** Server HTML still carries `scroll-reveal-hidden`; the feature-detect is an effect and cannot run on the server. Flipping to an isomorphic layout effect was considered and rejected: it makes the pre-hydration paint show content that then vanishes, which is exactly the flash the docs name as the hidden state's purpose. `animate={false}` remains the only cover. **Reach checked per traps §A:** `MasonryGrid`, `Timeline`, `Spotlight` and `Swimlane` all render the shared primitive and none bypasses it, so all four inherit the two closed clauses. |
| 24 | deferred · owner deferred it 2026-07-26 when the other three API rows were authorised; re-deciding needs the owner, not a re-measurement | Field | [Field.tsx:96](src/components/form/Field.tsx#L96 "fp:7c033c64") | low | No Label↔control association (no `htmlFor`/`id`) → label isn't the accessible name **Assessed and deliberately not attempted.** The correct fix is `Field` publishing a `controlId` that `Label` consumes for `htmlFor` and each control consumes for `id` — which needs `Label.tsx` and the three text controls. The one Field-only route is *actively wrong*: `mergeProps(props, ariaProps)` gives `ariaProps` the win, so injecting `id` there would override a caller's explicit `id` on every wired control at once, and still not associate the Label. `label.md` documents the manual wiring in three places and `field.md` in two, so the docs have to be answered, not reconciled away. |
| 61 | confirmed · source | Avatar | [Avatar.tsx:148](src/components/ui/Avatar.tsx#L148 "fp:d7aba943") | low | `ring-surface-0` hard-codes the backdrop; wrong colour on any non-`surface-0` background **Assessed: no in-package fix exists.** A themeable ring needs a token that does not exist; every in-package option is either another hard-coded surface or a raw arbitrary value, which ETHOS rules out. Needs a token in the CSS package. **LEFT OPEN 2026-07-26 — the conclusion holds, its stated reason does not, and the row is re-scoped.** Tested rather than inherited. **The row's author missed a token: `--C-CANVAS` exists**, is exposed as `bg-canvas`/`ring-canvas`, and is what `base.css` paints `body` with. It does not fix the row — it names the *page* backdrop, so it is wrong on a Card exactly as `surface-0` is — but it disproves the stated reason. `ring-offset-color` is the same problem wearing a different name, and `currentColor` resolves to inherited text colour, which nothing here uses to mean "backdrop". **A new instance the row missed:** `--C-CANVAS` and `--C-SURFACE-0` *diverge* in the dark themes — `oklch(0 0 0)` against `oklch(0.1684 0 0)` in `grimdark` and against `oklch(0.1408 …)` in `tech` — so an avatar sitting directly on the page in either already gets a visibly lighter ring. **The real gap is C3, not D2:** the ring lives on the inner status dot, which `className` cannot reach, so there is no override path at all. That is an API decision for the owner, not a missing token. **2026-07-29 — the row still stands, and the ramp redefinition moved the odds in its favour without closing it.** `@batthewz/response-ui-css@0.13.0` made `--C-SURFACE-0` the *raised sheet* rung — cards, dialogs, menus, input fills — rather than the rung nearest the page. So `ring-surface-0` now guesses right in the common case (an avatar on a card) instead of only on a bare page, which is a real improvement to the defect's frequency. It is not a fix: the ring is still a hard-coded layer, and it is now *wrong on the page canvas in all four themes* rather than in the two dark ones — `--C-CANVAS` and `--C-SURFACE-0` no longer coincide anywhere by design. The earlier note's observation that `--C-CANVAS` names the page and is therefore not the missing token still holds, as does its real conclusion: the ring lives on the inner status dot, `className` cannot reach it, and that is an API decision for the owner. Related: this pass replaced the `offline` dot's `bg-surface-3` with `bg-border-strong` for the same class of reason — a surface rung is never contrast-guaranteed against another surface. |
| 66 | confirmed · source | Skeleton | [Skeleton.tsx:34](src/components/ui/Skeleton.tsx#L34 "fp:08af8b51") | low | `width` defaults to an inline `100%`, so `w-*` utilities silently never apply **Investigated; the row's implied fix does not work.** Moving the default width from the inline style into CSS would **not** restore `w-*`: this package's component CSS is imported *unlayered* from `src/styles.css`, and unlayered author CSS outranks Tailwind's `utilities` layer, so `w-*` loses to `.skeleton { width }` exactly as it loses to the inline default. The real fix is layering this package's component CSS — a `styles.css`-wide change, not a Skeleton one. **Row's implied fix REFUTED by measurement 2026-07-26; the row itself stays open.** The sentence is true and the row's own scepticism about its implied fix was correct — both candidates were measured in Firefox 146 against the real built Tailwind output, served over http. With `w-80` applied: a positive control gives **320px**; today's inline `style.width = 100%` gives **1280px**; the row's implied fix (unlayered `.skeleton { width: 100% }`) also gives **1280px**; and `@layer components { … }` gives **320px**. So moving the default into unlayered CSS changes nothing and only layering works. (A first run produced a false null — `w-32` measured `auto` because Tailwind never generated it for that build — and was re-run with a generated utility after establishing the control.) **Layering only `Skeleton.css` was rejected with a reason:** the unlayered behaviour is not a Skeleton accident but a library-wide contract stated on ~12 doc pages, each with the same escape hatches, so layering one component would make it the sole exception to a documented rule and silently change specificity for existing consumers. The fix is a `styles.css`-wide packaging decision plus a doc sweep — far beyond this row's harm. **Widened by #65's fix (2026-07):** the unlayered base rule now sets `height: 1em` on every variant, so *height* utilities were exactly as inert as width ones, on all four variants — same mechanism, same layering door. `skeleton.md` documents both halves since the reconcile wave (adcde77). **HALF CLOSED BY PHASE 1 — the height half, and the row narrows to `width` alone.** `src/styles.css` now imports every component stylesheet with `layer(components)`, and Tailwind orders that layer below `@layer utilities`, so `.skeleton { height: 1em }` no longer out-ranks anything. `height` has **no default** in `Skeleton.tsx`, and React omits an `undefined` style key, so a Skeleton without the prop emits no inline height at all and `className="h-48"` now applies — the layering door this row named as the only fix is the one that was taken. **`width` is untouched and is why the row stays open:** it defaults to `"100%"` and always ships as an inline `style`, which beats a class in every layer, so `w-*` is still silently inert however this package's CSS is registered. Layering was never going to reach it — the fix is the one the row's title implies, dropping the inline default. Note the two routes do not compose: passing the `height` prop writes an inline style that beats the utility, so a caller must pick one. `skeleton.md` and `Skeleton.examples.tsx` were corrected in the same pass; both had described `h-48` as dead code. |
| 67 | confirmed · source | Skeleton | [Skeleton.css:39](src/components/ui/Skeleton.css#L39 "fp:dfe29f9a") | low | Pulse timing and the reduced-motion `opacity` are literals; no `--MOTION-*` token is read **Half fixed by 4fd9f22.** Pulse timing now reads `calc(var(--MOTION-DURATION-SHIFT, 400ms) * 4)` instead of a frozen 1.5s. **Still open:** the reduced-motion `opacity: 0.7` remains a literal — no opacity token exists in `response-ui-css` or `src/tokens.css`, so tokenising it needs a new token in the CSS package. Cross-package. |
| 182 | confirmed · source | MasonryGrid | [MasonryGrid.tsx:78](src/components/ui/MasonryGrid.tsx#L78 "fp:27b2335d") | low | `animate` defaults to `true`, so SSR ships every item at `opacity: 0` (instance of #16) — logged low, not med like #175, because `animate={false}` is a real opt-out **Inherits #16's two closed clauses; this row's own sentence still stands.** MasonryGrid renders the shared `ScrollReveal` (verified, it does not bypass it), so a missing `IntersectionObserver` and scripting-off now both reveal the grid. **Still true as written:** `animate` defaults to `true`, so **SSR still ships every item at `opacity: 0`** — which is what this row says. No MasonryGrid source change was needed or made. **Re-confirmed by measurement 2026-07-26; no source change made or needed.** `MasonryGrid.Item` renders the shared `ScrollReveal` and does not bypass it, so it inherits #16's two closed clauses. **The SSR clause was measured rather than reasoned about:** `renderToStaticMarkup` emits `<div class="scroll-reveal-hidden masonry-grid__item">`, because `isHidden` is `animate && !reducedMotion && !revealed` and `revealed` flips only in an effect, which never runs on the server. The row's sentence stands exactly as written. The layout-effect route was **not** re-attempted — #16 records why it is wrong, and re-deriving it would have burned the lane. |
| 194 | confirmed · source | Spotlight | [Spotlight.tsx:42](src/components/ui/Spotlight.tsx#L42 "fp:26be6eb5") | low | `animate` defaults to `true`, so every `Spotlight.Content` is server-rendered inside `scroll-reveal-hidden` while the images are not — a page whose JS never runs shows the pictures and none of the copy (instance of #16; `animate={false}` is a real opt-out, so low rather than med) **Inherits #16's two closed clauses; this row's own sentence still stands.** Spotlight renders the shared `ScrollReveal` (verified), so a missing `IntersectionObserver` and scripting-off now both reveal the copy. **Still true as written:** the server-rendered page still ships `Spotlight.Content` inside `scroll-reveal-hidden` while the images are not wrapped, so a page whose bundle never executes shows the pictures and none of the words. No Spotlight source change was needed or made. **Re-confirmed by measurement 2026-07-26; no source change made or needed.** Same route as #182, and this row's sharper sentence reproduced exactly in static markup: `<div class="spotlight-image"><img …/></div>` followed by `<div class="scroll-reveal-hidden"><div class="spotlight-content">…`, because `Spotlight.Image` renders a bare `<div>` with no reveal wrapper. Pictures visible, copy not — still true as written. |
| 196 | confirmed · source | Spotlight | [Spotlight.tsx:128](src/components/ui/Spotlight.tsx#L128 "fp:15e3fffd") | med | The parallax layer is `size-full` inside an `overflow: hidden` wrapper with no overscan, and `Parallax`'s `clamp` is not forwarded, so the drift exposes a blank band the width of the translation **Clamp clause fixed by ee5d181; overscan clause open.** `parallaxClamp` now forwards to `Parallax`. The overscan half is deferred with a reason: `.spotlight-image` has auto height in the common case, so Hero's `inset: -50%` trick would collapse the wrapper to 0px. Bounding the drift needs a height contract for the image wrapper first. **Deferral reason confirmed by browser measurement 2026-07-26, not by re-reasoning.** Firefox 146 at 1280px with an 800×400 image: `.spotlight-image` is **630 × 319px** as shipped (content-sized by the image), **630 × 0px** with Hero's `inset: -50%` applied to the parallax layer — the image disappears — and 300px tall with a positive control giving the wrapper an explicit height, which proves the collapse is the missing height rather than the recipe. `.spotlight-image` declares no height at all, so absolutely positioning its child takes the image out of flow. Bounding the drift needs a height contract (`aspect-ratio` or a height prop) first, which is a Spotlight design decision. Measurement and control recorded on `spotlight.md`. |
| 205 | confirmed · source | ProgressBar | [ProgressBar.tsx:139](src/components/ui/ProgressBar.tsx#L139 "fp:f445cddb") | med | Status conveyed by colour alone (WCAG 1.4.1) — `color="success\|warning\|error"` changes only the fill hue, with no data attribute, `aria-valuetext` or text alternative (instance of the recurring pattern; worse than `Meter` #21, which at least exposes `data-status`) **AT half closed by ec81ed4; visual half open.** Carried on `aria-valuetext` ("96%, Error") rather than a hidden child — `role="progressbar"` makes its children presentational too, and an appended `aria-label` would silently lose to a caller's `aria-labelledby`. `statusLabel` overrides; the caller's own `aria-valuetext` still wins. **Still true:** the fill hue is the only visual channel. **Assessed with measurement and deliberately LEFT OPEN — the sentence still holds, and the reason is now on the record so nobody re-attempts it blind.** Every sibling in this family (#1 Alert, #44 Badge, #104 Toast, #21 Meter) closed its visual half with a glyph. ProgressBar cannot: the `role="progressbar"` element **is** the track — measured in Firefox at 1280px as **4px / 12px / 20px** high for `sm`/`md`/`lg` (4/8/12 below 40rem) with `overflow: hidden` confirmed computed — so a 12–16px glyph is illegible at two of the three sizes and clipped at all of them. ARIA would not have objected (progressbar children are presentational); the glyph simply could not be *seen*. There is no adjacent element either: the root `Omit`s `children`, and `ProgressBar.Label`/`Value` are the bar's **siblings** with no context joining them, as its own docblock states. A texture channel was considered and rejected rather than invented: `variant="striped"` already owns `background-image` on the fill, and three distinguishable textures inside a 4px bar is not a defensible claim — a cue that works at one of three sizes is worse than none. Closing this needs a structural change to what the root element is, which breaks the documented `className`/`.progress-bar` contract. `progress-bar.md` now carries the measurement. |
| 315 | confirmed · source | CalendarBase | [CalendarBase.tsx:637](src/components/ui/CalendarBase.tsx#L637 "fp:17168e29") | med | In-range and preview days expose no ARIA state at all — only `data-in-range`/`data-preview` — so a range's extent is conveyed by a `--C-SURFACE-2` wash measured at 1.08–1.16:1 (WCAG 1.4.1, and below the 3:1 non-text floor of #210) **Narrowed by d08ad08, not closed.** The ARIA half is done: range membership is now in the day's accessible name (", in selected range" / ", in previewed range"), overridable via `labels`. **Still open:** the contrast clause. `d08ad08` moved the in-range wash to `--C-SURFACE-3` to separate it from hover, but nothing re-measured it, so the 1.08–1.16:1 figure in this row stands unrefuted. **Re-measured 2026-07-26 against shipped v0.10.0; verdict unchanged, numbers and door both corrected.** `d08ad08` moved the wash to `--C-SURFACE-3`, which nothing had re-measured; against the calendar body's actual `--C-SURFACE-0` (`Calendar.css:38`) it is **1.24 default / 1.18 events / 1.34 grimdark / 1.20 tech**, up from 1.10/1.08/1.16/1.08. So that change bought about +0.13 against a 3:1 floor — replace this row's stale 1.08–1.16 figures with these. **The door this row implies is wrong: it is NOT cross-package.** No pair of surface tokens in any shipped theme reaches 3:1 — the entire surface ramp spans only 1.24 / 1.18 / 1.34 / 1.20 end to end — so no palette retune can fix this without destroying what the surface ramp is for. The fix is in this package: the range band needs a non-surface channel, e.g. a border or underline in `--C-BORDER-STRONG` (measured 3.23–3.49 on SURFACE-0, adequate) or an accent tint. A Calendar design decision, not a palette one. |
| 384 | confirmed · source | AvatarUpload | [AvatarUpload.tsx:311](src/components/ui/AvatarUpload.tsx#L311 "fp:90366e47") | low | The hover scrim and its glyph hard-code `bg-black/50`, `text-white` and `border-white` — none of which resolve to any contract variable — instead of `--OVERLAY-SCRIM-COLOR`, which the shipped themes set to 0.45/0.7/0.8 and which `Drawer`, `AppShell` and `CommandPalette` all read **HALF FIXED · 78a896a — the scrim closed, the ink deliberately did not.** `bg-black/50` now reads `bg-[var(--OVERLAY-SCRIM-COLOR,…)]`, spelled exactly as `Dialog.tsx` spells it; AvatarUpload was the only file in `src/` containing `bg-black`/`text-white`/`border-white`, enumerated by grep. **Still true:** `text-white` and `border-white` remain, and the ratios say why — measured against the scrim composited over the avatar, with the harness validated by reproducing #415's independently-measured 4.41. `--C-TEXT-INVERSE` is near-black in `tech` (oklch 0.1408) and `grimdark` (0.1684), so it reads **2.35 / 1.52** over a white photo and **1.05 / 1.10** over a black one — it would make the glyph invisible. `--C-TEXT-ON-PRIMARY` stays light but is the wrong token *role* and is chromatic. **There is no "ink on an overlay" token in the contract**, which is the same shape as #61 and lands in `response-ui-css`. **A new measured finding the row did not contain:** adopting the scrim token makes `events` *worse* for the glyph — 3.98 → **2.89**, under WCAG 1.4.11's 3:1 for a non-text cue over a light photo — because that theme's scrim is only 45%. Recorded on the doc page. |
| 397 | confirmed · source | AppShell | [AppShell.css:10](src/components/ui/AppShell.css#L10 "fp:2296129e") | low | **Half closed by `da9f457`; the other half is structurally unfixable here — record the reasoning and do not re-attempt it.** Navbar height is genuinely single-sourced: `--app-shell-navbar-height: 3.5rem` at :10, read by the navbar (:28) and by both sidebar rules (:83 `top`, :84 `calc(100vh - …)`). The breakpoint **cannot** be: `639px` is stated once in JS as `MOBILE_VIEWPORT_QUERY` (AppShell.tsx:47) and once in CSS (:249), and neither copy is removable — a CSS media query cannot read a custom property, this package has no CSS build step, and the stylesheet's block is what keeps the pre-hydration render (where `isMobile` is still `false`) from showing the inline sidebar on a phone. No test can gate the match either: vitest runs `css: false`, which stubs every CSS request including `?raw`. Both files carry the warning in a comment. Closing this needs a build step or a shared constant emitted into CSS — a packaging decision, not a patch. (`:214`'s `calc(100vw - 3.5rem)` is the drawer's peek gutter, not the navbar height — unrelated literal) **Re-verified 2026-07-26 and deliberately not re-attempted, which is the correct outcome for this row.** Navbar height is genuinely single-sourced — the token at `:10` is read by the navbar's `height` and both sidebar rules, and the `calc(100vw - 3.5rem)` elsewhere is the drawer's peek gutter, an unrelated literal, exactly as the row says. **The load-bearing premise was measured rather than asserted:** a page declaring `:root { --bp: 639px }` and `@media (max-width: var(--bp))` never applies the rule at a 500px viewport, while the positive control `@media (max-width: 639px)` on the same page does. A media query cannot read a custom property. (The first harness gave a false negative — the control lost on source order — which is exactly why the control exists.) No CSS build step exists: the Vite config copies `src/**/*.css` verbatim. Both warning comments are present and correct. |
| 415 | confirmed · source | FileUpload | [FileUpload.css:118](src/components/ui/FileUpload.css#L118 "fp:65ee4a97") | med | Wording corrected: the rule does **not** use a paired status background — FileUpload.css:114-119 paints the status *foreground* (`--C-STATUS-SUCCESS`, error twin at :105-111) directly on the dropzone's own `--C-SURFACE-1` fill (:18) at `--BodyText-3`. The pairing the row names is not what the CSS does; the ratio claim may still stand, against a different backdrop. **Narrowed 2026-07-26:** only the *success* half survives — `--C-STATUS-SUCCESS` on the dropzone's `SURFACE-1` at `--BodyText-3` is **3.15:1 default / 3.10:1 events**. The error twin re-measures 4.54–5.44:1 and is fine **Re-measured; the verdict stands and one mechanism was wrong.** The success message never sits on `--C-SURFACE-1`: `.file-upload--success` repaints the dropzone `--C-STATUS-SUCCESS-BG` at the same moment the message appears. Re-measured against that actual backdrop: **3.15:1 default / 3.15:1 events / 6.70:1 grimdark**. Still failing on two of three, so the row holds — with the correct backdrop named. **Re-measured 2026-07-26 against shipped v0.10.0 — the success half CLOSES and the error half INVERTS. Do not read this row as it was written.** The mechanism this row corrected is right and was re-verified at source: `FileUpload.tsx:550-551` applies `file-upload--success`/`--error` under the *same* condition that renders the message (`:658`/`:650`), and `FileUpload.css:50`/`:56` repaint the dropzone, so the message is never on `--C-SURFACE-1`. **Success half — now passes:** `--C-STATUS-SUCCESS` on `--C-STATUS-SUCCESS-BG` at `--BodyText-3` is **4.57 / 4.57 / 6.70 / 13.39**, because the retune darkened the shared green. That is the half this row said was the only survivor, and it is closed. **Error half — this row exonerated it using the very backdrop it had just disproved.** The "error twin re-measures 4.54–5.44:1 and is fine" figures are `--C-STATUS-ERROR` on `--C-SURFACE-1` (independently reproduced: 4.62/4.54/4.76/5.44) — the surface the row proved the message never sits on. Against the backdrop `.file-upload--error` actually paints, `--C-STATUS-ERROR` on `--C-STATUS-ERROR-BG` at `--BodyText-3` is **4.41 default / 4.41 events / 4.59 grimdark / 5.35 tech** — under AA in two themes. `--C-STATUS-ERROR` was untouched by the retune, so this has been true the whole time; the identical error caught for success was left uncorrected on the twin. **Enumerated, because the recipe is not FileUpload's alone:** `bg-status-error-bg` + `text-status-error` at body size is also `Badge.tsx:17`, `Alert.tsx:12` and `Toast.tsx:19`, all reading 4.41 in default and events. Their success twins (`Badge.tsx:15`, `Alert.tsx:10`, `Toast.tsx:17`) now pass at 4.57 and close with the success half. **Door: cross-package.** `--C-STATUS-ERROR` on `--C-STATUS-ERROR-BG` is the contract's own designated ink/fill pair and every component here is using it correctly; the fix is the same lightness-only move the success green received, in `@batthewz/response-ui-css`. |
| 446 | confirmed · source | **response-ui-css** | responsive/text.css:143-183 | med | Every heading ignores the theme's own weight tokens: `responsive/text.css:147,154,161,168,175` hard-code `font-weight: 700` on `h1`–`h5` and `:182` `600` on `h6`, while the same file exposes `--font-weight-bold: var(--Bold-Weight)` at :110 and makes the token responsive at :33-34 (500/600) and :69-70 (600/700). Themes override it further — events 800, tech 600, grimdark 900 — and none of it reaches a heading. d706bf2 fixed the literal weights in the react-components CSS and did not cross the package boundary. Cross-package, so the fix lands in `response-ui-css` |
| 470 | confirmed · source | Rating | [Rating.tsx:131](src/components/ui/Rating.tsx#L131 "fp:d46db0bf") | low | `useRovingFocus` is consumed for `tabIndex`/`setFocusedIndex` only; the hook's own `onKeyDown` — and so its `Home`/`End` and looping — is dead code for this consumer, which now runs one key handler of its own. #91 cites Rating as the hook's exemplar and that citation is now weaker than it reads **Widened after `656a6e0`:** the hook has exactly two consumers — `Rating.tsx` and `ThemeSwitcher.tsx`, enumerated — and **neither** uses `roving.onKeyDown`, the returned `focusedIndex`, or `loop`. ThemeSwitcher rejected the hook's handler for a reason: a radiogroup's tab stop must be the checked radio, and the hook moves focus without selecting. Since `orientation` only feeds that handler, it is inert for every consumer. The hook's key handling is dead code package-wide. **LEFT OPEN by decision 2026-07-26, with the duplication removed and #91's citation made true.** The key handling was **not** deleted and the row's sentence stands. Three things decided it: `useRovingFocus` is a **public export** (barrel, README, AGENTS) at v0.9.0, so its key model is shipped API for composite widgets and "dead" means "no in-package consumer", not "unreachable"; the handler is *correct* code — focus-without-selection is right for a toolbar or menubar and wrong only for the two radiogroups that consume it; and `theme-switcher.md` already documents the split **as an intentional decision**, which traps §L calls a refutation rather than drift. What was done instead is the other half of the question — making the hook do what both consumers actually need: `setFocusedIndex` now moves DOM focus with the tab stop **when the group already holds focus**, so the identical hand-rolled effect is gone from both `Rating` and `ThemeSwitcher` (−22 / −21 lines). Per traps §H the value-and-focus state machine stays in the component; only the mechanical focus move moved. Removing that move reddens **11 tests across three suites**, and the new contract's mirror direction — not stealing focus when the group does not hold it — was untested and now is. **Still open:** `onKeyDown`, `focusedIndex`, `loop` and `orientation` remain unused in-package. Deleting them is a breaking change to a published hook and is an owner's call, not a `low` row's. |


| 478 | confirmed · measured | ProgressBar · Card | [ProgressBar.css:11](src/components/ui/ProgressBar.css#L11 "fp:6473c1fa") | low | Two tokens the contract assigns separately now collide: the empty track paints `--C-SURFACE-1` and a Card is `--C-SURFACE-1` since #4, so a ProgressBar inside a Card has a track at exactly **1.00:1** — the total the bar is measured against cannot be seen at all. It was already near-invisible before (1.02–1.07:1 on `--C-SURFACE-0`, table in `progress-bar.md`), so the collision sharpened a standing problem rather than creating one. `ProgressRing` uses `--C-SURFACE-2` for the equivalent track and is the in-family precedent; `DataTable.tsx:578` uses `bg-surface-1` for the expanded-row cell and carries the same risk, unverified **Confirmed 2026-07-26 at source and by measurement — 1.00:1 exactly, in all four themes, by identity.** `ProgressBar.css:10` is `--C-SURFACE-1` and `Card.tsx:36` is `bg-surface-1`. **The row's unverified clause is now verified, and its mechanism is wrong.** `DataTable.tsx:578` does carry `bg-surface-1`, but it never collides with a Card: `Table.css:13-16` gives `.table` its own opaque `--C-SURFACE-0`, so a Card behind it is never adjacent. The real 1.00:1 neighbour is `Table.css:87-89` `.table-row--striped { background: var(--C-SURFACE-1) }` — an expanded detail row is not itself striped, so it renders flush against a striped parent row, and against the `--C-SURFACE-1` `.table-head` (`Table.css:19-22`) in a one-row table. Same ratio, different neighbour. **The in-family precedent does not actually solve it, which is the finding.** Measured inside a Card: `--C-SURFACE-2` (ProgressRing's choice, `ProgressRing.css:16`) gives **1.04–1.09**, `--C-SURFACE-3` and `--C-BORDER-DEFAULT` both give **1.15–1.26**. No surface token clears 3:1 against `--C-SURFACE-1` in any theme; the widest step available is 1.25. So `--C-SURFACE-2` escapes the exact-identity collision and buys family consistency, and it costs the un-carded case (SURFACE-0 backdrop 1.05 → 1.10) — but a track a user can actually see needs a border, which is a ProgressBar design change of the same shape as #315's. **2026-07-29 — the `--C-SURFACE-1` rung gained a third resident, and the contract now states outright why a fill can never close this row.** `StatCard.css` moved from `--C-SURFACE-0` to `--C-SURFACE-1` to sit on the contract's card rung alongside `Card` (the same correction as #4), so a `ProgressBar` inside a `StatCard` is now the identical 1.00:1 it is inside a `Card` — one more neighbour, same defect, no change to this row's verdict or its recommended fix. StatCard itself is not exposed: it carries `--C-BORDER-DEFAULT`, measured **1.15–1.26** against `--C-SURFACE-1` in this row's own table, so the tile is bounded by an edge rather than by a step. `Card` gained the same border on the same change for the same reason — `AppShell` paints its body `--C-SURFACE-1`, so an un-bordered card was already at 1.00:1 against the shell. The general rule is now in `docs/theme-contract.md` **Surfaces**: the rung records how far a region is nested from `--C-CANVAS` and reverses its lightness direction between light and dark themes, so a boundary must be a border or a shadow and never a neighbouring rung. **2026-07-29 — the 1.00:1 identity collision is RESOLVED at the source; the row is re-scoped to what survives.** `@batthewz/response-ui-css@0.13.0` redefined the surface ramp (raised → recessed, one lightness direction in every theme, `--C-CANVAS` moved off the end of the scale into the middle of it). Under it `ProgressBar`'s track is `--C-SURFACE-3` and `Card`/`StatCard` are `--C-SURFACE-0`, so the track measures **1.13–1.25:1** inside either — no longer invisible by identity, in any theme. The same move fixes the `.table-row--striped` neighbour this row identified: the band is now `--C-SURFACE-2` and the `DataTable` expanded cell `--C-SURFACE-3`. **What does not change is the row's real conclusion:** 1.25:1 is still an order below the 3:1 a non-text boundary owes, so a track a user can actually see still needs a border, and that is still a `ProgressBar` design change. The row stays open on that, not on the collision. Note the previous entry's closing paragraph cites the old contract text ("the rung records how far a region is nested from `--C-CANVAS` and reverses its lightness direction between light and dark themes") — that sentence is no longer in `docs/theme-contract.md` and no longer true. |


| 486 | confirmed · measured | Repeater | [Repeater.tsx:48](src/components/form/Repeater.tsx#L48 "fp:01387169") | low | A **generic wrapper component** over `Repeater` no longer compiles, and its error message is unreadable. #260 narrowed `name` to `ArrayPath<T>`, which is correct and catches real typos — but with `T` a naked type parameter the conditional stays deferred, so `function MyRepeater<T>(…) { return <Repeater<T> name={name} …/> }` fails and TypeScript expands the conditional over three lines to say why. Concrete-`T` call sites are unaffected: all 7 examples, both dev-gallery instances and the whole test file compile unchanged. **Named by the lane that created it rather than left to be discovered**, with the trade stated: the fix is to export a `RepeaterName<T>` alias so a wrapper can annotate its own prop, at the cost of one barrel line and one more public type. `repeater.md` documents the existing escape hatch meanwhile — wrap `useFieldArray`, which the page already recommends for custom controls. **Owner's call**, because it is public API surface, not a defect in the narrowing |

| 488 | confirmed · measured | **library-wide** | library-wide | low | `verify-component-docs.mjs` cannot resolve a token read through an arbitrary-value utility, so such a read can never appear in a token table and the gate silently under-reports. Two shapes measured: `bg-[var(--X,fallback)]` (a `var()` with a fallback) and `pr-[calc(var(--R-SIZE-4)+1rem+var(--R-SIZE-5))]` (a composed expression) — only the bare single-token form `prefix-[var(--TOKEN)]` resolves. It also does not recognise inset utilities such as `right-r4` as utilities at all. **This changed a shipped decision:** #471 shipped a `calc()` gutter that fitted the chevron better, then reverted to a bare rung because the doc row claiming those tokens failed this gate. A gate that cannot see a legitimate token read pushes authors off the contract it exists to protect |

| 489 | confirmed · measured | Input · Field | [Input.tsx:36](src/components/form/Input.tsx#L36 "fp:42a9c101") | low | A wrapping component cannot **add** to `aria-describedby` inside a `Field` — it can only be replaced. `useFieldError` returns `field.describedBy` whenever the field is invalid and `mergeProps(props, ariaProps)` gives that priority, so a caller's or a wrapper's own description id is dropped. Measured as the reason both date pickers' new reject-message id (#330/#338) disappears inside an errored `Field`, and the same shape would bite any future wrapper that wants to describe its own control. The fix is to **compose** the two id lists rather than replace, which is one line in a shared file — deliberately not made by the lane that found it, since `Input.tsx` is shared and mid-wave |


| 493 | confirmed · measured | **library-wide** | library-wide | low | `--C-SURFACE-3` is excluded from both contrast retunes, so anything rendered on the most elevated surface still fails. Measured against the shipped `@batthewz/response-ui-css` **v0.10.1**: `--C-BORDER-FOCUS` on `--C-SURFACE-3` is **2.97 default / 2.87 events / 12.37 tech / 2.74 grimdark**, under the 3:1 WCAG 1.4.11 floor in three of four themes; and `--C-TEXT-MUTED` on the same surface is **4.00 / 4.10 / 4.06 / 3.92**, under AA's 4.5:1 in all four. **The exclusion is deliberate upstream** — v0.10.0 solved SURFACE-0..2 on the inactive-control exemption and said so — so this is a scope question for the CSS package rather than a defect in it, and nothing in this package can fix it. Surfaced 2026-07-26 by the v0.10.1 doc sweep, and now stated on the two pages where a component can realistically sit on SURFACE-3 (`calendar.md`, `breadcrumbs.md`) rather than left for a consumer to discover. Cross-package |

| 494 | confirmed · source | AppShell | [AppShell.tsx:291](src/components/ui/AppShell.tsx#L291 "fp:70abb017") | low | The mobile drawer's `role="dialog"` wrapper hard-codes `aria-label="Main navigation"` with no override path. The inner `<aside>`'s identical label *is* caller-overridable — `aria-label` sits before `{...props}` at :285 — so a non-English page can rename the navigation landmark yet not the dialog that wraps it, and the two then announce in different languages. Hard-coded English with no override is the logged-defect shape (traps §J; #485 family). Surfaced by the 2026-07 docs-reconcile wave |

| 495 | confirmed · source | MediaCard | [MediaCard.css:81](src/components/ui/MediaCard.css#L81 "fp:c722ffcf") | low | The reduced-motion block re-times `box-shadow` and zeroes the transform for `:hover` only; `:focus-within` (:18-21) still applies the full scale/lift — untransitioned, so it lands as a jump — meaning a keyboard user who asked for reduced motion still gets the movement a mouse user is spared. One selector added to the existing block closes it. `media-card.md` describes the split accurately since the reconcile wave (adcde77) |

| 496 | confirmed · source | DateRangePicker | [DateRangePicker.tsx:110](src/components/form/DateRangePicker.tsx#L110 "fp:9d3b8d32") | low | The `startInputId`/`endInputId` docblocks promise "so a `<Label htmlFor>` can name it", but both inputs carry an unconditional `aria-label` (:349, :371), which outranks a native label in accessible-name computation — the id buys click-to-focus, never the name. Either the docblocks narrow to what the id actually does, or the `aria-label` yields when a label is associated, which is an API decision. `date-range-picker.md` states the ARIA-correct behaviour since the reconcile wave (adcde77) |

## Detail blocks

Added by the 2026-07 reconcile for `med` rows with no spoke in `bugs/components/`.

### 446 · response-ui-css — headings ignore the theme's weight tokens (med, cross-package)

`src/responsive/text.css` publishes the weight contract and then declines to use it:

| | value | source |
| - | ----- | ------ |
| `--Bold-Weight` | 600 below 40rem, 700 above; events 800, tech 600, grimdark 900 | `:33-34`, `:69-70`, `themes/*.css:65-68` |
| `--font-weight-bold` | `var(--Bold-Weight)` | `:110` |
| `h1`–`h5` | literal `700` | `:147, :154, :161, :168, :175` |
| `h6` | literal `600` | `:182` |

So no heading anywhere responds to the breakpoint step-up or to a theme's override — `grimdark`
asks for 900 and gets 700, `tech` asks for 600 and gets 700. `d706bf2` fixed exactly this defect
in the react-components component CSS and stopped at the package boundary.

The fix is one substitution per rule and lands in `response-ui-css`, so it needs a version bump
there and a dependency bump in react-components (see the repo root `CLAUDE.md`).
