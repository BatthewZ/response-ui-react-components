# Findings ledger

Open defects in this package's shipped code, one row per finding. **This file is the work
list**: everything in it is believed true at source and unfixed. Closed rows live in
[`ARCHIVE.md`](./ARCHIVE.md); findings about the audit itself — gates, tests, the record —
live in [`AUDIT.md`](./AUDIT.md).

- **What a row *is*** — its kind and its harm — is defined in [`TAXONOMY.md`](./TAXONOMY.md).
  Work order is **harm first**: blocking, then content-loss, then exclusionary, then
  portability, then contract-only.
- **How rows group into fixes** is [`PLAN.md`](./PLAN.md).
- **Verify before you fix.** A row is a claim, not a fact. This file has carried a row marked
  `confirmed · high` whose sentence was false at source, and seven claims that measurement
  disproved. Read the code before you change it; if the row is wrong, say so in the row.
- **Enumerate, never count.** A row saying "three components" that does not name them cannot be
  audited, and one such row hid the same defect in three further components for a whole pass.
- **Same-root siblings are not duplicates.** Rows sharing a cause in different components have
  different anchors and sometimes different fixing commits. Merging them destroys a reference.
- **One instance fixed is not a row closed.** A row naming two components needs both.
- `deferred` means an owner declined it, with the reason in the status. Those rows are open on
  purpose; re-deciding needs the owner, not a re-measurement.

Line numbers in anchors drift as code moves. `node scripts/bugs-ledger.mjs --reanchor` slides
them back when the code merely moved, and prints the rows where the content actually changed —
those may now describe different code, and only a reader can say.

## Findings

| # | Status | Component | File:line | Sev | Summary |
| - | ------ | --------- | --------- | --- | ------- |
| 1 | confirmed · source | Alert | [Alert.tsx:9](src/components/ui/Alert.tsx#L9 "fp:20e5dd41") | med | Severity conveyed by colour alone (WCAG 1.4.1) |
| 4 | confirmed · source | Card | [Card.tsx:36](src/components/ui/Card.tsx#L36 "fp:72c385f6") | med | Uses `bg-surface-0`; contract designates `--C-SURFACE-1` for cards |
| 6 | confirmed · source | StatCard | [StatCard.tsx:131](src/components/ui/StatCard.tsx#L131 "fp:f83396d2") | low | Pre-scroll accessible value is the `from` placeholder (usually `0`) |
| 8 | confirmed · source | DescriptionList | [DescriptionList.tsx:22](src/components/data-display/DescriptionList.tsx#L22 "fp:db024a7f") | low | `display:grid/flex` on `<dl>` can drop list semantics in Safari+VoiceOver **Investigated and deliberately not fixed.** `role="list"` on a `<dl>` is not the remedy it is on `<ul>`/`<ol>`: it would leave `dt`/`dd` as non-`listitem` children, which risks worse semantics than the bug. Needs a decision about what the `<dl>` should expose, not a one-line attribute. |
| 11 | confirmed · source | RequireAuth | [RequireAuth.tsx:50](src/components/guards/RequireAuth.tsx#L50 "fp:be4df10f") | med | Inline ref callback `ref={el=>el?.click()}` re-fires navigation every render |
| 12 | confirmed · source | RequireAuth | [RequireAuth.tsx:25](src/components/guards/RequireAuth.tsx#L25 "fp:b7f7d17c") | low | Docblock "hard navigation via `<a>`" is misleading (delegates to adapter Link) |
| 15 | confirmed · source | AnimatePresence | [AnimatePresence.tsx:49](src/components/animation/AnimatePresence.tsx#L49 "fp:c47161d2") | low | `exitClass` without a CSS animation → mounted forever (no timeout fallback) |
| 16 | confirmed · source | ScrollReveal | [ScrollReveal.tsx:66](src/components/animation/ScrollReveal.tsx#L66 "fp:831257b1") | med | No IntersectionObserver / no-JS / SSR → element stuck `opacity:0`, content hidden |
| 17 | confirmed · source | Stagger | [Stagger.tsx:37](src/components/animation/Stagger.tsx#L37 "fp:2977f4d7") | med | `staggerDelay` is a dead prop, and worse than "confirm the CSS consumes it": Stagger.tsx:37 sets `--stagger-delay` on the **parent**, while (css pkg) `animations/stagger.css:3` re-declares `--stagger-delay: var(--MOTION-STAGGER-DELAY)` on `.stagger-item` itself — the very element that consumes it — so no ancestor value can ever reach it. Any consumer setting the variable in their own CSS hits the same wall |
| 18 | confirmed · source | ViewTransition | [ViewTransition.tsx:48](src/components/animation/ViewTransition.tsx#L48 "fp:facbd63d") | med | `useViewTransition` never awaits `navigate`'s promise → async routers get no transition |
| 19 | confirmed · source | ViewTransition | [ViewTransition.tsx:47](src/components/animation/ViewTransition.tsx#L47 "fp:6622504a") | med | Neither export imports `usePrefersReducedMotion`. Blast radius narrower than stated: (css pkg) `animations/view-transitions.css:22-27` already zeroes `animation-duration` under `prefers-reduced-motion` for `::view-transition-old(root)`/`-new(root)`, so `useViewTransition` page transitions are neutralised; a *named* `<ViewTransition name="x">` group is not |
| 20 | confirmed · source | ViewTransition | [ViewTransition.tsx:1](src/components/animation/ViewTransition.tsx#L1 "fp:ea4ee469") | low | `"use client"` forces the pure component into the client bundle |
| 21 | confirmed · source | Meter | [Meter.tsx:20](src/components/data-display/Meter.tsx#L20 "fp:ac0b2ad3") | med | Warning/critical status conveyed by colour alone (WCAG 1.4.1) |
| 23 | confirmed · source | Field | [Field.tsx:48](src/components/form/Field.tsx#L48 "fp:c6680dc9") | med | Control emits `aria-describedby` to a FieldError id that isn't rendered → dangling ref |
| 24 | confirmed · source | Field | [Field.tsx:69](src/components/form/Field.tsx#L69 "fp:7c033c64") | low | No Label↔control association (no `htmlFor`/`id`) → label isn't the accessible name |
| 26 | confirmed · source | Checkbox | [Checkbox.tsx:16](src/components/form/Checkbox.tsx#L16 "fp:1c2cebaa") | low | Themed border/radius are dead styling on a native checkbox (no `appearance-none`) |
| 29 | confirmed · source | Sparkline | [Sparkline.tsx:69](src/components/data-display/Sparkline.tsx#L69 "fp:77ee9472") | low | Default `aria-label` states value *count*, not data; `role="img"` always on |
| 31 | confirmed · source | Parallax | [Parallax.tsx:63](src/components/animation/Parallax.tsx#L63 "fp:d9913d9a") | low | Residual `translateY` not cleared on reduced-motion toggle / effect teardown |
| 32 | confirmed · source | Parallax | [Parallax.tsx:60](src/components/animation/Parallax.tsx#L60 "fp:e6f86527") | low | No resize/orientationchange recompute → viewport-center offset goes stale |
| 33 | confirmed · source | Parallax | [Parallax.tsx:75](src/components/animation/Parallax.tsx#L75 "fp:86522262") | low | `will-change: transform` for the element's whole life parks a permanent compositor layer |
| 42 | confirmed · source | IconButton | [IconButton.tsx:6](src/components/ui/IconButton.tsx#L6 "fp:cd22428b") | low | Required `aria-label: string` accepts `""`, and `aria-labelledby` cannot substitute for it |
| 43 | confirmed · source | IconButton | [IconButton.tsx:10](src/components/ui/IconButton.tsx#L10 "fp:e4081a8a") | low | `active:scale-95` has no reduced-motion guard (WCAG 2.3.3) |
| 44 | confirmed · source | Badge | [Badge.tsx:9](src/components/ui/Badge.tsx#L9 "fp:8cb73f39") | med | Variant meaning carried by tint alone — no icon, label, role or `aria-*` (WCAG 1.4.1) |
| 45 | confirmed · source | Badge | [Badge.tsx:7](src/components/ui/Badge.tsx#L7 "fp:50399e09") | low | `text-body-3` line-height with no leading reset inflates every chip (~2.25rem on the default scale) (the ~2.25rem figure is unrendered arithmetic; the missing leading reset is the verified part) |
| 48 | confirmed · source | Kbd | [Kbd.tsx:6](src/components/ui/Kbd.tsx#L6 "fp:793f000b") | low | Keycap never reads `--DEFAULT-MONO-FONT`; falls through to Tailwind Preflight's default mono stack |
| 49 | confirmed · source | Kbd | [Kbd.tsx:6](src/components/ui/Kbd.tsx#L6 "fp:793f000b") | low | Package's only `font-medium` (off-contract weight); no leading reset, so cap height is purely `--BodyText-3-line-height` |
| 53 | confirmed · source | Text | [Text.tsx:11](src/components/ui/Text.tsx#L11 "fp:ed6c4a09") | med | `variant` sets size only; heading face/tracking/weight come from `h1`–`h6` element selectors, so `variant`+`as` desyncs look from size |
| 61 | confirmed · source | Avatar | [Avatar.tsx:131](src/components/ui/Avatar.tsx#L131 "fp:d7aba943") | low | `ring-surface-0` hard-codes the backdrop; wrong colour on any non-`surface-0` background **Assessed: no in-package fix exists.** A themeable ring needs a token that does not exist; every in-package option is either another hard-coded surface or a raw arbitrary value, which ETHOS rules out. Needs a token in the CSS package. |
| 66 | confirmed · source | Skeleton | [Skeleton.tsx:34](src/components/ui/Skeleton.tsx#L34 "fp:08af8b51") | low | `width` defaults to an inline `100%`, so `w-*` utilities silently never apply **Investigated; the row's implied fix does not work.** Moving the default width from the inline style into CSS would **not** restore `w-*`: this package's component CSS is imported *unlayered* from `src/styles.css`, and unlayered author CSS outranks Tailwind's `utilities` layer, so `w-*` loses to `.skeleton { width }` exactly as it loses to the inline default. The real fix is layering this package's component CSS — a `styles.css`-wide change, not a Skeleton one. |
| 67 | confirmed · source | Skeleton | [Skeleton.css:38](src/components/ui/Skeleton.css#L38 "fp:dfe29f9a") | low | Pulse timing and the reduced-motion `opacity` are literals; no `--MOTION-*` token is read **Half fixed by 4fd9f22.** Pulse timing now reads `calc(var(--MOTION-DURATION-SHIFT, 400ms) * 4)` instead of a frozen 1.5s. **Still open:** the reduced-motion `opacity: 0.7` remains a literal — no opacity token exists in `response-ui-css` or `src/tokens.css`, so tokenising it needs a new token in the CSS package. Cross-package. |
| 75 | confirmed · source | Radio | [Radio.tsx:12](src/components/form/Radio.tsx#L12 "fp:5df62807") | med | Never consumes `useFieldError`, so a Radio in an invalid `Field` gets no `aria-invalid`/`aria-describedby` |
| 76 | confirmed · source | Checkbox | [Checkbox.tsx:12](src/components/form/Checkbox.tsx#L12 "fp:5df62807") | med | Same as #75 — Radio and Checkbox are the form module's only two unwired controls |
| 80 | unaudited · corroborated · caveat | Slider | [Slider.css:56](src/components/form/Slider.css#L56 "fp:b2747c00") | med | `::-moz-range-thumb` lacks `box-sizing: border-box`, so the handle is 24px in Firefox vs 20px in Chromium (same in RangeSlider). **Left open deliberately:** source facts verified (no `box-sizing` on either thumb pseudo-element, both `1.25rem` + `2px`, and Preflight's `*, ::before, ::after` reaches neither), but the claim is a UA-default divergence that needs a real Firefox *and* Chromium render — jsdom applies no stylesheets |
| 88 | unaudited · spot-checked · caveat | RangeSlider | [RangeSlider.css:111](src/components/form/RangeSlider.css#L111 "fp:64938929") | low | Dead `::-moz-range-track` rule and a false comment ("Firefox paints a default track"). **Left open deliberately:** the rule is at RangeSlider.css:117-119 and the input is already `background: transparent; appearance: none` at :44-54, but whether Firefox's UA still paints a track box under `appearance:none` — which decides whether the rule is dead or load-bearing — needs a Firefox render |
| 91 | confirmed · source | ThemeSwitcher | [ThemeSwitcher.tsx:26](src/components/ui/ThemeSwitcher.tsx#L26 "fp:43ca0846") | med | `role="radiogroup"`/`role="radio"` with no roving focus — no `tabIndex`, no `onKeyDown`, though `useRovingFocus` exists and `Rating` uses it |
| 92 | confirmed · source | ThemeSwitcher | [ThemeSwitcher.tsx:20](src/components/ui/ThemeSwitcher.tsx#L20 "fp:dc192c67") | med | Calls `useTheme()` with no options, so themes registered via `useTheme({themes})` are invisible and mis-reported |
| 93 | confirmed · source | ThemeSwitcher | [ThemeSwitcher.tsx:9](src/components/ui/ThemeSwitcher.tsx#L9 "fp:f6351b0a") | med | `LABELS` is module-private English and `children` is omitted — option text is unreachable in any locale |
| 104 | confirmed · source | Toast | [Toast.tsx:16](src/components/ui/Toast.tsx#L16 "fp:7b627bdd") | med | Variant severity is colour-only — no icon, label or `sr-only` word (fifth instance of the pattern) |
| 128 | confirmed · source | Popover · HoverCard · menu-internals | [Popover.tsx:185](src/components/ui/Popover.tsx#L185 "fp:d793efcd") | low | The 150 ms fade is an inline literal in the `.tsx` — no `--MOTION-*` token and no reduced-motion guard **Assessed, not taken.** The fix wants a shared "read `--MOTION-*` at render" hook used by Popover, HoverCard and menu-internals; adding a module under `src/hooks/` was judged not worth the collision risk mid-sweep. Contract-only, so it waits. |
| 139 | confirmed · source | Breadcrumbs | [Breadcrumbs.tsx:77](src/components/ui/Breadcrumbs.tsx#L77 "fp:69416403") | low | Ellipsis expansion is one-way and never resets, so one instance across route changes stays expanded forever |
| 146 | confirmed · source | Breadcrumbs | [Breadcrumbs.tsx:93](src/components/ui/Breadcrumbs.tsx#L93 "fp:47465c9a") | low | The root interleaves its own separator around a caller-rendered `Breadcrumbs.Separator`, so the exported sub-part has no correct direct use |
| 147 | confirmed · source | Stepper | [Stepper.css:129](src/components/ui/Stepper.css#L129 "fp:e57f06bd") | low | Active and upcoming steps differ by tint alone — same ring, numeral and weight; `aria-current="step"` covers AT only |
| 152 | confirmed · source | CodeBlock | [CodeBlock.tsx:60](src/components/ui/CodeBlock.tsx#L60 "fp:284cb80e") | low | CopyButton is handed only `value` and a class, so `copiedLabel`, `timeout` and a per-block `aria-label` are unreachable — every block's button is named "Copy" |
| 156 | confirmed · source | EmptyState | [EmptyState.css:32](src/components/ui/EmptyState.css#L32 "fp:7bc98d0c") | med | The icon slot scales via `font-size` only and sets no `width`/`height`, so every default `lucide-react` icon renders identically at `sm`, `md` and `lg` |
| 158 | confirmed · source | EmptyState | [EmptyState.css:47](src/components/ui/EmptyState.css#L47 "fp:c245c5f7") | low | `[data-size="md"]` and `[data-size="lg"]` both set the icon `font-size: var(--H4)`, so `lg` never enlarges the icon while padding, gap and title all step up |
| 159 | confirmed · source | EmptyState | [EmptyState.tsx:68](src/components/ui/EmptyState.tsx#L68 "fp:7158411b") | low | `EmptyStateTitle` always renders a `<p>` with no `as` prop and no heading role, so an empty state replacing a page's main content contributes nothing to the outline |
| 161 | confirmed · source | Hero | [Hero.tsx:127](src/components/ui/Hero.tsx#L127 "fp:445bd1ff") | med | `Hero.Content animate` composes `ScrollReveal > Stagger`, but the entrance class lands on the ScrollReveal ancestor and `.stagger-item` has no `animation-name` — the stagger can never fire, and the wrappers only add DOM depth **Confirmed at source, fix is out of scope.** `stagger.css` sets `animation-delay` and `animation-fill-mode` on `.stagger-item` with **no `animation-name`**, while the entrance class lands on the `ScrollReveal` ancestor. The fix belongs in `Stagger.tsx` or the CSS package's `stagger.css`. The only Hero-local option — replacing `ScrollReveal > Stagger` with per-child `ScrollReveal`s — changes the reveal trigger from block-level to per-item, which is a behaviour change, not a fix. |
| 173 | confirmed · source | Swimlane | [Swimlane.css:33](src/components/ui/Swimlane.css#L33 "fp:9827162e") | med | The "View all" link inks `--C-ACCENT` at `--BodyText-2` with `text-decoration: none`, failing AA on every surface in `events` (2.72–2.52:1) and `grimdark` (2.96–2.55:1), where `:hover` lowers it further **Re-measured and confirmed; the fix is out of this package.** `docs/components/swimlane.md` already carries the measurement and prescribes retinting `--C-ACCENT`, which lands in `@batthewz/response-ui-css` — the palette retune the owner has declined twice. The only in-package alternative is inking the CTA in `--C-TEXT-PRIMARY`, which removes brand accent from a call to action: a visible design decision, not a mechanical fix. |
| 182 | confirmed · source | MasonryGrid | [MasonryGrid.tsx:78](src/components/ui/MasonryGrid.tsx#L78 "fp:f7066ac5") | low | `animate` defaults to `true`, so SSR ships every item at `opacity: 0` (instance of #16) — logged low, not med like #175, because `animate={false}` is a real opt-out |
| 194 | confirmed · source | Spotlight | [Spotlight.tsx:42](src/components/ui/Spotlight.tsx#L42 "fp:26be6eb5") | low | `animate` defaults to `true`, so every `Spotlight.Content` is server-rendered inside `scroll-reveal-hidden` while the images are not — a page whose JS never runs shows the pictures and none of the copy (instance of #16; `animate={false}` is a real opt-out, so low rather than med) |
| 196 | confirmed · source | Spotlight | [Spotlight.tsx:115](src/components/ui/Spotlight.tsx#L115 "fp:12a6ae80") | med | The parallax layer is `size-full` inside an `overflow: hidden` wrapper with no overscan, and `Parallax`'s `clamp` is not forwarded, so the drift exposes a blank band the width of the translation **Clamp clause fixed by ee5d181; overscan clause open.** `parallaxClamp` now forwards to `Parallax`. The overscan half is deferred with a reason: `.spotlight-image` has auto height in the common case, so Hero's `inset: -50%` trick would collapse the wrapper to 0px. Bounding the drift needs a height contract for the image wrapper first. |
| 203 | confirmed · source | ProgressBar | [ProgressBar.tsx:99](src/components/ui/ProgressBar.tsx#L99 "fp:b400f8eb") | med | `ProgressBar.Label` emits no `id` and the root sets no `aria-labelledby`, so a bar sitting next to a `Label` still has no accessible name — the sub-part implies a wiring it never performs. Structurally unfixable from the call site: the root type is `Omit<…,"children">`, so Label and Value can only ever be siblings **Assessed: the fix is an API change, not a defect fix.** `ProgressBar.Label` and the root are siblings (the root is `Omit<…,"children">`), so no context can join them. Closing this needs either a required accessible name the way `Meter` does, or a `label` prop on the root — both breaking. |
| 205 | confirmed · source | ProgressBar | [ProgressBar.tsx:83](src/components/ui/ProgressBar.tsx#L83 "fp:f445cddb") | med | Status conveyed by colour alone (WCAG 1.4.1) — `color="success\|warning\|error"` changes only the fill hue, with no data attribute, `aria-valuetext` or text alternative (instance of the recurring pattern; worse than `Meter` #21, which at least exposes `data-status`) |
| 241 | confirmed · source | **library-wide** | [Input.tsx:27](src/components/form/Input.tsx#L27 "fp:42c0a863") | med | Form-control boundary `--C-BORDER-STRONG` on the `--C-SURFACE-0` fill measures **1.41–1.79:1** in all four themes, under the WCAG 1.4.11 3:1 floor. Affects Input, Textarea, Select, NumberInput, SearchInput, TagInput, OTPInput (pattern of #51) — the ratio is #51's token value, not this component's usage: the contract names the pairing, never a number. Component list verified: Input.tsx:22, Textarea.tsx:22, Select.tsx:22, OTPInput.tsx:214, TagInput.tsx:180. **Enumeration corrected 2026-07-26:** `.combobox-input` and `.multiselect-control` carry the identical `SURFACE-0` + `BORDER-STRONG` recipe and were missing from the list above. Survives the rubric: the controls paint `bg-surface-0` on a `SURFACE-0` page, so the 1px hairline is the sole rendered boundary |
| 252 | confirmed · source | TagInput | [TagInput.tsx:271](src/components/form/TagInput.tsx#L271 "fp:3571792c") | med | Adding or removing a tag is never announced and the chips carry no list semantics, so screen-reader users get no confirmation for Enter, Backspace, paste or the remove button **Blocked on the same decision as #262:** announcing add/remove needs an overridable announcement-string API. Deliberately not solved twice in two components with two different string conventions. |
| 260 | confirmed · source | Repeater | [Repeater.tsx:39](src/components/form/Repeater.tsx#L39 "fp:0c10c04c") | low | `name: string` and `defaultItem: () => unknown` are untyped against the form's values, so a mistyped path compiles and silently writes a second array into the submitted values **Contract-only; the fix needs a new path-type utility** reaching into `useFieldArray`, which is a larger change than the row's harm justifies. Typed `name`/`defaultItem` paths, no runtime consequence. |
| 262 | confirmed · source | Repeater | [Repeater.tsx:134](src/components/form/Repeater.tsx#L134 "fp:6a9b9fcd") | low | Rows are plain `<div>`s with no list/group semantics and no live region, so adding or removing a row is never announced **Half fixed by b742101, half blocked on a decision.** Rows are `role="list"`/`role="listitem"` now, tested. **Still open:** there is no live region, because announcing add/remove needs an announcement-string API — the same unresolved decision #252 names. Both wait on one choice, not two investigations. |
| 287 | confirmed · source | ColorPicker | [ColorPicker.tsx:275](src/components/form/ColorPicker.tsx#L275 "fp:cbb4595e") | med | The saturation/brightness square is `role="slider"` with no `aria-valuenow`, `aria-valuemin` or `aria-valuemax`, and models two axes as one slider **Assessed; the honest fix is a redesign, not an attribute.** The sat/brightness square models two axes as one `role="slider"`. Adding `aria-valuenow` would not stop that — the correct shape is React-Aria's ColorArea (a `role="group"` holding two visually-hidden `<input type="range">`, thumb presentational), which rewrites the panel's keyboard model. Owner decision. |
| 293 | confirmed · source | ColorPicker | [ColorPicker.css:9](src/components/form/ColorPicker.css#L9 "fp:094e9b49") | low | **Narrowed, not closed — same shape as #284.** `c3f244b` measured `.colorpicker-trigger:focus-visible` (:23-26) and `.colorpicker-hex:focus-visible` (:194-196) at parity with `Input.tsx` and removed the one real divergence (the hue thumb's restated `rgb(0 0 0 / 0.35)` hairline). Still true: both are hand-written CSS rather than the shared `src/util/focus.ts` constants, so the single fix still will not reach ColorPicker. Needs `.tsx` work, not done **Same shape as #284, not done.** ColorPicker still hand-writes its focus CSS instead of consuming `src/util/focus.ts`; #284 did exactly this move for Combobox and is the worked example. Left because it is contract-only and the tree was busy. |
| 315 | confirmed · source | CalendarBase | [CalendarBase.tsx:557](src/components/ui/CalendarBase.tsx#L557 "fp:cc4e35e9") | med | In-range and preview days expose no ARIA state at all — only `data-in-range`/`data-preview` — so a range's extent is conveyed by a `--C-SURFACE-2` wash measured at 1.08–1.16:1 (WCAG 1.4.1, and below the 3:1 non-text floor of #210) **Narrowed by d08ad08, not closed.** The ARIA half is done: range membership is now in the day's accessible name (", in selected range" / ", in previewed range"), overridable via `labels`. **Still open:** the contrast clause. `d08ad08` moved the in-range wash to `--C-SURFACE-3` to separate it from hover, but nothing re-measured it, so the 1.08–1.16:1 figure in this row stands unrefuted. |
| 319 | confirmed · source | Calendar | [Calendar.css:211](src/components/ui/Calendar.css#L211 "fp:0fe61c7d") | med | The selected day inks the contract's own intended pair `--C-TEXT-ON-ACCENT` on `--C-ACCENT` and still fails AA: measured **2.80:1** in `events` and **3.81:1** in `grimdark` (5.17 default, 14.84 `tech`) **Re-verified true, and the fix is out of this package.** `Calendar.css:211` already inks the contract's own intended pair, so there is nothing to correct here: the measured failure is in `@batthewz/response-ui-css`'s palette (`src/themes/*.css`). Fixing it in this package means abandoning the token contract. Cross-package door — needs the owner. |
| 330 | confirmed · source | DatePicker | [DatePicker.tsx:144](src/components/form/DatePicker.tsx#L144 "fp:eafe7cf9") | low | Unparseable text, and dates rejected by `isDateDisabled`, revert to the previous value with no message and no `aria-invalid` (measured `null`) — the failure is completely silent **Narrowed by d08ad08, not closed.** `aria-invalid` is now set — `DatePicker` tracks `rejected` and feeds `error || rejected`. **Still open:** the failure is still messageless, which is the half a user actually reads. |
| 338 | confirmed · source | DateRangePicker | [DateRangePicker.tsx:191](src/components/form/DateRangePicker.tsx#L191 "fp:697103e3") | low | A `formatOptions` the parser cannot read back (e.g. `{ month: "2-digit", day: "2-digit" }` → `06/10`) leaves the committed year permanently off screen: a complete typed date still commits, but any edit of the *displayed* text is discarded with no signal **Partly mitigated by d08ad08:** the `Resolution` change makes a refused date *revert* rather than clear. Still open — the range picker got no `rejected`/`aria-invalid` treatment and no message channel, so the discard is still silent. |
| 342 | confirmed · source | Timeline | [Timeline.tsx:81](src/components/ui/Timeline.tsx#L81 "fp:f5284c03") | low | The card's side is CSS `:nth-child` but its entrance direction is the React index, so a fragment child desynchronises them: measured `Item, <>Item Item</>, Item` emits `fade-right · fade-left · fade-left · fade-right` against a `left · right · left · right` layout |
| 343 | confirmed · source | Timeline | [Timeline.tsx:89](src/components/ui/Timeline.tsx#L89 "fp:d024b057") | low | Every item title is a hard-coded `<h3>` and `title` is typed `string`, so neither the level nor the element can be changed (`Swimlane` at least types its `<h2>` title as `ReactNode`) |
| 344 | confirmed · source | Timeline | [Timeline.css:46](src/components/ui/Timeline.css#L46 "fp:9b39224d") | low | Below 40rem the node's left offset is a `calc()` over `--_timeline-dot-size`, so a custom `icon` of any other size sits off-centre on the rail by half the difference; at 40rem+ the `50%`/`translateX(-50%)` rule is size-agnostic. Read off the cascade, not rendered **Verified from the cascade, not fixed.** The correct shape is ActivityFeed's per-item `::before` suppressed on `:last-child`, but no test here can read CSS and the agent could not render, so shipping it would be an unverifiable visual change. Same for #345. |
| 345 | confirmed · source | Timeline | [Timeline.css:17](src/components/ui/Timeline.css#L17 "fp:304e2eff") | low | `.timeline::before` is pinned `top: 0; bottom: 0` on the root while `.timeline-node` sits at `top: 0` of its item, so the rail hangs below the final dot by the full height of the last card. Read off the cascade, not rendered **Verified from the cascade, not fixed** — see #344; one fix covers both and neither is verifiable in this environment. |
| 351 | confirmed · source | Table | [Table.css:79](src/components/ui/Table.css#L79 "fp:6a482402") | med | `selected` is an 8% accent wash and nothing else, with no `aria-selected` and no data attribute. **Numbers re-scoped:** measured 1.07 default / 1.05 events / 1.16 grimdark but **2.11 in `tech`**, not the ≤1.13 the row claimed — tech's accent is a near-pure green over a near-black surface. **Scope widened:** `aria-selected` appears zero times in Table.tsx, DataTable.tsx *and* VirtualizedDataTable.tsx — selection is style-only across the whole family |
| 352 | confirmed · source | Table | [Table.css:119](src/components/ui/Table.css#L119 "fp:8b810590") | med | `stickyHeader` is inert by default: `.table-wrapper` is the header's scrollport (`overflow-x:auto` forces `overflow-y:auto`) but is content-height, so `<thead>` never pins **Verified true, no CSS-only fix exists.** `.table-wrapper` sets `overflow-x:auto`, which computes `overflow-y:auto`, making the wrapper the header's scrollport — and it is content-height. Sticky needs a bounded scrollport. `Table` callers can pass `style={{maxHeight}}` (documented); see #361 for the `DataTable` half. |
| 353 | confirmed · source | Table | [Table.tsx:219](src/components/ui/Table.tsx#L219 "fp:001cbc5e") | med | A sortable `<th>` gets `tabIndex=0` + Enter/Space but keeps `columnheader` with no `role="button"` and no inner `<button>`, so nothing announces it as activatable **Investigated and deliberately not fixed.** The APG remedy is `<th aria-sort><button>`, which moves the tab stop and the handlers off the `<th>` — and `docs/components/table.md` documents the current composition contract (the caller's `onClick`/`onKeyDown` run first, `preventDefault()` opts out), which a nested button breaks by bubbling order. Eight existing tests encode that contract. Owner decision. |
| 361 | confirmed · source | DataTable | [DataTable.tsx:95](src/components/ui/DataTable.tsx#L95 "fp:bc4ebc90") | med | Both cited clauses hold (the wrapper is the scrollport per Table.css:6-10 + 115-119; DataTableProps exposes no className/style/ref/rest). **"Can never take effect" overreaches:** as a scroll container the wrapper's flex/grid automatic min-size is 0, so a height-bounded flex or grid parent — or CSS on the public `.table-wrapper` class — does make the header pin. Default block-flow usage fails as claimed. docs/components/data-table.md:444-448 ships the stronger version and inherits the overreach **Verified true; closing it is an API addition, not a fix.** `DataTable` exposes no `className`/`style`/rest, so a caller cannot bound the scrollport at all. The fix is a new public prop — the same class of addition #463 was declined for, so it needs the owner rather than an agent. |
| 376 | confirmed · source | VirtualizedDataTable | [VirtualizedDataTable.tsx:349](src/components/ui/VirtualizedDataTable.tsx#L349 "fp:7707b84e") | low | `rowHeight` is written as a `<tr>` `height` — a CSS minimum — with no truncation, `overflow: hidden` or dev warning, so content taller than the value desyncs the rows from the spacer arithmetic. Read off the cascade, not rendered |
| 377 | confirmed · source | VirtualizedDataTable | [VirtualizedDataTable.tsx:251](src/components/ui/VirtualizedDataTable.tsx#L251 "fp:870667b7") | low | `ColumnDef.width` reaches only the `<th>` and the table has no `table-layout: fixed`, so column widths are negotiated from the mounted window and can re-measure mid-scroll. Read off the cascade, not rendered |
| 383 | confirmed · source | AvatarUpload | [AvatarUpload.tsx:122](src/components/ui/AvatarUpload.tsx#L122 "fp:9d26792e") | med | The `TResult` type parameter on `AvatarUploadProps` is erased by `forwardRef<HTMLDivElement, AvatarUploadProps>`, so `onUploadComplete` can never see fields beyond `{ url }` |
| 384 | confirmed · source | AvatarUpload | [AvatarUpload.tsx:238](src/components/ui/AvatarUpload.tsx#L238 "fp:5a099c2f") | low | The hover scrim and its glyph hard-code `bg-black/50`, `text-white` and `border-white` — none of which resolve to any contract variable — instead of `--OVERLAY-SCRIM-COLOR`, which the shipped themes set to 0.45/0.7/0.8 and which `Drawer`, `AppShell` and `CommandPalette` all read |
| 386 | confirmed · source | AvatarUpload | [AvatarUpload.tsx:262](src/components/ui/AvatarUpload.tsx#L262 "fp:9f89687d") | low | The error tooltip is `whitespace-nowrap`, absolutely positioned `-bottom-8` and centred, and clears only on the next file selection — a long `accept` list overflows the container in both directions with no dismiss or timeout |
| 395 | confirmed · source | AppShell | [AppShell.tsx:309](src/components/ui/AppShell.tsx#L309 "fp:874388b3") | low | `AppShell.SidebarSection` renders its `title` as a `<div>`, not a heading, so sidebar groups are unreachable by heading navigation; the collapsed rail then hides those titles outright |
| 397 | confirmed · source | AppShell | [AppShell.css:10](src/components/ui/AppShell.css#L10 "fp:2296129e") | low | **Half closed by `da9f457`; the other half is structurally unfixable here — record the reasoning and do not re-attempt it.** Navbar height is genuinely single-sourced: `--app-shell-navbar-height: 3.5rem` at :10, read by the navbar (:28) and by both sidebar rules (:83 `top`, :84 `calc(100vh - …)`). The breakpoint **cannot** be: `639px` is stated once in JS as `MOBILE_VIEWPORT_QUERY` (AppShell.tsx:47) and once in CSS (:249), and neither copy is removable — a CSS media query cannot read a custom property, this package has no CSS build step, and the stylesheet's block is what keeps the pre-hydration render (where `isMobile` is still `false`) from showing the inline sidebar on a phone. No test can gate the match either: vitest runs `css: false`, which stubs every CSS request including `?raw`. Both files carry the warning in a comment. Closing this needs a build step or a shared constant emitted into CSS — a packaging decision, not a patch. (`:214`'s `calc(100vw - 3.5rem)` is the drawer's peek gutter, not the navbar height — unrelated literal) |
| 415 | confirmed · source | FileUpload | [FileUpload.css:118](src/components/ui/FileUpload.css#L118 "fp:65ee4a97") | med | Wording corrected: the rule does **not** use a paired status background — FileUpload.css:114-119 paints the status *foreground* (`--C-STATUS-SUCCESS`, error twin at :105-111) directly on the dropzone's own `--C-SURFACE-1` fill (:18) at `--BodyText-3`. The pairing the row names is not what the CSS does; the ratio claim may still stand, against a different backdrop. **Narrowed 2026-07-26:** only the *success* half survives — `--C-STATUS-SUCCESS` on the dropzone's `SURFACE-1` at `--BodyText-3` is **3.15:1 default / 3.10:1 events**. The error twin re-measures 4.54–5.44:1 and is fine **Re-measured; the verdict stands and one mechanism was wrong.** The success message never sits on `--C-SURFACE-1`: `.file-upload--success` repaints the dropzone `--C-STATUS-SUCCESS-BG` at the same moment the message appears. Re-measured against that actual backdrop: **3.15:1 default / 3.15:1 events / 6.70:1 grimdark**. Still failing on two of three, so the row holds — with the correct backdrop named. |
| 416 | confirmed · source | FileUpload | [FileUpload.tsx:389](src/components/ui/FileUpload.tsx#L389 "fp:108501f8") | low | Object URLs are created as a side effect inside `useMemo` during render, so StrictMode's double-render leaks one URL per media file per mount permanently (measured), and an inline `files={[file]}` mints/revokes a fresh URL on every parent re-render |
| 420 | confirmed · source | FileUpload | [FileUpload.tsx:633](src/components/ui/FileUpload.tsx#L633 "fp:2fd4d09d") | low | All built-in copy is hard-coded English with no prop to reach it — "Drag & drop or", "browse", "Uploading...", "Replace", "Clear all", `Remove ${file.name}` and `aria-label="Upload file"` (only the last is overridable, via rest props) (hard-coded-English pattern) |
| 440 | confirmed · source | FieldError | [FieldError.tsx:23](src/components/form/FieldError.tsx#L23 "fp:82987a8e") | med | An explicit `id` on `FieldError` overrides the context `errorId` while every bound control still points `aria-describedby` at `field.errorId`, so naming your own error element produces a dangling IDREF on the control |
| 446 | confirmed · source | **response-ui-css** | responsive/text.css:143-183 | med | Every heading ignores the theme's own weight tokens: `responsive/text.css:147,154,161,168,175` hard-code `font-weight: 700` on `h1`–`h5` and `:182` `600` on `h6`, while the same file exposes `--font-weight-bold: var(--Bold-Weight)` at :110 and makes the token responsive at :33-34 (500/600) and :69-70 (600/700). Themes override it further — events 800, tech 600, grimdark 900 — and none of it reaches a heading. d706bf2 fixed the literal weights in the react-components CSS and did not cross the package boundary. Cross-package, so the fix lands in `response-ui-css` |
| 463 | confirmed · source | DataTable | [DataTable.tsx:87](src/components/ui/DataTable.tsx#L87 "fp:4e6f7108") | low | API asymmetry, deliberately not added: `sort` has a `defaultSort` (:51) but `page` (:80) has no `defaultPage`, so deep-linking to a page — "open this table on page 3, then let it manage itself" — requires taking full control of `page` + `onPageChange` and re-implementing the paging the component already does. `7f651a6` migrated paging onto `useControllableState`, which is what would make `defaultPage` a one-line addition; it was left out because it is a public API addition, not a fix |
| 465 | confirmed · measured | FileUpload | [FileUpload.css:94](src/components/ui/FileUpload.css#L94 "fp:d8c521e6") | med | The dropzone's call to action — the word "browse" in `.file-upload__text-emphasis` — inks `--C-ACCENT` on the `--C-SURFACE-1` fill the component paints: **2.63:1 `events` / 2.77:1 `grimdark`**, under AA for body text. Surfaced 2026-07-26 by the contrast re-scope, not by any earlier pass; the same category-1 defect as #173 and unlogged until now. Not closed by the `--C-TEXT-MUTED` retune — this is accent ink, a different token **Reproduced independently** from the shipped OKLCH token values: `--C-ACCENT` on `--C-SURFACE-1` = **2.63:1 events / 2.77:1 grimdark**. Same family and same door as #173 and #319 — a palette value owned by the CSS package. |
| 466 | confirmed · measured | Pagination | [Pagination.css:47](src/components/ui/Pagination.css#L47 "fp:ff3688c7") | med | The current page chip inks `--C-TEXT-ON-ACCENT` on `--C-ACCENT` — **2.80:1 `events` / 3.81:1 `grimdark`**, the same failing pair as #319. Surfaced 2026-07-26 by the contrast re-scope. `#133`'s fix was correct as far as it went (it escaped a 1.00:1 pairing) but landed on a pairing that still fails AA in two themes, so the row it closed should not be read as clearing this surface **Out of this package.** `--C-TEXT-ON-ACCENT` on `--C-ACCENT` is the contract-designated pairing, so the component is doing the right thing; the ratio is a palette value owned by `@batthewz/response-ui-css`. Same family as #319 — fixing it here means abandoning the named pairing. |
| 467 | confirmed · source | Swimlane | [Swimlane.tsx:66](src/components/ui/Swimlane.tsx#L66 "fp:4dda1c95") | med | `docs/components/swimlane.md` contradicts the component and the a11y advice built on it is wrong. The prop table (`:35`) says rest props are "**typed but never reach the DOM**" and `:249` repeats it, then concludes the lane "cannot be named" because `aria-label`/`aria-labelledby` are "among the props that never reach the DOM". They do reach it: `{...rest}` is spread onto the `<section>` at `Swimlane.tsx:66` and `:79`, with a passing test. `docs/` ships to npm, so this tells consumers they cannot do something they can |
| 468 | confirmed · source | menu-internals | [menu-internals.tsx:160](src/components/ui/menu-internals.tsx#L160 "fp:df916459") | med | `useTypeahead`'s reference handler consumes printable characters, so typing into a `<textarea>`/`<input>` inside a `ContextMenu.Trigger` is swallowed by menu typeahead. Identical shape to #125, which fixed only the Arrow/Home/End half — that fix skips caret keys aimed at a text control, and the same exemption was never applied to the typeahead handler |
| 469 | confirmed · source | Popover · menu-internals | [Popover.tsx:80](src/components/ui/Popover.tsx#L80 "fp:a6107544") | low | Both mint a `useId()` beside the id `useRole` actually puts on the element. Harmless today and measured so under #127 — both sides resolve to `floatingId` — but it is two sources for one id, and #127 is the worked example of what happens when a reader assumes the local one wins. `HoverCard` now reads `context.floatingId` directly and is the pattern to follow |
| 470 | confirmed · source | Rating | [Rating.tsx:108](src/components/ui/Rating.tsx#L108 "fp:d46db0bf") | low | `useRovingFocus` is consumed for `tabIndex`/`setFocusedIndex` only; the hook's own `onKeyDown` — and so its `Home`/`End` and looping — is dead code for this consumer, which now runs one key handler of its own. #91 cites Rating as the hook's exemplar and that citation is now weaker than it reads |
| 471 | confirmed · source | Select | [Select.tsx:40](src/components/form/Select.tsx#L40 "fp:eae6d908") | low | `pr-10` is Tailwind's default `2.5rem` step, not the `r`-scale the rest of the component's padding uses — off-contract per ETHOS. Pre-existing, documented in `select.md`, and surfaced only because the chevron work put eyes on the line |

## Detail blocks

Added by the 2026-07 reconcile for `med` rows with no spoke in `bugs/components/`.

### 467 · Swimlane — the published page contradicts the component (med)

Surfaced while fixing #174–#176, and **not** caused by that work — both halves predate it.

`docs/components/swimlane.md` states twice that rest props do not reach the DOM:

| where | what it says |
| ----- | ------------ |
| `:35` (prop table) | "`section` props minus `title` — **typed but never reach the DOM**" |
| `:249` (accessibility) | "`id`, `style`, `role`, `aria-*`, `data-*` and event handlers compile and are then dropped" |

Both are false. `Swimlane.tsx:66` renders `<section … {...rest}>` and `:79` does the same on the
animated path; `Swimlane.test.tsx` has a passing *"forwards rest props with the reveal off"*
asserting `id` and `data-analytics` land on the element. #171 fixed this in the code and the page
was never reconciled.

The consequence is worse than a stale sentence. The accessibility section reasons *from* the
false claim to a false conclusion — "The section cannot be named… `aria-label`/`aria-labelledby`
are among the props that never reach the DOM" — so a reader who needs to name the landmark is
told, on a page shipped to npm, that the library cannot do it. It can, with the obvious prop.

Fix: correct `:35` and `:249`, then rewrite the accessibility bullet to say `aria-label` works.
The Gotchas section already describes the true behaviour, so the page contradicts itself and only
one half needs to move.

### 468 · menu-internals — typeahead swallows text typed into a trigger (med)

The sibling #125 did not close. That row named `useListNavigation`'s reference handler, which
`preventDefault`s Arrow/Home/End and froze the caret of a `<textarea>` inside a
`ContextMenu.Trigger`; the fix wraps **only** Floating UI's own reference `onKeyDown` and skips
caret keys aimed at `input`/`textarea`/`select`/`contenteditable`.

`useTypeahead` (`menu-internals.tsx:160`) has the identical shape and never got the exemption, so
every **printable** character typed into that same `<textarea>` is consumed as a typeahead query
against the menu items. The caret moves now; the letters still do not arrive.

Fix: apply #125's existing text-control exemption to the typeahead handler as well. The predicate
is already written and already tested — this is reuse, not new logic.

A regression test belongs on the composed path, not on `useTypeahead` in isolation: the defect
only exists because the handler is mounted on the *reference*, and a unit test of the hook would
pass while the trigger stayed broken.

### 446 · response-ui-css — headings ignore the theme's weight tokens (med, cross-package)

`src/responsive/text.css` publishes the weight contract and then declines to use it:

| | value | source |
| - | ----- | ------ |
| `--Bold-Weight` | 600 below 40rem, 700 above; events 800, tech 600, grimdark 900 | `:33-34`, `:69-70`, `themes/*.css:65-68` |
| `--font-weight-bold` | `var(--Bold-Weight)` | `:110` |
| `h1`–`h5` | literal `700` | `:147, :154, :161, :168, :175` |
| `h6` | literal `600` | `:182` |

So no heading anywhere responds to the breakpoint step-up or to a theme's override — `grimdark`
asks for 900 and gets 700, `tech` asks for 600 and gets 700. `d706bf2` fixed exactly this defect
in the react-components component CSS and stopped at the package boundary.

The fix is one substitution per rule and lands in `response-ui-css`, so it needs a version bump
there and a dependency bump in react-components (see the repo root `CLAUDE.md`).
